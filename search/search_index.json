{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Contango","text":"<p>Contango is a python engine to backtest, parameterize, and graph trading strategies.</p>"},{"location":"#getting-started","title":"Getting Started","text":"<ul> <li>Installation</li> </ul>"},{"location":"#strategies","title":"Strategies","text":"<p>There's two ways to build a strategy. If you want full control, subclass <code>Strategy</code> directly and write everything yourself. If you'd rather create strategies quicker, <code>RuleBasedStrategy</code> gives you streams, conditions, rules, intents, and position sizing so you can compose a strategy without writing as much boilerplate.</p> <ul> <li>Strategy</li> <li>Rule-Based Strategy<ul> <li>Streams</li> <li>Conditions</li> <li>Actions</li> <li>Rules</li> <li>Intents</li> <li>Position Sizers</li> <li>Contexts</li> </ul> </li> <li>Indicators</li> </ul>"},{"location":"#data","title":"Data","text":"<p>Historical data comes from a data provider (Yfinance works with no API key needed), gets checked against a calendar for expected timestamps, and gets cached locally so you're not polling the same data.</p> <ul> <li>Data Repository</li> <li>Historical Data Providers</li> <li>Calendars</li> </ul>"},{"location":"#running-backtests","title":"Running Backtests","text":"<ul> <li>Engine</li> <li>Backtester</li> <li>Static Runner - runs a single strategy once.</li> <li>Grid Search Runner - runs every combination of a parameter grid.</li> </ul>"},{"location":"#analysis","title":"Analysis","text":"<ul> <li>Metric Generation</li> <li>Graphing</li> </ul>"},{"location":"data/repository/","title":"Data Repository","text":""},{"location":"data/repository/#what-is-it","title":"What is it?","text":"<p>The data repository, part of the internal API, allows for OHLCV data (<code>MarketDataEvent</code> instances) to be stored and easily retrieved instead of always polling historical brokers. Further, it permits the use of data offline if retrieving historical data through the internet is not accessible at the time.</p> <p>The <code>get_data_and_store</code> method must be called with a broker, the brokers respective configuration, and the expected timestamps for the call (generally derived from the <code>calender</code> attribute in a broker instance). The <code>get_data_and_store</code> method will check the storage, use any data available in the storage first, and then poll brokers for the additional data. The additional data will then be fed into the storage for further use.</p>"},{"location":"data/repository/#where-is-the-database-stored","title":"Where is the database stored?","text":"<p>It depends. If a database path is supplied to the data repository during initialization, it will be stored there. If not, it defaults to the OS-standard user directory. On windows, this is <code>%LOCALAPPDATA%\\contango\\contango\\database_storage.db</code> (e.g. <code>C:\\Users\\&lt;username&gt;\\AppData\\Local\\contango\\contango\\database_storage.db</code>) </p> <p>See data repository reference</p>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>Requires Python 3.11+ &amp; git</p>"},{"location":"getting-started/installation/#open-a-terminal-and-clone-the-repository","title":"Open a terminal and clone the repository:","text":"<p>Windows/Linux/macOS</p> <pre><code>git clone https://github.com/igowuu/Contango.git\n</code></pre>"},{"location":"getting-started/installation/#change-into-the-project-directory","title":"Change into the project directory:","text":"<p>Windows/Linux/macOS</p> <pre><code>cd Contango\n</code></pre>"},{"location":"getting-started/installation/#create-a-virtual-environment","title":"Create a virtual environment:","text":"<p>Windows</p> <pre><code>python -m venv .venv\n</code></pre> <p>Linux/macOS</p> <pre><code>python3 -m venv .venv\n</code></pre>"},{"location":"getting-started/installation/#activate-your-virtual-environment","title":"Activate your virtual environment:","text":"<p>Windows</p> <pre><code>.venv\\Scripts\\activate\n</code></pre> <p>Linux/macOS</p> <pre><code>source .venv/bin/activate\n</code></pre>"},{"location":"getting-started/installation/#install-required-packages-in-the-root-directory","title":"Install required packages (in the root directory):","text":"<p>Windows/Linux/macOS</p> <pre><code>pip install -r requirements.txt\n</code></pre>"},{"location":"market/calendar/","title":"Calendars","text":""},{"location":"market/calendar/#what-is-it","title":"What is it?","text":"<p>A calendar is a set of available trading dates for a certain method of trading. For example, one calendar may include only weekdays from 9:00AM to 4:00PM excluding holidays (stocks). They allow for the database to retrieve any necessary data that isn't already in the storage, effectively limiting the amount of historical API calls made.</p> <p>See calendar reference</p>"},{"location":"market/calendar/#nyse-calendar","title":"NYSE Calendar","text":"<p>The NYSE calendar includes all weekdays from 9:00AM to 4:00PM, excluding holidays. This is commonly used for trading stocks.</p> <p>See NYSE calendar reference</p>"},{"location":"market/calendar/#creating-a-calendar","title":"Creating a Calendar","text":"<p>To create your own calendar, subclass the <code>Calendar</code> abstract class and implement the <code>get_expected_timestamps</code> method:</p> <pre><code>class MyCalendar(Calendar):\n    def get_expected_timestamps(\n        self,\n        start_timestamp: int,\n        end_timestamp: int,\n        interval: Interval,\n    ) -&gt; tuple[datetime, ...]:\n        \"\"\"\n        Returns the expected available timestamps in a calendar for a specified period.\n\n        Args:\n            start_timestamp: The start time in unix ms.\n            end_timestamp: The end time in unix ms.\n            interval: The bar interval type.\n\n        Returns:\n            tuple[datetime]: The dates &amp; times of the available trading bars for the designated period.\n        \"\"\"\n        # Your implementation here\n        ...\n</code></pre> <p>One recommended resource for implementing your own calendars is the <code>exchange_calendars</code> library, which is used for the NYSE calendar and is a dependency for the project.</p>"},{"location":"market/historical-data-provider/","title":"Historical Data Providers","text":""},{"location":"market/historical-data-provider/#what-is-it","title":"What is it?","text":"<p>A historical data provider provides OHLCV data from an external API at the lowest level. Each <code>HistoricalDataProvider</code> instance converts the actual data provider's format into the expected format for the rest of the code - a list of <code>MarketDataEvent</code> instances. Each data provider corresponds to a separate source - for example, a <code>Yfinance</code> HistoricalDataProvider derives data from exclusively Yahoo Finance.</p> <p>Each historical data provider has a corresponding config, which allows for unique parameters to be passed to each instance.</p> <p>See historical data provdier reference.</p>"},{"location":"market/historical-data-provider/#yfinance","title":"Yfinance","text":"<p>The <code>Yfinance</code> data provider allows for access to historical data without the necessity of an API key. However, Yfinance has its limitations; data may be sparse (as in, it may occasionally skip bars), strict data and rate limits are enforced, and not all symbols are supported (nor give accurate data).</p> <p>See Yfinance reference</p>"},{"location":"market/historical-data-provider/#creating-a-historical-data-provider","title":"Creating a Historical Data Provider","text":"<pre><code>class MyDataProvider(Generic[TConfig]):\n    \"\"\"\n    A data provider from a single source.\n\n    Attributes:\n        TConfig: A corresponding config to the data provider. I.e. \n                 Yfinance -&gt; YfinanceConfig.\n    \"\"\"\n    @abstractmethod\n    def get_bars(self, config: TConfig) -&gt; list[MarketDataEvent]:\n        \"\"\"\n        Returns a list of market data events for any configuration parameters. This polls the actual source and converts the results from the source into `MarketDataEvent` instances.\n        \"\"\"\n        # Your implementation here.\n        ...\n\n    @abstractmethod\n    def get_expected_timestamps(\n        self,\n        config: TConfig,\n    ) -&gt; Iterable[datetime]:\n        \"\"\"\n        Returns an Iterable of the expected timestamps for the start &amp; finishing timestamps of a configuration. Typically speaking, this comes from a designated `Calendar` implementation supplied during initialization.\n        \"\"\"\n        ...\n</code></pre>"},{"location":"reference/data/data_repository/","title":"Data Repository","text":""},{"location":"reference/data/data_repository/#contangodatadata_repository","title":"<code>contango.data.data_repository</code>","text":""},{"location":"reference/data/data_repository/#contango.data.data_repository","title":"contango.data.data_repository","text":""},{"location":"reference/data/data_repository/#contango.data.data_repository.DataRepository","title":"DataRepository","text":"<p>               Bases: <code>Generic[TConfig]</code></p> <p>Retrieves data from either internal storage, a data provider, or both. Data that is not already in storage is automatically added.</p>"},{"location":"reference/data/data_repository/#contango.data.data_repository.DataRepository.get_data_and_store","title":"get_data_and_store  <code>staticmethod</code>","text":"<pre><code>get_data_and_store(provider: HistoricalDataProvider[TConfig], config: TConfig, expected_timestamps: Iterable[datetime], database_path: str | Path | None = None) -&gt; list[MarketDataEvent]\n</code></pre> <p>Retrieves data from either the database, a provider, or both. Any data not in the database that is retrieved from the provider is then put into storage for further use.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>HistoricalDataProvider[TConfig]</code> <p>The historical provider to derive data from if necessary.</p> required <code>config</code> <code>TConfig</code> <p>The corresponding config to the historical provider.</p> required <code>expected_timestamps</code> <code>Iterable[datetime]</code> <p>An iterable of the expected timestamps.</p> required <code>database_path</code> <code>str | Path | None</code> <p>Where the database should live. If not provided,            DataStorage resolves it via  the OS-standard            user data directory.</p> <code>None</code> Source code in <code>src\\contango\\data\\data_repository.py</code> <pre><code>@staticmethod\ndef get_data_and_store(\n    provider: HistoricalDataProvider[TConfig],\n    config: TConfig,\n    expected_timestamps: Iterable[datetime],\n    database_path: str | Path | None = None,\n) -&gt; list[MarketDataEvent]:\n    \"\"\"\n    Retrieves data from either the database, a provider, or both.\n    Any data not in the database that is retrieved from the provider is then put into storage for further use.\n\n    Args:\n        provider: The historical provider to derive data from if necessary.\n        config: The corresponding config to the historical provider.\n        expected_timestamps: An iterable of the expected timestamps.\n        database_path: Where the database should live. If not provided,\n                       DataStorage resolves it via  the OS-standard\n                       user data directory.\n    \"\"\"\n    with DataStorage(database_path) as storage:\n        ticker = config.ticker\n        interval = config.interval.__str__()\n        start_timestamp = config.start_timestamp\n        end_timestamp = config.end_timestamp\n        interval_ms = int(config.interval.value.total_seconds() * 1000)\n\n        earliest_available = storage.get_earliest_available(ticker, interval)\n\n        missing_timestamps = storage.get_missing_timestamps(\n            ticker, interval, expected_timestamps, earliest_available=earliest_available\n        )\n        if len(missing_timestamps) != 0:\n            min_timestamp = min(missing_timestamps)\n            max_timestamp = max(missing_timestamps)\n            new_config = replace(\n                config,\n                start_timestamp=min_timestamp,\n                end_timestamp=max(max_timestamp + interval_ms, min_timestamp + interval_ms),\n            )\n\n            try:\n                new_data = provider.get_bars(new_config)\n            except Exception:\n                new_data = []\n\n            if new_data:\n                storage.add_data_to_storage(interval, new_data)\n                discovered_earliest = min(event.timestamp for event in new_data)\n                if earliest_available is None or discovered_earliest &lt; earliest_available:\n                    storage.set_earliest_available(ticker, interval, discovered_earliest)\n                    earliest_available = discovered_earliest\n            else:\n                confirmed_empty_bound = new_config.end_timestamp + interval_ms\n                if earliest_available is None or confirmed_empty_bound &gt; earliest_available:\n                    storage.set_earliest_available(ticker, interval, confirmed_empty_bound)\n                    earliest_available = confirmed_empty_bound\n\n        data = storage.get_data(ticker, interval, start_timestamp, end_timestamp)\n\n    return data\n</code></pre>"},{"location":"reference/data/storage/store_market_data/","title":"Store Market Data","text":""},{"location":"reference/data/storage/store_market_data/#contangodatastoragestore_market_data","title":"<code>contango.data.storage.store_market_data</code>","text":""},{"location":"reference/data/storage/store_market_data/#contango.data.storage.store_market_data","title":"contango.data.storage.store_market_data","text":""},{"location":"reference/data/storage/store_market_data/#contango.data.storage.store_market_data.DataStorage","title":"DataStorage","text":"<pre><code>DataStorage(database_path: str | Path | None = None)\n</code></pre> <p>Allows for the storage of OHLCV data to prevent re-polling API data that has already been received before.</p> <p>Initializes <code>DataStorage</code>.</p> <p>Parameters:</p> Name Type Description Default <code>database_path</code> <code>str | Path | None</code> <p>Where to store the database. If not provided, falls            back to the OS-standard user data directory.</p> <code>None</code> Source code in <code>src\\contango\\data\\storage\\store_market_data.py</code> <pre><code>def __init__(self, database_path: str | Path | None = None) -&gt; None:\n    \"\"\"\n    Initializes `DataStorage`.\n\n    Args:\n        database_path: Where to store the database. If not provided, falls\n                       back to the OS-standard user data directory.\n    \"\"\"\n    resolved_path = Path(database_path) if database_path is not None else _default_database_path()\n    resolved_path.parent.mkdir(parents=True, exist_ok=True)\n\n    self._connection = sqlite3.connect(resolved_path)\n    self.database_path = resolved_path\n\n    self._create_tables()\n</code></pre>"},{"location":"reference/data/storage/store_market_data/#contango.data.storage.store_market_data.DataStorage.add_data_to_storage","title":"add_data_to_storage","text":"<pre><code>add_data_to_storage(interval: str, data: list[MarketDataEvent]) -&gt; None\n</code></pre> <p>Adds market data to storage. Any existig candles with the same symbol/interval/timestamp are ignored.</p> <p>Parameters:</p> Name Type Description Default <code>interval</code> <code>str</code> <p>The interval to save the data as (i.e. <code>1m</code>, <code>5m</code>, etc).</p> required <code>data</code> <code>list[MarketDataEvent]</code> <p>The data to save into the database.</p> required Source code in <code>src\\contango\\data\\storage\\store_market_data.py</code> <pre><code>def add_data_to_storage(\n    self,\n    interval: str,\n    data: list[MarketDataEvent],\n) -&gt; None:\n    \"\"\"\n    Adds market data to storage. Any existig candles with the same symbol/interval/timestamp are ignored.\n\n    Args:\n        interval: The interval to save the data as (i.e. `1m`, `5m`, etc).\n        data: The data to save into the database.\n    \"\"\"\n    self._connection.executemany(\n        \"\"\"\n        INSERT OR IGNORE INTO market_data\n        (\n            symbol,\n            interval,\n            timestamp,\n            open,\n            high,\n            low,\n            close,\n            volume\n        )\n        VALUES (?, ?, ?, ?, ?, ?, ?, ?)\n        \"\"\",\n        [\n            (\n                event.symbol,\n                interval,\n                event.timestamp,\n                event.open,\n                event.high,\n                event.low,\n                event.close,\n                event.volume,\n            )\n            for event in data\n        ],\n    )\n\n    self._connection.commit()\n</code></pre>"},{"location":"reference/data/storage/store_market_data/#contango.data.storage.store_market_data.DataStorage.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Closes the connection to the database.</p> Source code in <code>src\\contango\\data\\storage\\store_market_data.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"\n    Closes the connection to the database.\n    \"\"\"\n    self._connection.close()\n</code></pre>"},{"location":"reference/data/storage/store_market_data/#contango.data.storage.store_market_data.DataStorage.get_data","title":"get_data","text":"<pre><code>get_data(symbol: str, interval: str, start_timestamp: int, end_timestamp: int) -&gt; list[MarketDataEvent]\n</code></pre> <p>Retrieves market data in order by timestamp.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>The symbol to derive data from.</p> required <code>interval</code> <code>str</code> <p>The interval to retrieve data from for the symbol (i.e. <code>1m</code>, <code>5m</code>, etc).</p> required <code>start_timestamp</code> <code>int</code> <p>The start timestamp in unix ms.</p> required <code>end_timestamp</code> <code>int</code> <p>The end timestamp in unix ms.</p> required <p>Returns:</p> Type Description <code>list[MarketDataEvent]</code> <p>list[MarketDataEvent]: The data for the provided parameters.</p> Source code in <code>src\\contango\\data\\storage\\store_market_data.py</code> <pre><code>def get_data(\n    self,\n    symbol: str,\n    interval: str,\n    start_timestamp: int,\n    end_timestamp: int,\n) -&gt; list[MarketDataEvent]:\n    \"\"\"\n    Retrieves market data in order by timestamp.\n\n    Args:\n        symbol: The symbol to derive data from.\n        interval: The interval to retrieve data from for the symbol (i.e. `1m`, `5m`, etc).\n        start_timestamp: The start timestamp in unix ms.\n        end_timestamp: The end timestamp in unix ms.\n\n    Returns:\n        list[MarketDataEvent]: The data for the provided parameters.\n    \"\"\"\n    cursor = self._connection.execute(\n        \"\"\"\n        SELECT\n            timestamp,\n            symbol,\n            open,\n            high,\n            low,\n            close,\n            volume\n        FROM market_data\n        WHERE symbol = ?\n          AND interval = ?\n          AND timestamp BETWEEN ? AND ?\n        ORDER BY timestamp ASC\n        \"\"\",\n        (\n            symbol,\n            interval,\n            start_timestamp,\n            end_timestamp,\n        ),\n    )\n\n    return [\n        MarketDataEvent(\n            timestamp=row[0],\n            symbol=row[1],\n            open=row[2],\n            high=row[3],\n            low=row[4],\n            close=row[5],\n            volume=row[6],\n        )\n        for row in cursor.fetchall()\n    ]\n</code></pre>"},{"location":"reference/data/storage/store_market_data/#contango.data.storage.store_market_data.DataStorage.get_earliest_available","title":"get_earliest_available","text":"<pre><code>get_earliest_available(symbol: str, interval: str) -&gt; int | None\n</code></pre> <p>Returns the earliest timestamp (unix ms) known to be a valid lower bound for this symbol/interval's data, if one has been recorded. None if never recorded.</p> Source code in <code>src\\contango\\data\\storage\\store_market_data.py</code> <pre><code>def get_earliest_available(self, symbol: str, interval: str) -&gt; int | None:\n    \"\"\"\n    Returns the earliest timestamp (unix ms) known to be a valid lower\n    bound for this symbol/interval's data, if one has been recorded. None if never recorded.\n    \"\"\"\n    cursor = self._connection.execute(\n        \"\"\"\n        SELECT earliest_available_timestamp\n        FROM symbol_coverage\n        WHERE symbol = ? AND interval = ?\n        \"\"\",\n        (symbol, interval),\n    )\n    row = cursor.fetchone()\n    return row[0] if row is not None else None\n</code></pre>"},{"location":"reference/data/storage/store_market_data/#contango.data.storage.store_market_data.DataStorage.get_missing_timestamps","title":"get_missing_timestamps","text":"<pre><code>get_missing_timestamps(symbol: str, interval: str, expected_timestamps: Iterable[datetime], earliest_available: int | None = None) -&gt; list[int]\n</code></pre> <p>Returns the timestamps that are NOT currently stored for the given symbol or interval, out of a supplied set of expected timestamps.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>The symbol to check.</p> required <code>interval</code> <code>str</code> <p>The interval to check (i.e. <code>1m</code>, <code>5m</code>, etc).</p> required <code>expected_timestamps</code> <code>Iterable[datetime]</code> <p>The datetimes candles are expected to exist                  for. Must be timezone-aware (or assumed UTC if naive) since                  stored timestamps are unix ms in UTC.</p> required <code>earliest_available</code> <code>int | None</code> <p>If provided, expected timestamps earlier than                 this (unix ms) are dropped before the missing-check,                 since they're already known to have no data                 available from the provider.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[int]</code> <p>list[int]: Sorted list of missing timestamps (unix ms).</p> Source code in <code>src\\contango\\data\\storage\\store_market_data.py</code> <pre><code>def get_missing_timestamps(\n    self,\n    symbol: str,\n    interval: str,\n    expected_timestamps: Iterable[datetime],\n    earliest_available: int | None = None,\n) -&gt; list[int]:\n    \"\"\"\n    Returns the timestamps that are NOT currently stored for the given\n    symbol or interval, out of a supplied set of expected timestamps.\n\n    Args:\n        symbol: The symbol to check.\n        interval: The interval to check (i.e. `1m`, `5m`, etc).\n        expected_timestamps: The datetimes candles are expected to exist\n                             for. Must be timezone-aware (or assumed UTC if naive) since\n                             stored timestamps are unix ms in UTC.\n        earliest_available: If provided, expected timestamps earlier than\n                            this (unix ms) are dropped before the missing-check,\n                            since they're already known to have no data\n                            available from the provider.\n\n    Returns:\n        list[int]: Sorted list of missing timestamps (unix ms).\n    \"\"\"\n    expected_ms = {\n        int(\n            (ts if ts.tzinfo else ts.replace(tzinfo=timezone.utc))\n            .timestamp() * 1000\n        )\n        for ts in expected_timestamps\n    }\n\n    if earliest_available is not None:\n        expected_ms = {ts for ts in expected_ms if ts &gt;= earliest_available}\n\n    if not expected_ms:\n        return []\n\n    cursor = self._connection.execute(\n        \"\"\"\n        SELECT timestamp\n        FROM market_data\n        WHERE symbol = ?\n        AND interval = ?\n        AND timestamp BETWEEN ? AND ?\n        \"\"\",\n        (\n            symbol,\n            interval,\n            min(expected_ms),\n            max(expected_ms),\n        ),\n    )\n\n    existing = {row[0] for row in cursor.fetchall()}\n\n    return sorted(expected_ms - existing)\n</code></pre>"},{"location":"reference/data/storage/store_market_data/#contango.data.storage.store_market_data.DataStorage.set_earliest_available","title":"set_earliest_available","text":"<pre><code>set_earliest_available(symbol: str, interval: str, timestamp: int) -&gt; None\n</code></pre> <p>Records the earliest known-valid lower bound (unix ms) for the symbol/interval.</p> Source code in <code>src\\contango\\data\\storage\\store_market_data.py</code> <pre><code>def set_earliest_available(self, symbol: str, interval: str, timestamp: int) -&gt; None:\n    \"\"\"\n    Records the earliest known-valid lower bound (unix ms) for the\n    symbol/interval.\n    \"\"\"\n    self._connection.execute(\n        \"\"\"\n        INSERT INTO symbol_coverage (symbol, interval, earliest_available_timestamp)\n        VALUES (?, ?, ?)\n        ON CONFLICT(symbol, interval) DO UPDATE SET earliest_available_timestamp = excluded.earliest_available_timestamp\n        \"\"\",\n        (symbol, interval, timestamp),\n    )\n    self._connection.commit()\n</code></pre>"},{"location":"reference/market/calendar/calendar/","title":"Calendar","text":""},{"location":"reference/market/calendar/calendar/#contangomarketcalendarcalendar","title":"<code>contango.market.calendar.calendar</code>","text":""},{"location":"reference/market/calendar/calendar/#contango.market.calendar.calendar","title":"contango.market.calendar.calendar","text":""},{"location":"reference/market/calendar/calendar/#contango.market.calendar.calendar.Calendar","title":"Calendar","text":"<p>               Bases: <code>ABC</code></p> <p>A delegation of the available trading times for calendar type (i.e. NYSE or Crypto).</p>"},{"location":"reference/market/calendar/calendar/#contango.market.calendar.calendar.Calendar.get_expected_timestamps","title":"get_expected_timestamps  <code>abstractmethod</code>","text":"<pre><code>get_expected_timestamps(start_timestamp: int, end_timestamp: int, interval: Interval) -&gt; tuple[datetime, ...]\n</code></pre> <p>Returns the expected available timestamps in a calendar for a specified period.</p> <p>Parameters:</p> Name Type Description Default <code>start_timestamp</code> <code>int</code> <p>The start time in unix ms.</p> required <code>end_timestamp</code> <code>int</code> <p>The end time in unix ms.</p> required <code>interval</code> <code>Interval</code> <p>The bar interval type.</p> required <p>Returns:</p> Type Description <code>tuple[datetime, ...]</code> <p>tuple[datetime]: The dates &amp; times of the available trading bars for the designated period.</p> Source code in <code>src\\contango\\market\\calendar\\calendar.py</code> <pre><code>@abstractmethod\ndef get_expected_timestamps(\n    self,\n    start_timestamp: int,\n    end_timestamp: int,\n    interval: Interval,\n) -&gt; tuple[datetime, ...]:\n    \"\"\"\n    Returns the expected available timestamps in a calendar for a specified period.\n\n    Args:\n        start_timestamp: The start time in unix ms.\n        end_timestamp: The end time in unix ms.\n        interval: The bar interval type.\n\n    Returns:\n        tuple[datetime]: The dates &amp; times of the available trading bars for the designated period.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/market/calendar/nyse_calendar/","title":"NYSE Calendar","text":""},{"location":"reference/market/calendar/nyse_calendar/#contangomarketcalendarnyse_calendar","title":"<code>contango.market.calendar.nyse_calendar</code>","text":""},{"location":"reference/market/calendar/nyse_calendar/#contango.market.calendar.nyse_calendar","title":"contango.market.calendar.nyse_calendar","text":""},{"location":"reference/market/calendar/nyse_calendar/#contango.market.calendar.nyse_calendar.NYSECalendar","title":"NYSECalendar","text":"<pre><code>NYSECalendar()\n</code></pre> <p>               Bases: <code>Calendar</code></p> <p>Trading calendar for the New York Stock Exchange (9:00AM-4:00PM on weekdays, holidays taken into account).</p> <p>Initializes `NYSECalendar.</p> Source code in <code>src\\contango\\market\\calendar\\nyse_calendar.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"\n    Initializes `NYSECalendar.\n    \"\"\"\n    # xcals is cached by the library, so this does not have to be manually cached.\n    self._calendar = xcals.get_calendar(\"XNYS\", start=\"1990-01-01\")\n</code></pre>"},{"location":"reference/market/calendar/nyse_calendar/#contango.market.calendar.nyse_calendar.NYSECalendar.get_expected_timestamps","title":"get_expected_timestamps","text":"<pre><code>get_expected_timestamps(start_timestamp: int, end_timestamp: int, interval: Interval) -&gt; tuple[datetime, ...]\n</code></pre> <p>Returns the expected available NYSE timestamps for the specified period.</p> <p>Parameters:</p> Name Type Description Default <code>start_timestamp</code> <code>int</code> <p>The start time in unix ms.</p> required <code>end_timestamp</code> <code>int</code> <p>The end time in unix ms.</p> required <code>interval</code> <code>Interval</code> <p>The bar interval type.</p> required <p>Returns:</p> Type Description <code>tuple[datetime, ...]</code> <p>tuple[datetime]: The close times of the available NYSE trading bars for the designated period.</p> Source code in <code>src\\contango\\market\\calendar\\nyse_calendar.py</code> <pre><code>def get_expected_timestamps(\n    self,\n    start_timestamp: int,\n    end_timestamp: int,\n    interval: Interval,\n) -&gt; tuple[datetime, ...]:\n    \"\"\"\n    Returns the expected available NYSE timestamps for the specified period.\n\n    Args:\n        start_timestamp: The start time in unix ms.\n        end_timestamp: The end time in unix ms.\n        interval: The bar interval type.\n\n    Returns:\n        tuple[datetime]: The close times of the available NYSE trading bars for the designated period.\n    \"\"\"\n    return self._get_expected_timestamps_cached(start_timestamp, end_timestamp, interval)\n</code></pre>"},{"location":"reference/market/data_providers/config/","title":"Config","text":""},{"location":"reference/market/data_providers/config/#contangomarketdata_providersconfig","title":"<code>contango.market.data_providers.config</code>","text":""},{"location":"reference/market/data_providers/config/#contango.market.data_providers.config","title":"contango.market.data_providers.config","text":""},{"location":"reference/market/data_providers/config/#contango.market.data_providers.config.Config","title":"Config  <code>dataclass</code>","text":"<pre><code>Config(ticker: str, interval: Interval, start_timestamp: int, end_timestamp: int)\n</code></pre> <p>Marks a dataclass as a configurator for a historical data provider when deriving data from it.</p> <p>Attributes:</p> Name Type Description <code>ticker</code> <code>str</code> <p>The ticker symbol for a data provider call.</p> <code>interval</code> <code>Interval</code> <p>The interval for a data provider call.</p> <code>start_timestamp</code> <code>int</code> <p>The start time in unix ms to derive data from.</p> <code>end_timestamp</code> <code>int</code> <p>The end time in unix ms to derive data from.</p>"},{"location":"reference/market/data_providers/historical_data_provider/","title":"Historical Data Provider","text":""},{"location":"reference/market/data_providers/historical_data_provider/#contangomarketdata_providershistorical_data_provider","title":"<code>contango.market.data_providers.historical_data_provider</code>","text":""},{"location":"reference/market/data_providers/historical_data_provider/#contango.market.data_providers.historical_data_provider","title":"contango.market.data_providers.historical_data_provider","text":""},{"location":"reference/market/data_providers/historical_data_provider/#contango.market.data_providers.historical_data_provider.HistoricalDataProvider","title":"HistoricalDataProvider","text":"<p>               Bases: <code>ABC</code>, <code>Generic[TConfig]</code></p> <p>A single broker (or data provider) for historical market data.</p>"},{"location":"reference/market/data_providers/historical_data_provider/#contango.market.data_providers.historical_data_provider.HistoricalDataProvider.get_bars","title":"get_bars  <code>abstractmethod</code>","text":"<pre><code>get_bars(config: TConfig) -&gt; list[MarketDataEvent]\n</code></pre> <p>Returns a list of market data events for any configuration parameters.</p> Source code in <code>src\\contango\\market\\data_providers\\historical_data_provider.py</code> <pre><code>@abstractmethod\ndef get_bars(self, config: TConfig) -&gt; list[MarketDataEvent]:\n    \"\"\"\n    Returns a list of market data events for any configuration parameters.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/market/data_providers/historical_data_provider/#contango.market.data_providers.historical_data_provider.HistoricalDataProvider.get_expected_timestamps","title":"get_expected_timestamps  <code>abstractmethod</code>","text":"<pre><code>get_expected_timestamps(config: TConfig) -&gt; Iterable[datetime]\n</code></pre> <p>Returns an Iterable of the expected timestamps for the start &amp; finishing timestamps of a configuration.</p> Source code in <code>src\\contango\\market\\data_providers\\historical_data_provider.py</code> <pre><code>@abstractmethod\ndef get_expected_timestamps(\n    self,\n    config: TConfig,\n) -&gt; Iterable[datetime]:\n    \"\"\"\n    Returns an Iterable of the expected timestamps for the start &amp; finishing timestamps of a configuration.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/market/data_providers/interval/","title":"Interval","text":""},{"location":"reference/market/data_providers/interval/#contangomarketdata_providersinterval","title":"<code>contango.market.data_providers.interval</code>","text":""},{"location":"reference/market/data_providers/interval/#contango.market.data_providers.interval","title":"contango.market.data_providers.interval","text":""},{"location":"reference/market/data_providers/interval/#contango.market.data_providers.interval.Interval","title":"Interval","text":"<p>               Bases: <code>Enum</code></p> <p>The interval type to determine how frequent of bars to derive from a data provider.</p>"},{"location":"reference/market/data_providers/yfinance/yfinance/","title":"YFinance","text":""},{"location":"reference/market/data_providers/yfinance/yfinance/#contangomarketdata_providersyfinanceyfinance","title":"<code>contango.market.data_providers.yfinance.yfinance</code>","text":""},{"location":"reference/market/data_providers/yfinance/yfinance/#contango.market.data_providers.yfinance.yfinance","title":"contango.market.data_providers.yfinance.yfinance","text":""},{"location":"reference/market/data_providers/yfinance/yfinance/#contango.market.data_providers.yfinance.yfinance.Yfinance","title":"Yfinance","text":"<pre><code>Yfinance(calendar: Calendar)\n</code></pre> <p>               Bases: <code>HistoricalDataProvider[YfinanceConfig]</code></p> <p>The yfinance data provider. Data is limited in large quantities, some data may be innacurate, and rate limiting may be enforced with usage.</p> <p>Initializes <code>Yfinance</code>.</p> <p>Parameters:</p> Name Type Description Default <code>calendar</code> <code>Calendar</code> <p>The calender type to follow (i.e. NYSE).</p> required Source code in <code>src\\contango\\market\\data_providers\\yfinance\\yfinance.py</code> <pre><code>def __init__(self, calendar: Calendar) -&gt; None:\n    \"\"\"\n    Initializes `Yfinance`.\n\n    Args:\n        calendar: The calender type to follow (i.e. NYSE).\n    \"\"\"\n    self._calendar = calendar\n</code></pre>"},{"location":"reference/market/data_providers/yfinance/yfinance/#contango.market.data_providers.yfinance.yfinance.Yfinance.get_bars","title":"get_bars","text":"<pre><code>get_bars(config: YfinanceConfig) -&gt; list[MarketDataEvent]\n</code></pre> <p>Returns a list of the bars for the given period.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>YfinanceConfig</code> <p>The yfinance config to derive bars from.</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Upon yfinance not returning any data.</p> Source code in <code>src\\contango\\market\\data_providers\\yfinance\\yfinance.py</code> <pre><code>def get_bars(self, config: YfinanceConfig) -&gt; list[MarketDataEvent]:\n    \"\"\"\n    Returns a list of the bars for the given period.\n\n    Args:\n        config: The yfinance config to derive bars from.\n\n    Raises:\n        RuntimeError: Upon yfinance not returning any data.\n    \"\"\"\n    data = self._load_data(config)\n\n    if isinstance(data.columns, pd.MultiIndex):\n        data.columns = data.columns.get_level_values(0)\n\n    bars: list[MarketDataEvent] = []\n    for timestamp, row in data.iterrows():\n        bars.append(\n            MarketDataEvent(\n                timestamp=int(pd.Timestamp(timestamp).timestamp() * 1000),  # type: ignore[argumentType]\n                symbol=config.ticker,\n                open=float(row[\"Open\"]),\n                high=float(row[\"High\"]),\n                low=float(row[\"Low\"]),\n                close=float(row[\"Close\"]),\n                volume=int(row[\"Volume\"]),\n            )\n        )\n\n    return bars\n</code></pre>"},{"location":"reference/market/data_providers/yfinance/yfinance/#contango.market.data_providers.yfinance.yfinance.Yfinance.get_expected_timestamps","title":"get_expected_timestamps","text":"<pre><code>get_expected_timestamps(config: YfinanceConfig) -&gt; Iterable[datetime]\n</code></pre> <p>Returns the expected yfinance timestamps for the given calendar at initialization.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>YfinanceConfig</code> <p>The configuration, in which the start timestamp, end timestamp, &amp; interval will be used to      derive the valid timestamps from.</p> required <p>Returns:</p> Type Description <code>Iterable[datetime]</code> <p>Iterable[datetime]: An iterable of valid datetime objects.</p> Source code in <code>src\\contango\\market\\data_providers\\yfinance\\yfinance.py</code> <pre><code>def get_expected_timestamps(\n    self,\n    config: YfinanceConfig,\n) -&gt; Iterable[datetime]:\n    \"\"\"\n    Returns the expected yfinance timestamps for the given calendar at initialization.\n\n    Args:\n        config: The configuration, in which the start timestamp, end timestamp, &amp; interval will be used to \n                derive the valid timestamps from.\n\n    Returns:\n        Iterable[datetime]: An iterable of valid datetime objects.\n    \"\"\"\n    return self._calendar.get_expected_timestamps(\n        start_timestamp=config.start_timestamp,\n        end_timestamp=config.end_timestamp,\n        interval=config.interval,\n    )\n</code></pre>"},{"location":"reference/market/data_providers/yfinance/yfinance_config/","title":"YFinance Config","text":""},{"location":"reference/market/data_providers/yfinance/yfinance_config/#contangomarketdata_providersyfinanceyfinance_config","title":"<code>contango.market.data_providers.yfinance.yfinance_config</code>","text":""},{"location":"reference/market/data_providers/yfinance/yfinance_config/#contango.market.data_providers.yfinance.yfinance_config","title":"contango.market.data_providers.yfinance.yfinance_config","text":""},{"location":"reference/market/data_providers/yfinance/yfinance_config/#contango.market.data_providers.yfinance.yfinance_config.YfinanceConfig","title":"YfinanceConfig  <code>dataclass</code>","text":"<pre><code>YfinanceConfig(ticker: str, interval: Interval, start_timestamp: int, end_timestamp: int)\n</code></pre> <p>               Bases: <code>Config</code></p> <p>Holds the configuration for deriving data from the yfinance data provider.</p> <p>Attributes:</p> Name Type Description <code>ticker</code> <code>str</code> <p>The ticker symbol (e.g. AAPL) to derive yfinance data from.</p> <code>start_date</code> <code>str</code> <p>The start date in unix ms to derive yfinance data from.</p> <code>end_date</code> <code>str</code> <p>The end date in unix ms to derive yfinance data from.</p> <code>interval</code> <code>Interval</code> <p>The trading interval to derive yfinance data from.</p>"},{"location":"reference/research/demo_strategies/cartesian_bollinger_strategy/runner/","title":"Runner","text":""},{"location":"reference/research/demo_strategies/cartesian_bollinger_strategy/runner/#contangoresearchdemo_strategiescartesian_bollinger_strategyrunner","title":"<code>contango.research.demo_strategies.cartesian_bollinger_strategy.runner</code>","text":""},{"location":"reference/research/demo_strategies/cartesian_bollinger_strategy/runner/#contango.research.demo_strategies.cartesian_bollinger_strategy.runner","title":"contango.research.demo_strategies.cartesian_bollinger_strategy.runner","text":""},{"location":"reference/research/demo_strategies/cartesian_bollinger_strategy/strategy/","title":"Strategy","text":""},{"location":"reference/research/demo_strategies/cartesian_bollinger_strategy/strategy/#contangoresearchdemo_strategiescartesian_bollinger_strategystrategy","title":"<code>contango.research.demo_strategies.cartesian_bollinger_strategy.strategy</code>","text":""},{"location":"reference/research/demo_strategies/cartesian_bollinger_strategy/strategy/#contango.research.demo_strategies.cartesian_bollinger_strategy.strategy","title":"contango.research.demo_strategies.cartesian_bollinger_strategy.strategy","text":""},{"location":"reference/research/demo_strategies/research_bollinger_strategy/runner/","title":"Runner","text":""},{"location":"reference/research/demo_strategies/research_bollinger_strategy/runner/#contangoresearchdemo_strategiesresearch_bollinger_strategyrunner","title":"<code>contango.research.demo_strategies.research_bollinger_strategy.runner</code>","text":""},{"location":"reference/research/demo_strategies/research_bollinger_strategy/runner/#contango.research.demo_strategies.research_bollinger_strategy.runner","title":"contango.research.demo_strategies.research_bollinger_strategy.runner","text":""},{"location":"reference/research/demo_strategies/research_bollinger_strategy/strategy/","title":"Strategy","text":""},{"location":"reference/research/demo_strategies/research_bollinger_strategy/strategy/#contangoresearchdemo_strategiesresearch_bollinger_strategystrategy","title":"<code>contango.research.demo_strategies.research_bollinger_strategy.strategy</code>","text":""},{"location":"reference/research/demo_strategies/research_bollinger_strategy/strategy/#contango.research.demo_strategies.research_bollinger_strategy.strategy","title":"contango.research.demo_strategies.research_bollinger_strategy.strategy","text":""},{"location":"reference/research/demo_strategies/static_bollinger_strategy/runner/","title":"Runner","text":""},{"location":"reference/research/demo_strategies/static_bollinger_strategy/runner/#contangoresearchdemo_strategiesstatic_bollinger_strategyrunner","title":"<code>contango.research.demo_strategies.static_bollinger_strategy.runner</code>","text":""},{"location":"reference/research/demo_strategies/static_bollinger_strategy/runner/#contango.research.demo_strategies.static_bollinger_strategy.runner","title":"contango.research.demo_strategies.static_bollinger_strategy.runner","text":""},{"location":"reference/research/demo_strategies/static_bollinger_strategy/strategy/","title":"Strategy","text":""},{"location":"reference/research/demo_strategies/static_bollinger_strategy/strategy/#contangoresearchdemo_strategiesstatic_bollinger_strategystrategy","title":"<code>contango.research.demo_strategies.static_bollinger_strategy.strategy</code>","text":""},{"location":"reference/research/demo_strategies/static_bollinger_strategy/strategy/#contango.research.demo_strategies.static_bollinger_strategy.strategy","title":"contango.research.demo_strategies.static_bollinger_strategy.strategy","text":""},{"location":"reference/runners/grid/grid_search_runner/","title":"Grid Search Runner","text":""},{"location":"reference/runners/grid/grid_search_runner/#contangorunnersgridgrid_search_runner","title":"<code>contango.runners.grid.grid_search_runner</code>","text":""},{"location":"reference/runners/grid/grid_search_runner/#contango.runners.grid.grid_search_runner","title":"contango.runners.grid.grid_search_runner","text":""},{"location":"reference/runners/grid/grid_search_runner/#contango.runners.grid.grid_search_runner.ExperimentResult","title":"ExperimentResult","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The resulting experiment of backtesting a single strategy variant from a grid search.</p> <p>Attributes:</p> Name Type Description <code>label</code> <code>str</code> <p>The name for the experiment result.</p> <code>kwargs</code> <code>dict[str, Any]</code> <p>A dictionary mapping strings to the parameters tested for the experiment.</p> <code>metrics</code> <code>Metrics</code> <p>The metrics resulting from the experiment.</p>"},{"location":"reference/runners/grid/grid_search_runner/#contango.runners.grid.grid_search_runner.GridSearchRunner","title":"GridSearchRunner","text":"<p>               Bases: <code>Generic[TConfig]</code></p> <p>Runner for fetching data and executing every strategy variant produced by a Cartesian parameter grid search.</p>"},{"location":"reference/runners/grid/grid_search_runner/#contango.runners.grid.grid_search_runner.GridSearchRunner.fetch_data","title":"fetch_data  <code>staticmethod</code>","text":"<pre><code>fetch_data(provider: HistoricalDataProvider[TConfig], config: TConfig, database_path: str | Path | None = None) -&gt; list[MarketDataEvent]\n</code></pre> <p>Fetches and stores data into the database.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>HistoricalDataProvider[TConfig]</code> <p>The historical data provider (i.e. Yfinance) implementation.</p> required <code>config</code> <code>TConfig</code> <p>The configuration that matches the data provider (i.e. YfinanceConfig).</p> required <code>database_path</code> <code>str | Path | None</code> <p>The optional database path; if not provided, defaults to the OS-standard user data directory.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[MarketDataEvent]</code> <p>list[MarketDataEvent]: The OHLCV data from the database and/or historical data provider.</p> Source code in <code>src\\contango\\runners\\grid\\grid_search_runner.py</code> <pre><code>@staticmethod\ndef fetch_data(\n    provider: HistoricalDataProvider[TConfig],\n    config: TConfig,\n    database_path: str | Path | None = None,\n) -&gt; list[MarketDataEvent]:\n    \"\"\"\n    Fetches and stores data into the database.\n\n    Args:\n        provider: The historical data provider (i.e. Yfinance) implementation.\n        config: The configuration that matches the data provider (i.e. YfinanceConfig).\n        database_path: The optional database path; if not provided, defaults to the OS-standard user data directory.\n\n    Returns:\n        list[MarketDataEvent]: The OHLCV data from the database and/or historical data provider.\n    \"\"\"\n    return DataRepository[config].get_data_and_store(\n        provider=provider,\n        config=config,\n        expected_timestamps=provider.get_expected_timestamps(config),\n        database_path=database_path,\n    )\n</code></pre>"},{"location":"reference/runners/grid/grid_search_runner/#contango.runners.grid.grid_search_runner.GridSearchRunner.fetch_data_multi","title":"fetch_data_multi  <code>staticmethod</code>","text":"<pre><code>fetch_data_multi(provider: HistoricalDataProvider[TConfig], configs: Mapping[str, TConfig], database_path: str | Path | None = None) -&gt; dict[str, list[MarketDataEvent]]\n</code></pre> <p>Fetches and stores data for multiple symbols, keyed by the same symbol strings intend to use in param_grid[\"symbol\"].</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>HistoricalDataProvider[TConfig]</code> <p>The historical data provider (i.e. Yfinance) implementation.</p> required <code>configs</code> <code>Mapping[str, TConfig]</code> <p>A mapping of symbol -&gt; config for the symbol (e.g. {\"AAPL\": YfinanceConfig(ticker=\"AAPL\", ...)}).</p> required <code>database_path</code> <code>str | Path | None</code> <p>The optional database path. if not provided, defaults to the OS-standard user data directory.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, list[MarketDataEvent]]</code> <p>dict[str, list[MarketDataEvent]]: OHLCV data per symbol.</p> Source code in <code>src\\contango\\runners\\grid\\grid_search_runner.py</code> <pre><code>@staticmethod\ndef fetch_data_multi(\n    provider: HistoricalDataProvider[TConfig],\n    configs: Mapping[str, TConfig],\n    database_path: str | Path | None = None,\n) -&gt; dict[str, list[MarketDataEvent]]:\n    \"\"\"\n    Fetches and stores data for multiple symbols, keyed by the same\n    symbol strings intend to use in param_grid[\"symbol\"].\n\n    Args:\n        provider: The historical data provider (i.e. Yfinance) implementation.\n        configs: A mapping of symbol -&gt; config for the symbol (e.g. {\"AAPL\": YfinanceConfig(ticker=\"AAPL\", ...)}).\n        database_path: The optional database path. if not provided, defaults to the OS-standard user data directory.\n\n    Returns:\n        dict[str, list[MarketDataEvent]]: OHLCV data per symbol.\n    \"\"\"\n    return {\n        symbol: GridSearchRunner[TConfig].fetch_data(provider=provider, config=config, database_path=database_path)\n        for symbol, config in configs.items()\n    }\n</code></pre>"},{"location":"reference/runners/grid/grid_search_runner/#contango.runners.grid.grid_search_runner.GridSearchRunner.run","title":"run  <code>staticmethod</code>","text":"<pre><code>run(ohlcv_data: Mapping[str, list[MarketDataEvent]], strategy_cls: type[StrategyT], param_grid: Mapping[str, Sequence[Any]], backtester_config: BacktesterConfig, base_label: str | None = None, verbose: bool = True) -&gt; list[ExperimentResult]\n</code></pre> <p>Runs a backtest for multiple instances of a strategy, against per-symbol data.</p> <p>Parameters:</p> Name Type Description Default <code>ohlcv_data</code> <code>Mapping[str, list[MarketDataEvent]]</code> <p>A mapping of symbol -&gt; historical market data events for the symbol.         Every value that appears in param_grid[\"symbol\"] must be a key here.</p> required <code>strategy_cls</code> <code>type[StrategyT]</code> <p>The strategy class that will be parameterized.</p> required <code>param_grid</code> <code>Mapping[str, Sequence[Any]]</code> <p>A mapping of strings (parameter names) to sequences of the kwargs to test.         Must include a \"symbol\" key whose values are all present in ohlcv_data.</p> required <code>base_label</code> <code>str | None</code> <p>The base label for each result.</p> <code>None</code> <code>backtester_config</code> <code>BacktesterConfig</code> <p>The configuration for the backtester.</p> required <code>verbose</code> <code>bool</code> <p>Whether to print the current strategy being tested to terminal.</p> <code>True</code> <p>Returns:</p> Type Description <code>list[ExperimentResult]</code> <p>list[ExperimentResult]: The metrics along with parameters &amp; labels that were tested.</p> Source code in <code>src\\contango\\runners\\grid\\grid_search_runner.py</code> <pre><code>@staticmethod\ndef run(\n    ohlcv_data: Mapping[str, list[MarketDataEvent]],\n    strategy_cls: type[StrategyT],\n    param_grid: Mapping[str, Sequence[Any]],\n    backtester_config: BacktesterConfig,\n    base_label: str | None = None,\n    verbose: bool = True\n) -&gt; list[ExperimentResult]:\n    \"\"\"\n    Runs a backtest for multiple instances of a strategy, against\n    per-symbol data.\n\n    Args:\n        ohlcv_data: A mapping of symbol -&gt; historical market data events for the symbol.\n                    Every value that appears in param_grid[\"symbol\"] must be a key here.\n        strategy_cls: The strategy class that will be parameterized.\n        param_grid: A mapping of strings (parameter names) to sequences of the kwargs to test.\n                    Must include a \"symbol\" key whose values are all present in ohlcv_data.\n        base_label: The base label for each result.\n        backtester_config: The configuration for the backtester.\n        verbose: Whether to print the current strategy being tested to terminal.\n\n    Returns:\n        list[ExperimentResult]: The metrics along with parameters &amp; labels that were tested.\n    \"\"\"\n    if \"symbol\" not in param_grid:\n        raise ValueError('param_grid must include a \"symbol\" key to select data from ohlcv_data')\n\n    results: list[ExperimentResult] = []\n    base_label = base_label if base_label is not None else strategy_cls.__name__\n    varying_keys = GridSearchRunner._varying_keys(param_grid)\n    for factory, kwargs in cartesian_search(strategy_cls, param_grid):\n        symbol = kwargs[\"symbol\"]\n        if symbol not in ohlcv_data:\n            raise KeyError(f\"No fetched data for symbol {symbol!r}; available symbols: {sorted(ohlcv_data)}\")\n\n        name = f\"{base_label}({GridSearchRunner._format_kwargs(kwargs, varying_keys)})\"\n\n        if verbose:\n            logger.info(f\"Testing {name}\")\n\n        raw_results = StrategyBacktester.backtest(\n            ohlcv_data=ohlcv_data[symbol], strategy=factory(), config=backtester_config\n        )\n        results.append(\n            ExperimentResult(\n                label=name, \n                kwargs=dict(kwargs), \n                metrics=calculate_metrics(raw_results)\n            )\n        )\n    return results\n</code></pre>"},{"location":"reference/runners/static/static_runner/","title":"Static Runner","text":""},{"location":"reference/runners/static/static_runner/#contangorunnersstaticstatic_runner","title":"<code>contango.runners.static.static_runner</code>","text":""},{"location":"reference/runners/static/static_runner/#contango.runners.static.static_runner","title":"contango.runners.static.static_runner","text":""},{"location":"reference/runners/static/static_runner/#contango.runners.static.static_runner.StaticRunner","title":"StaticRunner","text":"<p>               Bases: <code>Generic[TConfig]</code></p> <p>Runner for fetching data and executing a singular, unchanging <code>Strategy</code> implementation.</p>"},{"location":"reference/runners/static/static_runner/#contango.runners.static.static_runner.StaticRunner.fetch_data","title":"fetch_data  <code>staticmethod</code>","text":"<pre><code>fetch_data(provider: HistoricalDataProvider[TConfig], config: TConfig, database_path: str | Path | None = None) -&gt; list[MarketDataEvent]\n</code></pre> <p>Fetches and stores data into the database.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>HistoricalDataProvider[TConfig]</code> <p>The historical data provider (i.e. Yfinance) implementation.</p> required <code>config</code> <code>TConfig</code> <p>The configuration that matches the data provider (i.e. YfinanceConfig).</p> required <code>database_path</code> <code>str | Path | None</code> <p>The optional database path; if not provided, defaults to the OS-standard user data directory.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[MarketDataEvent]</code> <p>list[MarketDataEvent]: The OHLCV data from the database and/or historical data provider.</p> Source code in <code>src\\contango\\runners\\static\\static_runner.py</code> <pre><code>@staticmethod\ndef fetch_data(\n    provider: HistoricalDataProvider[TConfig],\n    config: TConfig,\n    database_path: str | Path | None = None,\n) -&gt; list[MarketDataEvent]:\n    \"\"\"\n    Fetches and stores data into the database.\n\n    Args:\n        provider: The historical data provider (i.e. Yfinance) implementation.\n        config: The configuration that matches the data provider (i.e. YfinanceConfig).\n        database_path: The optional database path; if not provided, defaults to the OS-standard user data directory.\n\n    Returns:\n        list[MarketDataEvent]: The OHLCV data from the database and/or historical data provider.\n    \"\"\"\n    return DataRepository[config].get_data_and_store(\n        provider=provider,\n        config=config,\n        expected_timestamps=provider.get_expected_timestamps(config),\n        database_path=database_path,\n    )\n</code></pre>"},{"location":"reference/runners/static/static_runner/#contango.runners.static.static_runner.StaticRunner.fetch_data_multi","title":"fetch_data_multi  <code>staticmethod</code>","text":"<pre><code>fetch_data_multi(provider: HistoricalDataProvider[TConfig], configs: Mapping[str, TConfig], database_path: str | Path | None = None) -&gt; dict[str, list[MarketDataEvent]]\n</code></pre> <p>Fetches and stores data for multiple symbols, keyed by the same symbol strings intend to use in param_grid[\"symbol\"].</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>HistoricalDataProvider[TConfig]</code> <p>The historical data provider (i.e. Yfinance) implementation.</p> required <code>configs</code> <code>Mapping[str, TConfig]</code> <p>A mapping of symbol -&gt; config for the symbol (e.g. {\"AAPL\": YfinanceConfig(ticker=\"AAPL\", ...)}).</p> required <code>database_path</code> <code>str | Path | None</code> <p>The optional database path. if not provided, defaults to the OS-standard user data directory.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, list[MarketDataEvent]]</code> <p>dict[str, list[MarketDataEvent]]: OHLCV data per symbol.</p> Source code in <code>src\\contango\\runners\\static\\static_runner.py</code> <pre><code>@staticmethod\ndef fetch_data_multi(\n    provider: HistoricalDataProvider[TConfig],\n    configs: Mapping[str, TConfig],\n    database_path: str | Path | None = None,\n) -&gt; dict[str, list[MarketDataEvent]]:\n    \"\"\"\n    Fetches and stores data for multiple symbols, keyed by the same\n    symbol strings intend to use in param_grid[\"symbol\"].\n\n    Args:\n        provider: The historical data provider (i.e. Yfinance) implementation.\n        configs: A mapping of symbol -&gt; config for the symbol (e.g. {\"AAPL\": YfinanceConfig(ticker=\"AAPL\", ...)}).\n        database_path: The optional database path. if not provided, defaults to the OS-standard user data directory.\n\n    Returns:\n        dict[str, list[MarketDataEvent]]: OHLCV data per symbol.\n    \"\"\"\n    return {\n        symbol: StaticRunner[TConfig].fetch_data(provider=provider, config=config, database_path=database_path)\n        for symbol, config in configs.items()\n    }\n</code></pre>"},{"location":"reference/runners/static/static_runner/#contango.runners.static.static_runner.StaticRunner.run","title":"run  <code>staticmethod</code>","text":"<pre><code>run(ohlcv_data: list[MarketDataEvent], strategy: Strategy, backtester_config: BacktesterConfig) -&gt; Metrics\n</code></pre> <p>Runs a backtest for a static, singular strategy.</p> <p>Parameters:</p> Name Type Description Default <code>ohlcv_data</code> <code>list[MarketDataEvent]</code> <p>A list of historical market data events to use for the backtest.</p> required <code>strategy</code> <code>Strategy</code> <p>The singular strategy implementation to backtest.</p> required <code>backtester_config</code> <code>BacktesterConfig</code> <p>The configuration for the backtester.</p> required <p>Returns:</p> Name Type Description <code>Metrics</code> <code>Metrics</code> <p>The metrics for the backtest.</p> Source code in <code>src\\contango\\runners\\static\\static_runner.py</code> <pre><code>@staticmethod\ndef run(\n    ohlcv_data: list[MarketDataEvent], \n    strategy: Strategy, \n    backtester_config: BacktesterConfig\n) -&gt; Metrics:\n    \"\"\"\n    Runs a backtest for a static, singular strategy.\n\n    Args:\n        ohlcv_data: A list of historical market data events to use for the backtest.\n        strategy: The singular strategy implementation to backtest.\n        backtester_config: The configuration for the backtester.\n\n    Returns:\n        Metrics: The metrics for the backtest.\n    \"\"\"\n    raw_results = StrategyBacktester.backtest(\n        ohlcv_data=ohlcv_data, strategy=strategy, config=backtester_config\n    )\n    return calculate_metrics(raw_results)\n</code></pre>"},{"location":"reference/strategy/strategy/","title":"Strategy","text":""},{"location":"reference/strategy/strategy/#contangostrategystrategy","title":"<code>contango.strategy.strategy</code>","text":""},{"location":"reference/strategy/strategy/#contango.strategy.strategy","title":"contango.strategy.strategy","text":""},{"location":"reference/strategy/strategy/#contango.strategy.strategy.Strategy","title":"Strategy","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for implementing trading strategies.</p> <p>A Strategy defines user logic by responding to lifecycle and market events emitted by the engine. Subclasses should implement the required event handlers to initialize state, process market data, and react to order/trade events.</p> Lifecycle <ul> <li><code>on_start()</code>: called once before any data is processed.</li> <li><code>on_end()</code>: called once after all data has been processed.</li> </ul> Event hooks <ul> <li><code>on_market_event()</code>: triggered upon a market event (bar).</li> <li><code>on_order_event()</code>: triggered when an order is submitted.</li> <li><code>on_fill_event()</code>: triggered when an order is filled.</li> </ul> <p>Fields (injected):     - <code>order_api</code>: allows for communication with the engine to make orders.     - <code>portfolio_snapshot</code>: the current snapshot of the account that holds all positions &amp; cash.</p>"},{"location":"reference/strategy/strategy/#contango.strategy.strategy.Strategy.on_accepted_fill_event","title":"on_accepted_fill_event","text":"<pre><code>on_accepted_fill_event(trade: AcceptedFillEvent) -&gt; None\n</code></pre> <p>Called once when an order has been successfully filled.</p> Source code in <code>src\\contango\\strategy\\strategy.py</code> <pre><code>def on_accepted_fill_event(self, trade: AcceptedFillEvent) -&gt; None:\n    \"\"\"\n    Called once when an order has been successfully filled.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/strategy/#contango.strategy.strategy.Strategy.on_end","title":"on_end","text":"<pre><code>on_end() -&gt; None\n</code></pre> <p>Called exactly once at the end of strategy execution.</p> Source code in <code>src\\contango\\strategy\\strategy.py</code> <pre><code>def on_end(self) -&gt; None:\n    \"\"\"\n    Called exactly once at the end of strategy execution.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/strategy/#contango.strategy.strategy.Strategy.on_market_event","title":"on_market_event  <code>abstractmethod</code>","text":"<pre><code>on_market_event(event: MarketDataEvent) -&gt; None\n</code></pre> <p>Called once for market event (bar) in the given OHLCV data.</p> Source code in <code>src\\contango\\strategy\\strategy.py</code> <pre><code>@abstractmethod\ndef on_market_event(self, event: MarketDataEvent) -&gt; None:\n    \"\"\"\n    Called once for market event (bar) in the given OHLCV data.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/strategy/#contango.strategy.strategy.Strategy.on_order_event","title":"on_order_event","text":"<pre><code>on_order_event(order: OrderEvent) -&gt; None\n</code></pre> <p>Called once when an order has been submitted.</p> Source code in <code>src\\contango\\strategy\\strategy.py</code> <pre><code>def on_order_event(self, order: OrderEvent) -&gt; None:\n    \"\"\"\n    Called once when an order has been submitted.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/strategy/#contango.strategy.strategy.Strategy.on_rejected_fill_event","title":"on_rejected_fill_event","text":"<pre><code>on_rejected_fill_event(trade: RejectedFillEvent) -&gt; None\n</code></pre> <p>Called once when an order was rejected instead of filled.</p> Source code in <code>src\\contango\\strategy\\strategy.py</code> <pre><code>def on_rejected_fill_event(self, trade: RejectedFillEvent) -&gt; None:\n    \"\"\"\n    Called once when an order was rejected instead of filled.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/strategy/#contango.strategy.strategy.Strategy.on_start","title":"on_start","text":"<pre><code>on_start() -&gt; None\n</code></pre> <p>Called exactly once at the beginning of strategy execution.</p> Source code in <code>src\\contango\\strategy\\strategy.py</code> <pre><code>def on_start(self) -&gt; None:\n    \"\"\"\n    Called exactly once at the beginning of strategy execution.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/rule_based/cartesian_search/","title":"Cartesian Search","text":""},{"location":"reference/strategy/rule_based/cartesian_search/#contangostrategyrule_basedcartesian_search","title":"<code>contango.strategy.rule_based.cartesian_search</code>","text":""},{"location":"reference/strategy/rule_based/cartesian_search/#contango.strategy.rule_based.cartesian_search","title":"contango.strategy.rule_based.cartesian_search","text":""},{"location":"reference/strategy/rule_based/cartesian_search/#contango.strategy.rule_based.cartesian_search.cartesian_search","title":"cartesian_search","text":"<pre><code>cartesian_search(strategy_cls: Type[StrategyT], param_grid: Mapping[str, Sequence[Any]]) -&gt; Iterator[tuple[Callable[[], StrategyT], dict[str, Any]]]\n</code></pre> <p>Generates every parameter combination for a strategy class.</p> <p>Parameters:</p> Name Type Description Default <code>strategy_cls</code> <code>Type[StrategyT]</code> <p>The strategy to perform a Cartesian search on.</p> required <code>param_grid</code> <code>Mapping[str, Sequence[Any]]</code> <p>A mapping of strings to sequences of values to be          tested and parameterized into the strategy cls.</p> required <p>Yields:</p> Type Description <code>Callable[[], StrategyT]</code> <p>A tuple of <code>(factory, kwargs)</code>, where factory is a callable that</p> <code>dict[str, Any]</code> <p>constructs <code>strategy_cls(**kwargs)</code>.</p> Source code in <code>src\\contango\\strategy\\rule_based\\cartesian_search.py</code> <pre><code>def cartesian_search(\n    strategy_cls: Type[StrategyT],\n    param_grid: Mapping[str, Sequence[Any]],\n) -&gt; Iterator[tuple[Callable[[], StrategyT], dict[str, Any]]]:\n    \"\"\"\n    Generates every parameter combination for a strategy class.\n\n    Args:\n        strategy_cls: The strategy to perform a Cartesian search on.\n        param_grid: A mapping of strings to sequences of values to be \n                    tested and parameterized into the strategy cls.\n\n    Yields:\n        A tuple of `(factory, kwargs)`, where factory is a callable that\n        constructs `strategy_cls(**kwargs)`.\n    \"\"\"\n    keys = list(param_grid.keys())\n    for combo in product(*param_grid.values()):\n        kwargs = dict(zip(keys, combo))\n        yield partial(strategy_cls, **kwargs), kwargs\n</code></pre>"},{"location":"reference/strategy/rule_based/labeled/","title":"Labeled","text":""},{"location":"reference/strategy/rule_based/labeled/#contangostrategyrule_basedlabeled","title":"<code>contango.strategy.rule_based.labeled</code>","text":""},{"location":"reference/strategy/rule_based/labeled/#contango.strategy.rule_based.labeled","title":"contango.strategy.rule_based.labeled","text":""},{"location":"reference/strategy/rule_based/labeled/#contango.strategy.rule_based.labeled.Labeled","title":"Labeled","text":"<p>               Bases: <code>ABC</code></p> <p>Marks a class as an implementer of str.</p>"},{"location":"reference/strategy/rule_based/labeled/#contango.strategy.rule_based.labeled.Labeled.__str__","title":"__str__  <code>abstractmethod</code>","text":"<pre><code>__str__() -&gt; str\n</code></pre> <p>Returns a string representation of the class.</p> Source code in <code>src\\contango\\strategy\\rule_based\\labeled.py</code> <pre><code>@abstractmethod\ndef __str__(self) -&gt; str:\n    \"\"\"\n    Returns a string representation of the class.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/rule_based/rule_based_strategy/","title":"Rule Based Strategy","text":""},{"location":"reference/strategy/rule_based/rule_based_strategy/#contangostrategyrule_basedrule_based_strategy","title":"<code>contango.strategy.rule_based.rule_based_strategy</code>","text":""},{"location":"reference/strategy/rule_based/rule_based_strategy/#contango.strategy.rule_based.rule_based_strategy","title":"contango.strategy.rule_based.rule_based_strategy","text":""},{"location":"reference/strategy/rule_based/rule_based_strategy/#contango.strategy.rule_based.rule_based_strategy.RuleBasedStrategy","title":"RuleBasedStrategy","text":"<pre><code>RuleBasedStrategy(streams: Sequence[Stream[Any]], rules: Sequence[Rule[TradingContext, Intent | None]], position_sizer: PositionSizer[TradingContext])\n</code></pre> <p>               Bases: <code>Strategy</code></p> <p>Allows rules to be executed upon and intents to be executed.</p> <p>Initializes <code>RuleBasedStrategy</code>.</p> <p>Parameters:</p> Name Type Description Default <code>streams</code> <code>Sequence[Stream[Any]]</code> <p>A list of streams to be updated with context every market data event.</p> required <code>rules</code> <code>Sequence[Rule[TradingContext, Intent | None]]</code> <p>A sequence of rules, where each rule returns their intent type or None (executed in order).</p> required <code>position_sizer</code> <code>PositionSizer[TradingContext]</code> <p>The sizing rule for all trades made.</p> required Source code in <code>src\\contango\\strategy\\rule_based\\rule_based_strategy.py</code> <pre><code>def __init__(\n    self,\n    streams: Sequence[Stream[Any]],\n    rules: Sequence[Rule[TradingContext, Intent | None]],\n    position_sizer: PositionSizer[TradingContext],\n) -&gt; None:\n    \"\"\"\n    Initializes `RuleBasedStrategy`.\n\n    Args:\n        streams: A list of streams to be updated with context every market data event.\n        rules: A sequence of rules, where each rule returns their intent type or None (executed in order).\n        position_sizer: The sizing rule for all trades made.\n    \"\"\"\n    self._streams = streams\n    self._rules = rules\n    self._position_sizer = position_sizer\n    self._last_event: MarketDataEvent | None = None\n</code></pre>"},{"location":"reference/strategy/rule_based/rule_based_strategy/#contango.strategy.rule_based.rule_based_strategy.RuleBasedStrategy.on_end","title":"on_end","text":"<pre><code>on_end() -&gt; None\n</code></pre> <p>Closes any open long position at the final market event.</p> Source code in <code>src\\contango\\strategy\\rule_based\\rule_based_strategy.py</code> <pre><code>@final\ndef on_end(self) -&gt; None:\n    \"\"\"\n    Closes any open long position at the final market event.\n    \"\"\"\n    final_event = self._last_event\n\n    if final_event is None:\n        raise ValueError(\"The final market data event was never set.\")\n\n    size = self.portfolio_snapshot.position\n    if size &gt; 0:\n        self.order_api.submit_order(final_event, final_event.symbol, -size)\n</code></pre>"},{"location":"reference/strategy/rule_based/rule_based_strategy/#contango.strategy.rule_based.rule_based_strategy.RuleBasedStrategy.on_market_event","title":"on_market_event","text":"<pre><code>on_market_event(event: MarketDataEvent) -&gt; None\n</code></pre> <p>Updates all streams, executes rules, and executes intents every event.</p> Source code in <code>src\\contango\\strategy\\rule_based\\rule_based_strategy.py</code> <pre><code>@final\ndef on_market_event(self, event: MarketDataEvent) -&gt; None:\n    \"\"\"\n    Updates all streams, executes rules, and executes intents every event.\n    \"\"\"\n    self._last_event = event\n\n    for stream in self._streams:\n        stream.update(event)\n    self._position_sizer.update(event)\n\n    context = TradingContext(\n        event=event,\n        portfolio=self.portfolio_snapshot,\n    )\n\n    for rule in self._rules:\n        intent = rule.evaluate(context)\n        if intent is not None:\n            self._translate(intent, context)\n</code></pre>"},{"location":"reference/strategy/rule_based/actions/action/","title":"Action","text":""},{"location":"reference/strategy/rule_based/actions/action/#contangostrategyrule_basedactionsaction","title":"<code>contango.strategy.rule_based.actions.action</code>","text":""},{"location":"reference/strategy/rule_based/actions/action/#contango.strategy.rule_based.actions.action","title":"contango.strategy.rule_based.actions.action","text":""},{"location":"reference/strategy/rule_based/actions/action/#contango.strategy.rule_based.actions.action.Action","title":"Action","text":"<p>               Bases: <code>ABC</code>, <code>Generic[C, R]</code></p> <p>An object that executes a single action or behavior.</p> <p>Attributes:</p> Name Type Description <code>C</code> <p>The context type for the action to be passed into the execute() method.</p> <code>R</code> <p>The return type for the action when executed.</p>"},{"location":"reference/strategy/rule_based/actions/action/#contango.strategy.rule_based.actions.action.Action.execute","title":"execute  <code>abstractmethod</code>","text":"<pre><code>execute(context: C) -&gt; R\n</code></pre> <p>Executes the behavior of the action.</p> Source code in <code>src\\contango\\strategy\\rule_based\\actions\\action.py</code> <pre><code>@abstractmethod\ndef execute(self, context: C) -&gt; R:\n    \"\"\"\n    Executes the behavior of the action.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/rule_based/actions/emit/","title":"Emit","text":""},{"location":"reference/strategy/rule_based/actions/emit/#contangostrategyrule_basedactionsemit","title":"<code>contango.strategy.rule_based.actions.emit</code>","text":""},{"location":"reference/strategy/rule_based/actions/emit/#contango.strategy.rule_based.actions.emit","title":"contango.strategy.rule_based.actions.emit","text":""},{"location":"reference/strategy/rule_based/actions/emit/#contango.strategy.rule_based.actions.emit.Emit","title":"Emit","text":"<pre><code>Emit(intent: INTENT_T)\n</code></pre> <p>               Bases: <code>Action[TradingContext, INTENT_T]</code>, <code>Generic[INTENT_T]</code></p> <p>A simple action that emits an intent.</p> <p>Attributes:</p> Name Type Description <code>INTENT_T</code> <p>The intent type that the execute() method will always return.</p> <p>Initializes <code>Emit</code>.</p> <p>Parameters:</p> Name Type Description Default <code>intent</code> <code>INTENT_T</code> <p>The intent to release what the action is executed.</p> required Source code in <code>src\\contango\\strategy\\rule_based\\actions\\emit.py</code> <pre><code>def __init__(self, intent: INTENT_T) -&gt; None:\n    \"\"\"\n    Initializes `Emit`.\n\n    Args:\n        intent: The intent to release what the action is executed.\n    \"\"\"\n    self._intent = intent\n</code></pre>"},{"location":"reference/strategy/rule_based/actions/emit/#contango.strategy.rule_based.actions.emit.Emit.execute","title":"execute","text":"<pre><code>execute(context: TradingContext) -&gt; INTENT_T\n</code></pre> <p>Executes the <code>Emit</code> action.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>TradingContext</code> <p>The context (unused) for the current market data event.</p> required <p>Returns:</p> Name Type Description <code>INTENT_T</code> <code>INTENT_T</code> <p>The unchanging intent to emit.</p> Source code in <code>src\\contango\\strategy\\rule_based\\actions\\emit.py</code> <pre><code>def execute(self, context: TradingContext) -&gt; INTENT_T:\n    \"\"\"\n    Executes the `Emit` action.\n\n    Args:\n        context: The context (unused) for the current market data event.\n\n    Returns:\n        INTENT_T: The unchanging intent to emit.\n    \"\"\"\n    return self._intent\n</code></pre>"},{"location":"reference/strategy/rule_based/conditions/condition/","title":"Condition","text":""},{"location":"reference/strategy/rule_based/conditions/condition/#contangostrategyrule_basedconditionscondition","title":"<code>contango.strategy.rule_based.conditions.condition</code>","text":""},{"location":"reference/strategy/rule_based/conditions/condition/#contango.strategy.rule_based.conditions.condition","title":"contango.strategy.rule_based.conditions.condition","text":""},{"location":"reference/strategy/rule_based/conditions/condition/#contango.strategy.rule_based.conditions.condition.Condition","title":"Condition","text":"<p>               Bases: <code>ABC</code>, <code>Generic[C]</code></p> <p>A single condition when creating a strategy.</p>"},{"location":"reference/strategy/rule_based/conditions/condition/#contango.strategy.rule_based.conditions.condition.Condition.evaluate","title":"evaluate  <code>abstractmethod</code>","text":"<pre><code>evaluate(context: C) -&gt; bool\n</code></pre> <p>Returns the status of a condition given a context object.</p> Source code in <code>src\\contango\\strategy\\rule_based\\conditions\\condition.py</code> <pre><code>@abstractmethod\ndef evaluate(self, context: C) -&gt; bool:\n    \"\"\"\n    Returns the status of a condition given a context object.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/rule_based/conditions/predicate/","title":"Predicate","text":""},{"location":"reference/strategy/rule_based/conditions/predicate/#contangostrategyrule_basedconditionspredicate","title":"<code>contango.strategy.rule_based.conditions.predicate</code>","text":""},{"location":"reference/strategy/rule_based/conditions/predicate/#contango.strategy.rule_based.conditions.predicate","title":"contango.strategy.rule_based.conditions.predicate","text":""},{"location":"reference/strategy/rule_based/conditions/predicate/#contango.strategy.rule_based.conditions.predicate.Predicate","title":"Predicate","text":"<pre><code>Predicate(fn: Callable[[TradingContext], bool])\n</code></pre> <p>               Bases: <code>Condition[TradingContext]</code></p> <p>A lambda expression that evaluates to a boolean.</p> <p>Initializes <code>Predicate</code>.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>Callable[[TradingContext], bool]</code> <p>The callable that returns a boolean.</p> required Source code in <code>src\\contango\\strategy\\rule_based\\conditions\\predicate.py</code> <pre><code>def __init__(self, fn: Callable[[TradingContext], bool]) -&gt; None:\n    \"\"\"\n    Initializes `Predicate`.\n\n    Args:   \n        fn: The callable that returns a boolean.\n    \"\"\"\n    self._fn = fn\n</code></pre>"},{"location":"reference/strategy/rule_based/conditions/predicate/#contango.strategy.rule_based.conditions.predicate.Predicate.evaluate","title":"evaluate","text":"<pre><code>evaluate(context: TradingContext) -&gt; bool\n</code></pre> <p>Evaluates the state of the predicate.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>TradingContext</code> <p>The current context to feed into the lambda.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>The boolean value of the lambda.</p> Source code in <code>src\\contango\\strategy\\rule_based\\conditions\\predicate.py</code> <pre><code>def evaluate(self, context: TradingContext) -&gt; bool:\n    \"\"\"\n    Evaluates the state of the predicate.\n\n    Args:\n        context: The current context to feed into the lambda.\n\n    Returns:\n        bool: The boolean value of the lambda.\n    \"\"\"\n    return self._fn(context)\n</code></pre>"},{"location":"reference/strategy/rule_based/contexts/context/","title":"Context","text":""},{"location":"reference/strategy/rule_based/contexts/context/#contangostrategyrule_basedcontextscontext","title":"<code>contango.strategy.rule_based.contexts.context</code>","text":""},{"location":"reference/strategy/rule_based/contexts/context/#contango.strategy.rule_based.contexts.context","title":"contango.strategy.rule_based.contexts.context","text":""},{"location":"reference/strategy/rule_based/contexts/context/#contango.strategy.rule_based.contexts.context.Context","title":"Context  <code>dataclass</code>","text":"<pre><code>Context()\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>The necessary context that anything related to strategy creation may need.</p>"},{"location":"reference/strategy/rule_based/contexts/trading_context/","title":"Trading Context","text":""},{"location":"reference/strategy/rule_based/contexts/trading_context/#contangostrategyrule_basedcontextstrading_context","title":"<code>contango.strategy.rule_based.contexts.trading_context</code>","text":""},{"location":"reference/strategy/rule_based/contexts/trading_context/#contango.strategy.rule_based.contexts.trading_context","title":"contango.strategy.rule_based.contexts.trading_context","text":""},{"location":"reference/strategy/rule_based/contexts/trading_context/#contango.strategy.rule_based.contexts.trading_context.TradingContext","title":"TradingContext  <code>dataclass</code>","text":"<pre><code>TradingContext(event: MarketDataEvent, portfolio: PortfolioSnapshotEvent)\n</code></pre> <p>               Bases: <code>Context</code></p> <p>A specific context for trading strategies.</p> <p>Attributes:</p> Name Type Description <code>event</code> <code>MarketDataEvent</code> <p>The underlying market data event for the current context snapshot.</p> <code>portfolio</code> <code>PortfolioSnapshotEvent</code> <p>The portfolio event for the current context snapshot.</p>"},{"location":"reference/strategy/rule_based/intents/enter_long_intent/","title":"Enter Long Intent","text":""},{"location":"reference/strategy/rule_based/intents/enter_long_intent/#contangostrategyrule_basedintentsenter_long_intent","title":"<code>contango.strategy.rule_based.intents.enter_long_intent</code>","text":""},{"location":"reference/strategy/rule_based/intents/enter_long_intent/#contango.strategy.rule_based.intents.enter_long_intent","title":"contango.strategy.rule_based.intents.enter_long_intent","text":""},{"location":"reference/strategy/rule_based/intents/enter_long_intent/#contango.strategy.rule_based.intents.enter_long_intent.EnterLongIntent","title":"EnterLongIntent  <code>dataclass</code>","text":"<pre><code>EnterLongIntent(symbol: str)\n</code></pre> <p>               Bases: <code>Intent</code></p> <p>Enters a long trade with a provided symbol.</p>"},{"location":"reference/strategy/rule_based/intents/exit_long_intent/","title":"Exit Long Intent","text":""},{"location":"reference/strategy/rule_based/intents/exit_long_intent/#contangostrategyrule_basedintentsexit_long_intent","title":"<code>contango.strategy.rule_based.intents.exit_long_intent</code>","text":""},{"location":"reference/strategy/rule_based/intents/exit_long_intent/#contango.strategy.rule_based.intents.exit_long_intent","title":"contango.strategy.rule_based.intents.exit_long_intent","text":""},{"location":"reference/strategy/rule_based/intents/exit_long_intent/#contango.strategy.rule_based.intents.exit_long_intent.ExitLongIntent","title":"ExitLongIntent  <code>dataclass</code>","text":"<pre><code>ExitLongIntent(symbol: str)\n</code></pre> <p>               Bases: <code>Intent</code></p> <p>Exits a long trade with a provided symbol.</p>"},{"location":"reference/strategy/rule_based/intents/intent/","title":"Intent","text":""},{"location":"reference/strategy/rule_based/intents/intent/#contangostrategyrule_basedintentsintent","title":"<code>contango.strategy.rule_based.intents.intent</code>","text":""},{"location":"reference/strategy/rule_based/intents/intent/#contango.strategy.rule_based.intents.intent","title":"contango.strategy.rule_based.intents.intent","text":""},{"location":"reference/strategy/rule_based/intents/intent/#contango.strategy.rule_based.intents.intent.Intent","title":"Intent  <code>dataclass</code>","text":"<pre><code>Intent()\n</code></pre> <p>Any intent for a rule, whether it be an intent to create a trade, exit a trade, etc.</p>"},{"location":"reference/strategy/rule_based/position_sizers/allocation_position_sizer/","title":"Allocation Position Sizer","text":""},{"location":"reference/strategy/rule_based/position_sizers/allocation_position_sizer/#contangostrategyrule_basedposition_sizersallocation_position_sizer","title":"<code>contango.strategy.rule_based.position_sizers.allocation_position_sizer</code>","text":""},{"location":"reference/strategy/rule_based/position_sizers/allocation_position_sizer/#contango.strategy.rule_based.position_sizers.allocation_position_sizer","title":"contango.strategy.rule_based.position_sizers.allocation_position_sizer","text":""},{"location":"reference/strategy/rule_based/position_sizers/allocation_position_sizer/#contango.strategy.rule_based.position_sizers.allocation_position_sizer.AllocationPositionSizer","title":"AllocationPositionSizer","text":"<pre><code>AllocationPositionSizer(allocation: float)\n</code></pre> <p>               Bases: <code>PositionSizer[TradingContext]</code></p> <p>A position sizer with an allocation of the portfolio cash.</p> <p>Initializes `FixedPositionSizer.</p> <p>Parameters:</p> Name Type Description Default <code>allocation</code> <code>float</code> <p>The allocation of the portfolio cash to trade.</p> required Source code in <code>src\\contango\\strategy\\rule_based\\position_sizers\\allocation_position_sizer.py</code> <pre><code>def __init__(self, allocation: float) -&gt; None:\n    \"\"\"\n    Initializes `FixedPositionSizer.\n\n    Args:\n        allocation: The allocation of the portfolio cash to trade.\n    \"\"\"\n    self._allocation = allocation\n</code></pre>"},{"location":"reference/strategy/rule_based/position_sizers/allocation_position_sizer/#contango.strategy.rule_based.position_sizers.allocation_position_sizer.AllocationPositionSizer.size","title":"size","text":"<pre><code>size(context: TradingContext) -&gt; int\n</code></pre> <p>Returns the allocation percent of the portfolio cash (converted to units).</p> Source code in <code>src\\contango\\strategy\\rule_based\\position_sizers\\allocation_position_sizer.py</code> <pre><code>def size(self, context: TradingContext) -&gt; int:\n    \"\"\"\n    Returns the allocation percent of the portfolio cash (converted to units).\n    \"\"\"\n    cash = context.portfolio.cash * self._allocation\n\n    if context.event.close == 0.0:\n        return 0\n\n    units = int(cash / context.event.close)\n    return units\n</code></pre>"},{"location":"reference/strategy/rule_based/position_sizers/fixed_position_sizer/","title":"Fixed Position Sizer","text":""},{"location":"reference/strategy/rule_based/position_sizers/fixed_position_sizer/#contangostrategyrule_basedposition_sizersfixed_position_sizer","title":"<code>contango.strategy.rule_based.position_sizers.fixed_position_sizer</code>","text":""},{"location":"reference/strategy/rule_based/position_sizers/fixed_position_sizer/#contango.strategy.rule_based.position_sizers.fixed_position_sizer","title":"contango.strategy.rule_based.position_sizers.fixed_position_sizer","text":""},{"location":"reference/strategy/rule_based/position_sizers/fixed_position_sizer/#contango.strategy.rule_based.position_sizers.fixed_position_sizer.FixedPositionSizer","title":"FixedPositionSizer","text":"<pre><code>FixedPositionSizer(fixed_units: int)\n</code></pre> <p>               Bases: <code>PositionSizer[TradingContext]</code></p> <p>A position sizer with a fixed amount of units.</p> <p>Initializes `FixedPositionSizer.</p> <p>Parameters:</p> Name Type Description Default <code>fixed_units</code> <code>int</code> <p>The unchanging amount of units to order for every trade.</p> required Source code in <code>src\\contango\\strategy\\rule_based\\position_sizers\\fixed_position_sizer.py</code> <pre><code>def __init__(self, fixed_units: int) -&gt; None:\n    \"\"\"\n    Initializes `FixedPositionSizer.\n\n    Args:\n        fixed_units: The unchanging amount of units to order for every trade.\n    \"\"\"\n    self._fixed_units = fixed_units\n</code></pre>"},{"location":"reference/strategy/rule_based/position_sizers/fixed_position_sizer/#contango.strategy.rule_based.position_sizers.fixed_position_sizer.FixedPositionSizer.size","title":"size","text":"<pre><code>size(context: TradingContext) -&gt; int\n</code></pre> <p>Returns the fixed size set during initialization.</p> Source code in <code>src\\contango\\strategy\\rule_based\\position_sizers\\fixed_position_sizer.py</code> <pre><code>def size(self, context: TradingContext) -&gt; int:\n    \"\"\"\n    Returns the fixed size set during initialization.\n    \"\"\"\n    return self._fixed_units\n</code></pre>"},{"location":"reference/strategy/rule_based/position_sizers/position_sizer/","title":"Position Sizer","text":""},{"location":"reference/strategy/rule_based/position_sizers/position_sizer/#contangostrategyrule_basedposition_sizersposition_sizer","title":"<code>contango.strategy.rule_based.position_sizers.position_sizer</code>","text":""},{"location":"reference/strategy/rule_based/position_sizers/position_sizer/#contango.strategy.rule_based.position_sizers.position_sizer","title":"contango.strategy.rule_based.position_sizers.position_sizer","text":""},{"location":"reference/strategy/rule_based/position_sizers/position_sizer/#contango.strategy.rule_based.position_sizers.position_sizer.PositionSizer","title":"PositionSizer","text":"<p>               Bases: <code>Labeled</code>, <code>ABC</code>, <code>Generic[C]</code></p> <p>Decides how many units to trade when an intent is published.</p> <p>Attributes:</p> Name Type Description <code>C</code> <p>The context for the <code>PositionSizer</code>.</p>"},{"location":"reference/strategy/rule_based/position_sizers/position_sizer/#contango.strategy.rule_based.position_sizers.position_sizer.PositionSizer.size","title":"size  <code>abstractmethod</code>","text":"<pre><code>size(context: C) -&gt; int\n</code></pre> <p>Returns the size in units for the given context.</p> Source code in <code>src\\contango\\strategy\\rule_based\\position_sizers\\position_sizer.py</code> <pre><code>@abstractmethod\ndef size(self, context: C) -&gt; int:\n    \"\"\"\n    Returns the size in units for the given context.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/strategy/rule_based/position_sizers/position_sizer/#contango.strategy.rule_based.position_sizers.position_sizer.PositionSizer.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; None\n</code></pre> <p>Updates the state of the position sizer with the latest market data event.</p> Source code in <code>src\\contango\\strategy\\rule_based\\position_sizers\\position_sizer.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; None:\n    \"\"\"\n    Updates the state of the position sizer with the latest market data event.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"reference/strategy/rule_based/position_sizers/volatility_position_sizer/","title":"Volatility Position Sizer","text":""},{"location":"reference/strategy/rule_based/position_sizers/volatility_position_sizer/#contangostrategyrule_basedposition_sizersvolatility_position_sizer","title":"<code>contango.strategy.rule_based.position_sizers.volatility_position_sizer</code>","text":""},{"location":"reference/strategy/rule_based/position_sizers/volatility_position_sizer/#contango.strategy.rule_based.position_sizers.volatility_position_sizer","title":"contango.strategy.rule_based.position_sizers.volatility_position_sizer","text":""},{"location":"reference/strategy/rule_based/position_sizers/volatility_position_sizer/#contango.strategy.rule_based.position_sizers.volatility_position_sizer.VolatilityPositionSizer","title":"VolatilityPositionSizer","text":"<pre><code>VolatilityPositionSizer(vol_indicator: AverageTrueRange, target_vol: float, periods_per_year: int = 252)\n</code></pre> <p>               Bases: <code>PositionSizer[TradingContext]</code></p> <p>Sizes positions inversely to volatility.</p> <p>Initializes <code>VolatilityPositionSizer</code>.</p> <p>Parameters:</p> Name Type Description Default <code>vol_indicator</code> <code>AverageTrueRange</code> <p>An indicator producing the periodic return volatility.</p> required <code>target_vol</code> <code>float</code> <p>The desired annualized volatility contribution of a full position.</p> required <code>periods_per_year</code> <code>int</code> <p>The number of periods per year, used to annualize the indicator's per-period volatility.</p> <code>252</code> Source code in <code>src\\contango\\strategy\\rule_based\\position_sizers\\volatility_position_sizer.py</code> <pre><code>def __init__(\n    self,\n    vol_indicator: AverageTrueRange,\n    target_vol: float,\n    periods_per_year: int = 252,\n) -&gt; None:\n    \"\"\"\n    Initializes `VolatilityPositionSizer`.\n\n    Args:\n        vol_indicator: An indicator producing the periodic return volatility.\n        target_vol: The desired annualized volatility contribution of a full position.\n        periods_per_year: The number of periods per year, used to annualize the indicator's per-period volatility.\n    \"\"\"\n    self._vol_indicator = vol_indicator\n    self._target_vol = target_vol\n    self._periods_per_year = periods_per_year\n</code></pre>"},{"location":"reference/strategy/rule_based/position_sizers/volatility_position_sizer/#contango.strategy.rule_based.position_sizers.volatility_position_sizer.VolatilityPositionSizer.size","title":"size","text":"<pre><code>size(context: TradingContext) -&gt; int\n</code></pre> <p>Returns units sized so the position's annualized volatility = target_vol.</p> Source code in <code>src\\contango\\strategy\\rule_based\\position_sizers\\volatility_position_sizer.py</code> <pre><code>def size(self, context: TradingContext) -&gt; int:\n    \"\"\"\n    Returns units sized so the position's annualized volatility = target_vol.\n    \"\"\"\n    atr = self._vol_indicator.value\n    price = context.event.close\n\n    if atr is None or atr &lt;= 0 or price &lt;= 0:\n        return 0\n\n    periodic_vol = atr / price\n    annualized_vol = periodic_vol * math.sqrt(self._periods_per_year)\n    equity = context.portfolio.equity\n\n    if equity is None:\n        return 0\n\n    dollar_exposure = (self._target_vol / annualized_vol) * equity\n    return int(dollar_exposure / price)\n</code></pre>"},{"location":"reference/strategy/rule_based/rules/rule/","title":"Rule","text":""},{"location":"reference/strategy/rule_based/rules/rule/#contangostrategyrule_basedrulesrule","title":"<code>contango.strategy.rule_based.rules.rule</code>","text":""},{"location":"reference/strategy/rule_based/rules/rule/#contango.strategy.rule_based.rules.rule","title":"contango.strategy.rule_based.rules.rule","text":""},{"location":"reference/strategy/rule_based/rules/rule/#contango.strategy.rule_based.rules.rule.Rule","title":"Rule","text":"<pre><code>Rule(when: Condition[C], then: Action[C, R])\n</code></pre> <p>               Bases: <code>Generic[C, R]</code></p> <p>Executes an action upon a condition being True.</p> <p>Attributes:</p> Name Type Description <code>C</code> <p>The context for the rule.</p> <code>R</code> <p>The return type for the rule.</p> <p>Initializes a <code>Rule</code> with a condition &amp; action.</p> Source code in <code>src\\contango\\strategy\\rule_based\\rules\\rule.py</code> <pre><code>def __init__(self, when: Condition[C], then: Action[C, R]) -&gt; None:\n    \"\"\"\n    Initializes a `Rule` with a condition &amp; action.\n    \"\"\"\n    self._when = when\n    self._then = then\n</code></pre>"},{"location":"reference/strategy/rule_based/rules/rule/#contango.strategy.rule_based.rules.rule.Rule.evaluate","title":"evaluate","text":"<pre><code>evaluate(context: C) -&gt; R | None\n</code></pre> <p>Evaluates the status of the condition, executing &amp; returning the output from the action if True.</p> Source code in <code>src\\contango\\strategy\\rule_based\\rules\\rule.py</code> <pre><code>def evaluate(self, context: C) -&gt; R | None:\n    \"\"\"\n    Evaluates the status of the condition, executing &amp; returning the output from the action if True.\n    \"\"\"\n    if self._when.evaluate(context):\n        return self._then.execute(context)\n</code></pre>"},{"location":"reference/stream/historical_market_events/","title":"Historical Market Events","text":""},{"location":"reference/stream/historical_market_events/#contangostreamhistorical_market_events","title":"<code>contango.stream.historical_market_events</code>","text":""},{"location":"reference/stream/historical_market_events/#contango.stream.historical_market_events","title":"contango.stream.historical_market_events","text":""},{"location":"reference/stream/historical_market_events/#contango.stream.historical_market_events.HistoricalMarketEvents","title":"HistoricalMarketEvents","text":"<pre><code>HistoricalMarketEvents(window: int)\n</code></pre> <p>               Bases: <code>HistoricalStream[MarketDataEvent]</code></p> <p>A historical stream of market data events.</p> <p>Initializes <code>HistoricalMarketEvents</code>.</p> <p>Parameters:</p> Name Type Description Default <code>window</code> <code>int</code> <p>The maximum amount of lookback for the history of the stream.</p> required Source code in <code>src\\contango\\stream\\historical_market_events.py</code> <pre><code>def __init__(self, window: int) -&gt; None:\n    \"\"\"\n    Initializes `HistoricalMarketEvents`.\n\n    Args:\n        window: The maximum amount of lookback for the history of the stream.\n    \"\"\"\n    super().__init__(source=_MarketEventRelay(), window=window)\n</code></pre>"},{"location":"reference/stream/historical_stream/","title":"Historical Stream","text":""},{"location":"reference/stream/historical_stream/#contangostreamhistorical_stream","title":"<code>contango.stream.historical_stream</code>","text":""},{"location":"reference/stream/historical_stream/#contango.stream.historical_stream","title":"contango.stream.historical_stream","text":""},{"location":"reference/stream/historical_stream/#contango.stream.historical_stream.HistoricalStream","title":"HistoricalStream","text":"<pre><code>HistoricalStream(source: Stream[R], window: int)\n</code></pre> <p>               Bases: <code>Stream[R]</code>, <code>Generic[R]</code></p> <p>Wraps any stream and records a window of its past values.</p> <p>Attributes:</p> Name Type Description <code>R</code> <p>The return type for the historical stream.</p> <p>Initializes <code>HistoricalStream</code>.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>Stream[R]</code> <p>The stream source to wrap.</p> required <code>window</code> <code>int</code> <p>The maximum amount of lookback for the history of the stream.</p> required Source code in <code>src\\contango\\stream\\historical_stream.py</code> <pre><code>def __init__(self, source: Stream[R], window: int) -&gt; None:\n    \"\"\"\n    Initializes `HistoricalStream`.\n\n    Args:\n        source: The stream source to wrap.\n        window: The maximum amount of lookback for the history of the stream.\n    \"\"\"\n    self._source = source\n    self._history: Deque[R] = deque(maxlen=window)\n</code></pre>"},{"location":"reference/stream/historical_stream/#contango.stream.historical_stream.HistoricalStream.value","title":"value  <code>property</code>","text":"<pre><code>value: R\n</code></pre> <p>Returns the most recent value of the historical stream.</p>"},{"location":"reference/stream/historical_stream/#contango.stream.historical_stream.HistoricalStream.previous","title":"previous","text":"<pre><code>previous(n: int = 1) -&gt; R | None\n</code></pre> <p>Returns the previous n amount of values backwards. For example, n=1 will be the previous value relative to the current one.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The amount of lookback.</p> <code>1</code> <p>Returns:</p> Name Type Description <code>R</code> <code>R | None</code> <p>The stream value at n amount of lookback.</p> <code>None</code> <code>R | None</code> <p>If the history deque is shorter than the amount of lookback.</p> Source code in <code>src\\contango\\stream\\historical_stream.py</code> <pre><code>def previous(self, n: int = 1) -&gt; R | None:\n    \"\"\"\n    Returns the previous n amount of values backwards.\n    For example, n=1 will be the previous value relative to the current one.\n\n    Args:\n        n: The amount of lookback.\n\n    Returns:\n        R: The stream value at n amount of lookback.\n        None: If the history deque is shorter than the amount of lookback.\n    \"\"\"\n    idx = -1 - n\n    if len(self._history) &lt;= n:\n        return None\n    return self._history[idx]\n</code></pre>"},{"location":"reference/stream/historical_stream/#contango.stream.historical_stream.HistoricalStream.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; R\n</code></pre> <p>Updates the state of the historical stream given a market data event.</p> Source code in <code>src\\contango\\stream\\historical_stream.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; R:\n    \"\"\"\n    Updates the state of the historical stream given a market data event.\n    \"\"\"\n    self._source.update(event)\n    self._history.append(self._source.value)\n    return self.value\n</code></pre>"},{"location":"reference/stream/stream/","title":"Stream","text":""},{"location":"reference/stream/stream/#contangostreamstream","title":"<code>contango.stream.stream</code>","text":""},{"location":"reference/stream/stream/#contango.stream.stream","title":"contango.stream.stream","text":""},{"location":"reference/stream/stream/#contango.stream.stream.Stream","title":"Stream","text":"<p>               Bases: <code>ABC</code>, <code>Generic[R]</code></p> <p>Base class for anything that produces a value per market event.</p> <p>Attributes:</p> Name Type Description <code>R</code> <p>The return type for the stream.</p>"},{"location":"reference/stream/stream/#contango.stream.stream.Stream.value","title":"value  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>value: R\n</code></pre> <p>Returns the most recent value of the stream.</p>"},{"location":"reference/stream/stream/#contango.stream.stream.Stream.update","title":"update  <code>abstractmethod</code>","text":"<pre><code>update(event: MarketDataEvent) -&gt; R\n</code></pre> <p>Updates the state of the stream given a market data event.</p> <p>Returns:</p> Type Description <code>R</code> <p>The updated value.</p> Source code in <code>src\\contango\\stream\\stream.py</code> <pre><code>@abstractmethod\ndef update(self, event: MarketDataEvent) -&gt; R:\n    \"\"\"\n    Updates the state of the stream given a market data event.\n\n    Returns:    \n        The updated value.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/stream/indicators/historical_indicator/","title":"Historical Indicator","text":""},{"location":"reference/stream/indicators/historical_indicator/#contangostreamindicatorshistorical_indicator","title":"<code>contango.stream.indicators.historical_indicator</code>","text":""},{"location":"reference/stream/indicators/historical_indicator/#contango.stream.indicators.historical_indicator","title":"contango.stream.indicators.historical_indicator","text":""},{"location":"reference/stream/indicators/historical_indicator/#contango.stream.indicators.historical_indicator.HistoricalIndicator","title":"HistoricalIndicator","text":"<pre><code>HistoricalIndicator(source: Indicator[R], window: int)\n</code></pre> <p>               Bases: <code>HistoricalStream[R]</code>, <code>Generic[R]</code></p> <p>A historical stream that allows historical access of indicators.</p> <p>Attributes:</p> Name Type Description <code>R</code> <p>The return type for the historical indicator.</p> Source code in <code>src\\contango\\stream\\indicators\\historical_indicator.py</code> <pre><code>def __init__(self, source: Indicator[R], window: int) -&gt; None:\n    self._source = source\n    super().__init__(source=source, window=window)\n</code></pre>"},{"location":"reference/stream/indicators/indicator/","title":"Indicator","text":""},{"location":"reference/stream/indicators/indicator/#contangostreamindicatorsindicator","title":"<code>contango.stream.indicators.indicator</code>","text":""},{"location":"reference/stream/indicators/indicator/#contango.stream.indicators.indicator","title":"contango.stream.indicators.indicator","text":""},{"location":"reference/stream/indicators/indicator/#contango.stream.indicators.indicator.Indicator","title":"Indicator","text":"<p>               Bases: <code>Generic[R]</code>, <code>Stream[R]</code>, <code>ABC</code></p> <p>Base class for all per-bar indicators.</p> <p>Attributes:</p> Name Type Description <code>R</code> <p>The return type for the indicator.</p>"},{"location":"reference/stream/indicators/indicator/#contango.stream.indicators.indicator.Indicator.__repr__","title":"__repr__  <code>abstractmethod</code>","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Returns a string representation of the indicator.</p> Source code in <code>src\\contango\\stream\\indicators\\indicator.py</code> <pre><code>@abstractmethod\ndef __repr__(self) -&gt; str:\n    \"\"\"\n    Returns a string representation of the indicator.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/stream/indicators/calculations/average_true_range/","title":"Average True Range","text":""},{"location":"reference/stream/indicators/calculations/average_true_range/#contangostreamindicatorscalculationsaverage_true_range","title":"<code>contango.stream.indicators.calculations.average_true_range</code>","text":""},{"location":"reference/stream/indicators/calculations/average_true_range/#contango.stream.indicators.calculations.average_true_range","title":"contango.stream.indicators.calculations.average_true_range","text":""},{"location":"reference/stream/indicators/calculations/average_true_range/#contango.stream.indicators.calculations.average_true_range.AverageTrueRange","title":"AverageTrueRange","text":"<pre><code>AverageTrueRange(pd: days = 14)\n</code></pre> <p>               Bases: <code>Indicator[float | None]</code></p> <p>The average true range (volatility measure).</p> <p>Initializes <code>AverageTrueRange</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pd</code> <code>days</code> <p>Lookback period used to smooth the True Range.</p> <code>14</code> Source code in <code>src\\contango\\stream\\indicators\\calculations\\average_true_range.py</code> <pre><code>def __init__(self, pd: days = 14) -&gt; None:\n    \"\"\"\n    Initializes `AverageTrueRange`.\n\n    Args:\n        pd: Lookback period used to smooth the True Range.\n    \"\"\"\n    self._true_range = TrueRange()\n    self._average = WilderHelper(pd)\n    self._period = pd\n    self._value: float | None = None\n</code></pre>"},{"location":"reference/stream/indicators/calculations/average_true_range/#contango.stream.indicators.calculations.average_true_range.AverageTrueRange.value","title":"value  <code>property</code>","text":"<pre><code>value: float | None\n</code></pre> <p>Returns the latest value for the ATR.</p>"},{"location":"reference/stream/indicators/calculations/average_true_range/#contango.stream.indicators.calculations.average_true_range.AverageTrueRange.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; float | None\n</code></pre> <p>Updates the ATR for each bar.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>MarketDataEvent</code> <p>The underlying event to update the ATR with.</p> required <p>Returns:</p> Type Description <code>float | None</code> <p>The current ATR. Returns None until enough bars have been</p> <code>float | None</code> <p>observed to initialize the Wilder average.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\average_true_range.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; float | None:\n    \"\"\"\n    Updates the ATR for each bar.\n\n    Args:\n        event: The underlying event to update the ATR with.\n\n    Returns:\n        The current ATR. Returns None until enough bars have been\n        observed to initialize the Wilder average.\n    \"\"\"\n    tr = self._true_range.update(event)\n    if tr is None:\n        return None\n\n    self._value = self._average.update(tr)\n    return self._value\n</code></pre>"},{"location":"reference/stream/indicators/calculations/bollinger_bands/","title":"Bollinger Bands","text":""},{"location":"reference/stream/indicators/calculations/bollinger_bands/#contangostreamindicatorscalculationsbollinger_bands","title":"<code>contango.stream.indicators.calculations.bollinger_bands</code>","text":""},{"location":"reference/stream/indicators/calculations/bollinger_bands/#contango.stream.indicators.calculations.bollinger_bands","title":"contango.stream.indicators.calculations.bollinger_bands","text":""},{"location":"reference/stream/indicators/calculations/bollinger_bands/#contango.stream.indicators.calculations.bollinger_bands.BollingerBandSnapshot","title":"BollingerBandSnapshot","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single snapshot of Bollinger Bands.</p> <p>Attributes:</p> Name Type Description <code>upper</code> <code>float</code> <p>The upper band for the current price.</p> <code>middle</code> <code>float</code> <p>The middle band (pure SMA) for the current price.</p> <code>lower</code> <code>float</code> <p>The lower band for the current price.</p> <code>stdev</code> <code>float</code> <p>The population standard deviation over the lookback window.</p>"},{"location":"reference/stream/indicators/calculations/bollinger_bands/#contango.stream.indicators.calculations.bollinger_bands.BollingerBands","title":"BollingerBands","text":"<pre><code>BollingerBands(pd: days = 20, k: float = 2.0)\n</code></pre> <p>               Bases: <code>Indicator[BollingerBandSnapshot | None]</code></p> <p>Rolling Bollinger Bands with variance updates.</p> <p>Wraps an SMA and tracks a running sum-of-squares so that standard deviation is recomputed in constant time on every tick. Returns None until the lookback window is full.</p> <p>Parameters:</p> Name Type Description Default <code>pd</code> <code>days</code> <p>Lookback window in bars (default 20).</p> <code>20</code> <code>k</code> <code>float</code> <p>Band-width multiplier in standard deviations (default 2.0).</p> <code>2.0</code> Source code in <code>src\\contango\\stream\\indicators\\calculations\\bollinger_bands.py</code> <pre><code>def __init__(self, pd: days = 20, k: float = 2.0) -&gt; None:\n    \"\"\"\n    Args:\n        pd: Lookback window in bars (default 20).\n        k: Band-width multiplier in standard deviations (default 2.0).\n    \"\"\"\n    self._period = pd\n    self._k = k\n\n    self._sum: float = 0.0\n    self._sum_sq: float = 0.0\n    self._count: int = 0\n\n    self._window: list[float] = [0.0] * pd\n    self._head: int = 0\n\n    self._value: BollingerBandSnapshot | None = None\n</code></pre>"},{"location":"reference/stream/indicators/calculations/bollinger_bands/#contango.stream.indicators.calculations.bollinger_bands.BollingerBands.value","title":"value  <code>property</code>","text":"<pre><code>value: BollingerBandSnapshot | None\n</code></pre> <p>Returns the latest value for the bollinger bands.</p>"},{"location":"reference/stream/indicators/calculations/bollinger_bands/#contango.stream.indicators.calculations.bollinger_bands.BollingerBands.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; BollingerBandSnapshot | None\n</code></pre> <p>Takes in a new price and return the current bands, or None while the window is still filling.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>MarketDataEvent</code> <p>The underlying event to update the bollinger bands indicator with.</p> required <p>Returns:</p> Type Description <code>BollingerBandSnapshot | None</code> <p>A BollingerBandSnapshot(upper, middle, lower), or None until</p> <code>BollingerBandSnapshot | None</code> <p><code>period</code> prices have been seen.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\bollinger_bands.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; BollingerBandSnapshot | None:\n    \"\"\"\n    Takes in a new price and return the current bands, or None while the\n    window is still filling.\n\n    Args:\n        event: The underlying event to update the bollinger bands indicator with.\n\n    Returns:\n        A BollingerBandSnapshot(upper, middle, lower), or None until\n        `period` prices have been seen.\n    \"\"\"\n    if self._count == self._period:\n        oldest = self._window[self._head]\n        self._sum -= oldest\n        self._sum_sq -= oldest * oldest\n\n    self._window[self._head] = event.close\n    self._head = (self._head + 1) % self._period\n    self._count = min(self._count + 1, self._period)\n\n    self._sum += event.close\n    self._sum_sq += event.close * event.close\n\n    if self._count &lt; self._period:\n        return None\n\n    n = self._period\n    middle = self._sum / n\n    variance = (self._sum_sq - self._sum * self._sum / n) / n\n    stdev = variance ** 0.5\n    band = self._k * stdev\n\n    self._value = BollingerBandSnapshot(\n        upper=middle + band,\n        middle=middle,\n        lower=middle - band,\n        stdev=stdev,\n    )\n    return self._value\n</code></pre>"},{"location":"reference/stream/indicators/calculations/ema/","title":"EMA","text":""},{"location":"reference/stream/indicators/calculations/ema/#contangostreamindicatorscalculationsema","title":"<code>contango.stream.indicators.calculations.ema</code>","text":""},{"location":"reference/stream/indicators/calculations/ema/#contango.stream.indicators.calculations.ema","title":"contango.stream.indicators.calculations.ema","text":""},{"location":"reference/stream/indicators/calculations/ema/#contango.stream.indicators.calculations.ema.EMA","title":"EMA","text":"<pre><code>EMA(pd: days)\n</code></pre> <p>               Bases: <code>Indicator[float | None]</code></p> <p>Exponential Moving Average.</p> <p>Uses alpha = 2 / (period + 1) and seeds the first value directly from the first price to avoid bias (towards 0).</p> <p>Initializes <code>Ema</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pd</code> <code>days</code> <p>Lookback period to derive the smoothing factor. Larger values -&gt; slower to react to price changes.</p> required Source code in <code>src\\contango\\stream\\indicators\\calculations\\ema.py</code> <pre><code>def __init__(self, pd: days) -&gt; None:\n    \"\"\"\n    Initializes `Ema`.\n\n    Args:\n        pd: Lookback period to derive the smoothing factor. Larger values -&gt; slower to react to price changes.\n    \"\"\"\n    self._alpha = 2 / (pd + 1)\n    self._value: float | None = None\n    self._period = pd\n</code></pre>"},{"location":"reference/stream/indicators/calculations/ema/#contango.stream.indicators.calculations.ema.EMA.value","title":"value  <code>property</code>","text":"<pre><code>value: float | None\n</code></pre> <p>Returns the latest value for the EMA.</p>"},{"location":"reference/stream/indicators/calculations/ema/#contango.stream.indicators.calculations.ema.EMA.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; float\n</code></pre> <p>Takes in a new price and returns the current EMA.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>MarketDataEvent</code> <p>The current market data event.</p> required <p>Returns:</p> Type Description <code>float</code> <p>The current EMA. The first call returns <code>price</code> unsmoothed.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\ema.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; float:\n    \"\"\"\n    Takes in a new price and returns the current EMA.\n\n    Args:\n        event: The current market data event.\n\n    Returns:\n        The current EMA. The first call returns `price` unsmoothed.\n    \"\"\"\n    if self._value is None:\n        self._value = event.close\n    else:\n        self._value = event.close * self._alpha + self._value * (1 - self._alpha)\n    return self._value\n</code></pre>"},{"location":"reference/stream/indicators/calculations/rsi/","title":"RSI","text":""},{"location":"reference/stream/indicators/calculations/rsi/#contangostreamindicatorscalculationsrsi","title":"<code>contango.stream.indicators.calculations.rsi</code>","text":""},{"location":"reference/stream/indicators/calculations/rsi/#contango.stream.indicators.calculations.rsi","title":"contango.stream.indicators.calculations.rsi","text":""},{"location":"reference/stream/indicators/calculations/rsi/#contango.stream.indicators.calculations.rsi.RSI","title":"RSI","text":"<pre><code>RSI(pd: days = 14)\n</code></pre> <p>               Bases: <code>Indicator[float | None]</code></p> <p>Relative Strength Index (RSI).</p> <p>Uses Wilder's Moving Average to smooth average gains and losses over the given period. Returns None until both averages are seeded.</p> <p>Initializes <code>RSI</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pd</code> <code>days</code> <p>Lookback period for the Wilder averages.</p> <code>14</code> Source code in <code>src\\contango\\stream\\indicators\\calculations\\rsi.py</code> <pre><code>def __init__(self, pd: days = 14) -&gt; None:\n    \"\"\"\n    Initializes `RSI`.\n\n    Args:\n        pd: Lookback period for the Wilder averages.\n    \"\"\"\n    self._period = pd\n    self._avg_gain = WilderHelper(pd)\n    self._avg_loss = WilderHelper(pd)\n    self._prev_price: float | None = None\n    self._value: float | None = None\n</code></pre>"},{"location":"reference/stream/indicators/calculations/rsi/#contango.stream.indicators.calculations.rsi.RSI.value","title":"value  <code>property</code>","text":"<pre><code>value: float | None\n</code></pre> <p>Returns the latest value for the RSI.</p>"},{"location":"reference/stream/indicators/calculations/rsi/#contango.stream.indicators.calculations.rsi.RSI.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; float | None\n</code></pre> <p>Takes in a new price and returns the current RSI.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>MarketDataEvent</code> <p>The underlying event to update the RSI with.</p> required <p>Returns:</p> Type Description <code>float | None</code> <p>RSI in the range [0, 100], or None if the period has not been reached.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\rsi.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; float | None:\n    \"\"\"\n    Takes in a new price and returns the current RSI.\n\n    Args:\n        event: The underlying event to update the RSI with.\n\n    Returns:\n        RSI in the range [0, 100], or None if the period has not been reached.\n    \"\"\"\n    if self._prev_price is None:\n        self._prev_price = event.close\n        return None\n\n    delta = event.close - self._prev_price\n    self._prev_price = event.close\n\n    gain = max(delta, 0.0)\n    loss = max(-delta, 0.0)\n\n    avg_gain = self._avg_gain.update(gain)\n    avg_loss = self._avg_loss.update(loss)\n\n    if avg_gain is None or avg_loss is None:\n        return None\n\n    if avg_loss == 0.0:\n        return 100.0\n\n    rs = avg_gain / avg_loss\n\n    self._value = 100 - (100 / (1 + rs))\n\n    return self._value\n</code></pre>"},{"location":"reference/stream/indicators/calculations/sma/","title":"SMA","text":""},{"location":"reference/stream/indicators/calculations/sma/#contangostreamindicatorscalculationssma","title":"<code>contango.stream.indicators.calculations.sma</code>","text":""},{"location":"reference/stream/indicators/calculations/sma/#contango.stream.indicators.calculations.sma","title":"contango.stream.indicators.calculations.sma","text":""},{"location":"reference/stream/indicators/calculations/sma/#contango.stream.indicators.calculations.sma.SMA","title":"SMA","text":"<pre><code>SMA(pd: days)\n</code></pre> <p>               Bases: <code>Indicator[float | None]</code></p> <p>Simple Moving Average.</p> <p>Maintains a window of prices and tracks their sum incrementally. Returns None until the window is full.</p> <p>Initializes <code>SMA</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pd</code> <code>days</code> <p>The number of days (period) for the rolling window.</p> required Source code in <code>src\\contango\\stream\\indicators\\calculations\\sma.py</code> <pre><code>def __init__(self, pd: days) -&gt; None:\n    \"\"\"\n    Initializes `SMA`.\n\n    Args:\n        pd: The number of days (period) for the rolling window.\n    \"\"\"\n    self._period = pd\n    self._window: deque[float] = deque(maxlen=pd)\n    self._sum = 0.0\n    self._value: float | None = None\n</code></pre>"},{"location":"reference/stream/indicators/calculations/sma/#contango.stream.indicators.calculations.sma.SMA.value","title":"value  <code>property</code>","text":"<pre><code>value: float | None\n</code></pre> <p>Returns the latest value for the SMA.</p>"},{"location":"reference/stream/indicators/calculations/sma/#contango.stream.indicators.calculations.sma.SMA.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; float | None\n</code></pre> <p>Takes in a new price and returns the current SMA, or None if the window is not yet full.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>MarketDataEvent</code> <p>The underlying event to update teh SMA with.</p> required <p>Returns:</p> Type Description <code>float | None</code> <p>The SMA over the current window, or <code>None</code> if <code>period</code> amount of days have not passed.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\sma.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; float | None:\n    \"\"\"\n    Takes in a new price and returns the current SMA, or None if the window is not yet full.\n\n    Args:\n        event: The underlying event to update teh SMA with.\n\n    Returns:\n        The SMA over the current window, or `None` if `period` amount of days have not passed.\n    \"\"\"\n    if len(self._window) == self._period:\n        self._sum -= self._window[0]\n\n    self._window.append(event.close)\n    self._sum += event.close\n\n    if len(self._window) &lt; self._period:\n        return None\n\n    self._value = self._sum / self._period\n\n    return self._value\n</code></pre>"},{"location":"reference/stream/indicators/calculations/true_range/","title":"True Range","text":""},{"location":"reference/stream/indicators/calculations/true_range/#contangostreamindicatorscalculationstrue_range","title":"<code>contango.stream.indicators.calculations.true_range</code>","text":""},{"location":"reference/stream/indicators/calculations/true_range/#contango.stream.indicators.calculations.true_range","title":"contango.stream.indicators.calculations.true_range","text":""},{"location":"reference/stream/indicators/calculations/true_range/#contango.stream.indicators.calculations.true_range.TrueRange","title":"TrueRange","text":"<pre><code>TrueRange()\n</code></pre> <p>               Bases: <code>Indicator[float | None]</code></p> <p>The volatility during a single trading period.</p> <p>Initializes <code>TrueRange</code>.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\true_range.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"\n    Initializes `TrueRange`.\n    \"\"\"\n    self._previous_close: USD | None = None\n    self._value: float | None = None\n</code></pre>"},{"location":"reference/stream/indicators/calculations/true_range/#contango.stream.indicators.calculations.true_range.TrueRange.value","title":"value  <code>property</code>","text":"<pre><code>value: float | None\n</code></pre> <p>Returns the latest value for the true range.</p>"},{"location":"reference/stream/indicators/calculations/true_range/#contango.stream.indicators.calculations.true_range.TrueRange.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; float | None\n</code></pre> <p>Updates the state of the true range for every bar.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>MarketDataEvent</code> <p>The underlying event to update the true range with.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float | None</code> <p>The true range for the bar.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\true_range.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; float | None:\n    \"\"\"\n    Updates the state of the true range for every bar.\n\n    Args:\n        event: The underlying event to update the true range with.\n\n    Returns:\n        float: The true range for the bar.\n    \"\"\"\n    if self._previous_close is None:\n        tr = event.high - event.low\n    else:\n        tr = max(\n            event.high - event.low,\n            abs(event.high - self._previous_close),\n            abs(event.low - self._previous_close),\n        )\n\n    self._previous_close = event.close\n    self._value = tr\n\n    return tr\n</code></pre>"},{"location":"reference/stream/indicators/calculations/vwap/","title":"VWAP","text":""},{"location":"reference/stream/indicators/calculations/vwap/#contangostreamindicatorscalculationsvwap","title":"<code>contango.stream.indicators.calculations.vwap</code>","text":""},{"location":"reference/stream/indicators/calculations/vwap/#contango.stream.indicators.calculations.vwap","title":"contango.stream.indicators.calculations.vwap","text":""},{"location":"reference/stream/indicators/calculations/vwap/#contango.stream.indicators.calculations.vwap.VWAP","title":"VWAP","text":"<pre><code>VWAP(k: float)\n</code></pre> <p>               Bases: <code>Indicator[VWAPSnapshot | None]</code></p> <p>VWAP with standard deviation bands.</p> <p>Returns None on the first bar of each session (no deviation yet).</p> <p>Parameters:</p> Name Type Description Default <code>k</code> <code>float</code> <p>Band-width multiplier in standard deviations.</p> required Source code in <code>src\\contango\\stream\\indicators\\calculations\\vwap.py</code> <pre><code>def __init__(self, k: float) -&gt; None:\n    \"\"\"\n    Args:\n        k: Band-width multiplier in standard deviations.\n    \"\"\"\n    self._k = k\n\n    # Intraday accumulators \u2014 reset each session\n    self._cumulative_tp_volume: float = 0.0\n    self._cumulative_volume: float = 0.0\n    self._cumulative_tp_sq_volume: float = 0.0\n    self._bar_count: int = 0\n\n    self._value: VWAPSnapshot | None = None\n</code></pre>"},{"location":"reference/stream/indicators/calculations/vwap/#contango.stream.indicators.calculations.vwap.VWAP.value","title":"value  <code>property</code>","text":"<pre><code>value: VWAPSnapshot | None\n</code></pre> <p>Returns the latest value for the VWAP.</p>"},{"location":"reference/stream/indicators/calculations/vwap/#contango.stream.indicators.calculations.vwap.VWAP.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; VWAPSnapshot | None\n</code></pre> <p>Takes in a new bar and returns the current VWAP snapshot. Returns None on the first bar of the session (standard deviation undefined).</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>MarketDataEvent</code> <p>The underlying event to update the VWAP with.</p> required <p>Returns:</p> Type Description <code>VWAPSnapshot | None</code> <p>VWAPSnapshot | None: If the VWAP is settled, returns a snapshot containing                  the VWAP alongside the upper and lower VWAP bounds.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\vwap.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; VWAPSnapshot | None:\n    \"\"\"\n    Takes in a new bar and returns the current VWAP snapshot.\n    Returns None on the first bar of the session (standard deviation undefined).\n\n    Args:\n        event: The underlying event to update the VWAP with.\n\n    Returns:\n        VWAPSnapshot | None: If the VWAP is settled, returns a snapshot containing\n                             the VWAP alongside the upper and lower VWAP bounds.\n    \"\"\"\n    typical_price = (event.high + event.low + event.close) / 3\n\n    self._cumulative_tp_volume += typical_price * event.volume\n    self._cumulative_volume += event.volume\n    self._cumulative_tp_sq_volume += (typical_price ** 2) * event.volume\n    self._bar_count += 1\n\n    if self._cumulative_volume == 0:\n        return None\n\n    vwap = self._cumulative_tp_volume / self._cumulative_volume\n\n    # Volume-weighted variance\n    variance = (\n        self._cumulative_tp_sq_volume / self._cumulative_volume\n    ) - vwap ** 2\n\n    # Variance can be slightly negative due to floating point\n    std = max(variance, 0.0) ** 0.5\n\n    if self._bar_count &lt; 2:\n        return None\n\n    band = self._k * std\n\n    self._value = VWAPSnapshot(\n        vwap=vwap,\n        upper=vwap + band,\n        lower=vwap - band,\n    )\n\n    return self._value\n</code></pre>"},{"location":"reference/stream/indicators/calculations/vwap/#contango.stream.indicators.calculations.vwap.VWAPSnapshot","title":"VWAPSnapshot","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single snapshot of VWAP with bands.</p> <p>Attributes:</p> Name Type Description <code>vwap</code> <code>float</code> <p>The current volume-weighted average price.</p> <code>upper</code> <code>float</code> <p>VWAP + k standard deviations.</p> <code>lower</code> <code>float</code> <p>VWAP - k standard deviations.</p>"},{"location":"reference/stream/indicators/calculations/wilder_average/","title":"Wilder Average","text":""},{"location":"reference/stream/indicators/calculations/wilder_average/#contangostreamindicatorscalculationswilder_average","title":"<code>contango.stream.indicators.calculations.wilder_average</code>","text":""},{"location":"reference/stream/indicators/calculations/wilder_average/#contango.stream.indicators.calculations.wilder_average","title":"contango.stream.indicators.calculations.wilder_average","text":""},{"location":"reference/stream/indicators/calculations/wilder_average/#contango.stream.indicators.calculations.wilder_average.WilderAverage","title":"WilderAverage","text":"<pre><code>WilderAverage(pd: days)\n</code></pre> <p>               Bases: <code>Indicator[float | None]</code></p> <p>Wilder's Moving Average (RMA).</p> <p>Initializes <code>WilderAverage</code>.</p> <p>Parameters:</p> Name Type Description Default <code>pd</code> <code>days</code> <p>Lookback period to derive the smoothing factor. Larger values -&gt; slower to react to price changes.</p> required Source code in <code>src\\contango\\stream\\indicators\\calculations\\wilder_average.py</code> <pre><code>def __init__(self, pd: days) -&gt; None:\n    \"\"\"\n    Initializes `WilderAverage`.\n\n    Args:\n        pd: Lookback period to derive the smoothing factor. Larger values -&gt; slower to react to price changes.\n    \"\"\"\n    self._period = pd\n    self._window: deque[float] = deque(maxlen=pd)\n\n    self._value: float | None = None\n</code></pre>"},{"location":"reference/stream/indicators/calculations/wilder_average/#contango.stream.indicators.calculations.wilder_average.WilderAverage.value","title":"value  <code>property</code>","text":"<pre><code>value: float | None\n</code></pre> <p>Returns the latest value for the wilder average.</p>"},{"location":"reference/stream/indicators/calculations/wilder_average/#contango.stream.indicators.calculations.wilder_average.WilderAverage.update","title":"update","text":"<pre><code>update(event: MarketDataEvent) -&gt; float | None\n</code></pre> <p>Takes in a new value and return smoothed result.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>MarketDataEvent</code> <p>The underlying event to update the wilder average with.</p> required <p>Returns:</p> Type Description <code>float | None</code> <p>The current WilderAverage. Returns None if period amount of days have not been reached.</p> Source code in <code>src\\contango\\stream\\indicators\\calculations\\wilder_average.py</code> <pre><code>def update(self, event: MarketDataEvent) -&gt; float | None:\n    \"\"\"\n    Takes in a new value and return smoothed result.\n\n    Args:\n        event: The underlying event to update the wilder average with.\n\n    Returns:\n        The current WilderAverage. Returns None if period amount of days have not been reached.\n    \"\"\"\n    self._window.append(event.close)\n\n    if len(self._window) &lt; self._period:\n        return None\n\n    # First full initialization: simple average\n    if self._value is None:\n        self._value = sum(self._window) / self._period\n        return self._value\n\n    self._value = (\n        (self._value * (self._period - 1)) + event.close\n    ) / self._period\n\n    return self._value\n</code></pre>"},{"location":"reference/stream/indicators/helpers/wilder_helper/","title":"Wilder Helper","text":""},{"location":"reference/stream/indicators/helpers/wilder_helper/#contangostreamindicatorshelperswilder_helper","title":"<code>contango.stream.indicators.helpers.wilder_helper</code>","text":""},{"location":"reference/stream/indicators/helpers/wilder_helper/#contango.stream.indicators.helpers.wilder_helper","title":"contango.stream.indicators.helpers.wilder_helper","text":""},{"location":"reference/stream/indicators/helpers/wilder_helper/#contango.stream.indicators.helpers.wilder_helper.WilderHelper","title":"WilderHelper","text":"<pre><code>WilderHelper(period: days)\n</code></pre> <p>Wilder's Moving Average (RMA).</p> <p>Initializes <code>WilderAverage</code>.</p> <p>Parameters:</p> Name Type Description Default <code>period</code> <code>days</code> <p>Lookback period to derive the smoothing factor. Larger values -&gt; slower to react to price changes.</p> required Source code in <code>src\\contango\\stream\\indicators\\helpers\\wilder_helper.py</code> <pre><code>def __init__(self, period: days) -&gt; None:\n    \"\"\"\n    Initializes `WilderAverage`.\n\n    Args:\n        period: Lookback period to derive the smoothing factor. Larger values -&gt; slower to react to price changes.\n    \"\"\"\n    self._period = period\n    self._window: deque[float] = deque(maxlen=period)\n\n    self._value: float | None = None\n</code></pre>"},{"location":"reference/stream/indicators/helpers/wilder_helper/#contango.stream.indicators.helpers.wilder_helper.WilderHelper.value","title":"value  <code>property</code>","text":"<pre><code>value: float | None\n</code></pre> <p>Returns the latest value for the wilder average.</p>"},{"location":"reference/stream/indicators/helpers/wilder_helper/#contango.stream.indicators.helpers.wilder_helper.WilderHelper.update","title":"update","text":"<pre><code>update(price: USD) -&gt; float | None\n</code></pre> <p>Takes in a new value and return smoothed result.</p> <p>Parameters:</p> Name Type Description Default <code>price</code> <code>USD</code> <p>The price for the current bar.</p> required <p>Returns:</p> Type Description <code>float | None</code> <p>The current WilderAverage. Returns None if period amount of days have not been reached.</p> Source code in <code>src\\contango\\stream\\indicators\\helpers\\wilder_helper.py</code> <pre><code>def update(self, price: USD) -&gt; float | None:\n    \"\"\"\n    Takes in a new value and return smoothed result.\n\n    Args:\n        price: The price for the current bar.\n\n    Returns:\n        The current WilderAverage. Returns None if period amount of days have not been reached.\n    \"\"\"\n    self._window.append(price)\n\n    if len(self._window) &lt; self._period:\n        return None\n\n    # First full initialization: simple average\n    if self._value is None:\n        self._value = sum(self._window) / self._period\n        return self._value\n\n    self._value = (\n        (self._value * (self._period - 1)) + price\n    ) / self._period\n\n    return self._value\n</code></pre>"},{"location":"reference/stream/indicators/state/bollinger_bands_state/","title":"Bollinger Bands State","text":""},{"location":"reference/stream/indicators/state/bollinger_bands_state/#contangostreamindicatorsstatebollinger_bands_state","title":"<code>contango.stream.indicators.state.bollinger_bands_state</code>","text":""},{"location":"reference/stream/indicators/state/bollinger_bands_state/#contango.stream.indicators.state.bollinger_bands_state","title":"contango.stream.indicators.state.bollinger_bands_state","text":""},{"location":"reference/stream/indicators/state/bollinger_bands_state/#contango.stream.indicators.state.bollinger_bands_state.BollingerState","title":"BollingerState","text":"<p>               Bases: <code>Enum</code></p> <p>Current price location relative to Bollinger Bands.</p> <p>Attributes:</p> Name Type Description <code>BELOW_LOWER</code> <p>The price is below the lower bollinger band.</p> <code>BETWEEN_LOWER_AND_MIDDLE</code> <p>The price is between the lower &amp; middle bollinger bands.</p> <code>BETWEEN_MIDDLE_AND_HIGHER</code> <p>The price is above the middle bollinger band but below the higher one.</p> <code>ABOVE_HIGHER</code> <p>The price is above the higher bollinger band.</p>"},{"location":"reference/stream/indicators/state/bollinger_bands_state/#contango.stream.indicators.state.bollinger_bands_state.BollingerState.get_state","title":"get_state  <code>classmethod</code>","text":"<pre><code>get_state(current_price: float, bands: BollingerBandSnapshot) -&gt; BollingerState\n</code></pre> <p>Returns the band zone that the price is currently in.</p> <p>Parameters:</p> Name Type Description Default <code>current_price</code> <code>float</code> <p>The price for the current bar.</p> required <code>bands</code> <code>BollingerBandSnapshot</code> <p>The snapshot of bollinger bands for the current bar.</p> required <p>Returns:</p> Name Type Description <code>BollingerState</code> <code>BollingerState</code> <p>The state enum of the price relative to the bollinger bands.</p> Source code in <code>src\\contango\\stream\\indicators\\state\\bollinger_bands_state.py</code> <pre><code>@classmethod\ndef get_state(cls, current_price: float, bands: BollingerBandSnapshot) -&gt; BollingerState:\n    \"\"\"\n    Returns the band zone that the price is currently in.\n\n    Args:\n        current_price: The price for the current bar.\n        bands: The snapshot of bollinger bands for the current bar.\n\n    Returns:\n        BollingerState: The state enum of the price relative to the bollinger bands.\n    \"\"\"\n    if current_price &lt; bands.lower:\n        return BollingerState.BELOW_LOWER\n    if current_price &lt; bands.middle:\n        return BollingerState.BETWEEN_LOWER_AND_MIDDLE\n    if current_price &lt; bands.upper:\n        return BollingerState.BETWEEN_MIDDLE_AND_HIGHER\n    return BollingerState.ABOVE_HIGHER\n</code></pre>"},{"location":"reference/stream/indicators/state/ema_state/","title":"EMA State","text":""},{"location":"reference/stream/indicators/state/ema_state/#contangostreamindicatorsstateema_state","title":"<code>contango.stream.indicators.state.ema_state</code>","text":""},{"location":"reference/stream/indicators/state/ema_state/#contango.stream.indicators.state.ema_state","title":"contango.stream.indicators.state.ema_state","text":""},{"location":"reference/stream/indicators/state/ema_state/#contango.stream.indicators.state.ema_state.EMAState","title":"EMAState","text":"<p>               Bases: <code>Enum</code></p> <p>Current location of the fast EMA relative to the slow EMA for a bar.</p> <p>Attributes:</p> Name Type Description <code>BELOW</code> <p>The fast EMA is below the slow EMA.</p> <code>ABOVE</code> <p>The fast EMA is above the slow EMA.</p>"},{"location":"reference/stream/indicators/state/ema_state/#contango.stream.indicators.state.ema_state.EMAState.get_state","title":"get_state  <code>classmethod</code>","text":"<pre><code>get_state(fast_ema: float, slow_ema: float) -&gt; EMAState\n</code></pre> <p>Returns the state of the fast EMA relative to the slow EMA (above or below).</p> <p>Parameters:</p> Name Type Description Default <code>fast_ema</code> <code>float</code> <p>The fast EMA value for the current bar.</p> required <code>slow_ema</code> <code>float</code> <p>The slow EMA value for the current bar.</p> required <p>Returns:</p> Name Type Description <code>EMAState</code> <code>EMAState</code> <p>Whether the fast EMA is below or above the slow EMA.</p> Source code in <code>src\\contango\\stream\\indicators\\state\\ema_state.py</code> <pre><code>@classmethod\ndef get_state(cls, fast_ema: float, slow_ema: float) -&gt; EMAState:\n    \"\"\"\n    Returns the state of the fast EMA relative to the slow EMA (above or below).\n\n    Args:\n        fast_ema: The fast EMA value for the current bar.\n        slow_ema: The slow EMA value for the current bar.\n\n    Returns:\n        EMAState: Whether the fast EMA is below or above the slow EMA.\n    \"\"\"\n    if fast_ema &gt; slow_ema:\n        return EMAState.ABOVE\n    else:\n        return EMAState.BELOW\n</code></pre>"},{"location":"reference/stream/indicators/state/rsi_state/","title":"RSI State","text":""},{"location":"reference/stream/indicators/state/rsi_state/#contangostreamindicatorsstatersi_state","title":"<code>contango.stream.indicators.state.rsi_state</code>","text":""},{"location":"reference/stream/indicators/state/rsi_state/#contango.stream.indicators.state.rsi_state","title":"contango.stream.indicators.state.rsi_state","text":""},{"location":"reference/stream/indicators/state/rsi_state/#contango.stream.indicators.state.rsi_state.RSIState","title":"RSIState","text":"<p>               Bases: <code>Enum</code></p> <p>The position of the RSI relative to its thresholds.</p> <p>Attributes:</p> Name Type Description <code>OVERSOLD</code> <p>RSI is below lower threshold.</p> <code>NEUTRAL</code> <p>RSI is between thresholds.</p> <code>OVERBOUGHT</code> <p>RSI is above upper threshold.</p>"},{"location":"reference/stream/indicators/state/rsi_state/#contango.stream.indicators.state.rsi_state.RSIState.get_state","title":"get_state  <code>classmethod</code>","text":"<pre><code>get_state(rsi: float, lower_threshold: float, upper_threshold: float) -&gt; RSIState\n</code></pre> <p>Returns the state of the RSI relative to its thresholds.</p> <p>Parameters:</p> Name Type Description Default <code>rsi</code> <code>float</code> <p>The RSI value for the current bar.</p> required <code>lower_threshold</code> <code>float</code> <p>The lower threshold for the RSI (determines if oversold).</p> required <code>upper_threshold</code> <code>float</code> <p>The upper threshold for the RSI (determines if overbought).</p> required <p>Returns:</p> Name Type Description <code>RSIState</code> <code>RSIState</code> <p>Whether the RSI is above or below their respective thresholds (oversold or overbought).</p> Source code in <code>src\\contango\\stream\\indicators\\state\\rsi_state.py</code> <pre><code>@classmethod\ndef get_state(\n    cls,\n    rsi: float, \n    lower_threshold: float, \n    upper_threshold: float\n) -&gt; RSIState:\n    \"\"\"\n    Returns the state of the RSI relative to its thresholds.\n\n    Args:\n        rsi: The RSI value for the current bar.\n        lower_threshold: The lower threshold for the RSI (determines if oversold).\n        upper_threshold: The upper threshold for the RSI (determines if overbought).\n\n    Returns:\n        RSIState: Whether the RSI is above or below their respective thresholds (oversold or overbought).\n    \"\"\"\n    if rsi &lt; lower_threshold:\n        return RSIState.OVERSOLD\n    elif rsi &gt; upper_threshold:\n        return RSIState.OVERBOUGHT\n    else:\n        return RSIState.NEUTRAL\n</code></pre>"},{"location":"reference/stream/indicators/state/sma_state/","title":"SMA State","text":""},{"location":"reference/stream/indicators/state/sma_state/#contangostreamindicatorsstatesma_state","title":"<code>contango.stream.indicators.state.sma_state</code>","text":""},{"location":"reference/stream/indicators/state/sma_state/#contango.stream.indicators.state.sma_state","title":"contango.stream.indicators.state.sma_state","text":""},{"location":"reference/stream/indicators/state/sma_state/#contango.stream.indicators.state.sma_state.SMAState","title":"SMAState","text":"<p>               Bases: <code>Enum</code></p> <p>The position of the fast SMA relative to a slow SMA.</p> <p>Attributes:</p> Name Type Description <code>BELOW</code> <p>The fast SMA below the slow SMA.</p> <code>ABOVE</code> <p>The fast SMA is above the slow SMA.</p>"},{"location":"reference/stream/indicators/state/sma_state/#contango.stream.indicators.state.sma_state.SMAState.get_state","title":"get_state  <code>classmethod</code>","text":"<pre><code>get_state(fast_sma: float, slow_sma: float) -&gt; SMAState\n</code></pre> <p>Returns the SMA state of the fast SMA relative to the slow SMA.</p> <p>Parameters:</p> Name Type Description Default <code>fast_sma</code> <code>float</code> <p>The value of the fast SMA for the current bar.</p> required <code>slow_sma</code> <code>float</code> <p>The value of the slow SMA for the current bar.</p> required <p>Returns:</p> Name Type Description <code>SMAState</code> <code>SMAState</code> <p>Whether the fast SMA is above or below the slow SMA.</p> Source code in <code>src\\contango\\stream\\indicators\\state\\sma_state.py</code> <pre><code>@classmethod\ndef get_state(cls, fast_sma: float, slow_sma: float) -&gt; SMAState:\n    \"\"\"\n    Returns the SMA state of the fast SMA relative to the slow SMA.\n\n    Args:\n        fast_sma: The value of the fast SMA for the current bar.\n        slow_sma: The value of the slow SMA for the current bar.\n\n    Returns:\n        SMAState: Whether the fast SMA is above or below the slow SMA.\n    \"\"\"\n    if fast_sma &gt; slow_sma:\n        return SMAState.ABOVE\n    else:\n        return SMAState.BELOW\n</code></pre>"},{"location":"reference/stream/indicators/state/vwap_state/","title":"VWAP State","text":""},{"location":"reference/stream/indicators/state/vwap_state/#contangostreamindicatorsstatevwap_state","title":"<code>contango.stream.indicators.state.vwap_state</code>","text":""},{"location":"reference/stream/indicators/state/vwap_state/#contango.stream.indicators.state.vwap_state","title":"contango.stream.indicators.state.vwap_state","text":""},{"location":"reference/stream/indicators/state/vwap_state/#contango.stream.indicators.state.vwap_state.VWAPState","title":"VWAPState","text":"<p>               Bases: <code>Enum</code></p> <p>The current price location relative to VWAP.</p> <p>Attributes:</p> Name Type Description <code>BELOW_LOWER</code> <p>The price is below the lower band.</p> <code>BETWEEN_LOWER_AND_MIDDLE</code> <p>The price is between the lower &amp; VWAP band.</p> <code>ABOVE_MIDDLE</code> <p>The price is above the VWAP band.</p>"},{"location":"reference/stream/indicators/state/vwap_state/#contango.stream.indicators.state.vwap_state.VWAPState.get_state","title":"get_state  <code>classmethod</code>","text":"<pre><code>get_state(price: float, snapshot: VWAPSnapshot) -&gt; VWAPState\n</code></pre> <p>Returns the zone of the price relative to the VWAP bands.</p> <p>Parameters:</p> Name Type Description Default <code>price</code> <code>float</code> <p>The close price for the current bar.</p> required <code>snapshot</code> <code>VWAPSnapshot</code> <p>The VWAP snapshot for the current bar.</p> required <p>Returns:</p> Name Type Description <code>VWAPState</code> <code>VWAPState</code> <p>The price relative to the VWAP snapshot.</p> Source code in <code>src\\contango\\stream\\indicators\\state\\vwap_state.py</code> <pre><code>@classmethod\ndef get_state(cls, price: float, snapshot: VWAPSnapshot) -&gt; VWAPState:\n    \"\"\"\n    Returns the zone of the price relative to the VWAP bands.\n\n    Args:\n        price: The close price for the current bar.\n        snapshot: The VWAP snapshot for the current bar.\n\n    Returns:\n        VWAPState: The price relative to the VWAP snapshot.\n    \"\"\"\n    if price &lt; snapshot.lower:\n        return VWAPState.BELOW_LOWER\n    if price &lt; snapshot.vwap:\n        return VWAPState.BETWEEN_LOWER_AND_VWAP\n    return VWAPState.ABOVE_VWAP\n</code></pre>"},{"location":"reference/stream/indicators/state/wilder_average_state/","title":"Wilder Average State","text":""},{"location":"reference/stream/indicators/state/wilder_average_state/#contangostreamindicatorsstatewilder_average_state","title":"<code>contango.stream.indicators.state.wilder_average_state</code>","text":""},{"location":"reference/stream/indicators/state/wilder_average_state/#contango.stream.indicators.state.wilder_average_state","title":"contango.stream.indicators.state.wilder_average_state","text":""},{"location":"reference/stream/indicators/state/wilder_average_state/#contango.stream.indicators.state.wilder_average_state.WilderState","title":"WilderState","text":"<p>               Bases: <code>Enum</code></p> <p>Current location of the fast wilder average relative to the slow wilder average for a bar.</p> <p>Attributes:</p> Name Type Description <code>BELOW</code> <p>The fast wilder average is below the slow wilder average.</p> <code>ABOVE</code> <p>The fast wilder average is above the slow wilder average.</p>"},{"location":"reference/stream/indicators/state/wilder_average_state/#contango.stream.indicators.state.wilder_average_state.WilderState.get_state","title":"get_state  <code>classmethod</code>","text":"<pre><code>get_state(fast_wilder_average: float, slow_wilder_average: float) -&gt; WilderState\n</code></pre> <p>Returns the state of the fast wilder average relative to the slow wilder average (above or below).</p> <p>Parameters:</p> Name Type Description Default <code>fast_wilder_average</code> <code>float</code> <p>The fast wilder average value for the current bar.</p> required <code>slow_wilder_average</code> <code>float</code> <p>The slow wilder average value for the current bar.</p> required <p>Returns:</p> Name Type Description <code>WilderState</code> <code>WilderState</code> <p>Whether the fast wilder average is below or above the slow wilder average.</p> Source code in <code>src\\contango\\stream\\indicators\\state\\wilder_average_state.py</code> <pre><code>@classmethod\ndef get_state(cls, fast_wilder_average: float, slow_wilder_average: float) -&gt; WilderState:\n    \"\"\"\n    Returns the state of the fast wilder average relative to the slow wilder average (above or below).\n\n    Args:\n        fast_wilder_average: The fast wilder average value for the current bar.\n        slow_wilder_average: The slow wilder average value for the current bar.\n\n    Returns:\n        WilderState: Whether the fast wilder average is below or above the slow wilder average.\n    \"\"\"\n    if fast_wilder_average &gt; slow_wilder_average:\n        return WilderState.ABOVE\n    else:\n        return WilderState.BELOW\n</code></pre>"},{"location":"reference/trading/analysis/graph_config/","title":"Graph Config","text":""},{"location":"reference/trading/analysis/graph_config/#contangotradinganalysisgraph_config","title":"<code>contango.trading.analysis.graph_config</code>","text":""},{"location":"reference/trading/analysis/graph_config/#contango.trading.analysis.graph_config","title":"contango.trading.analysis.graph_config","text":""},{"location":"reference/trading/analysis/graph_config/#contango.trading.analysis.graph_config.GraphConfig","title":"GraphConfig  <code>dataclass</code>","text":"<pre><code>GraphConfig()\n</code></pre> <p>A base config for all graph types.</p>"},{"location":"reference/trading/analysis/graph_registry/","title":"Graph Registry","text":""},{"location":"reference/trading/analysis/graph_registry/#contangotradinganalysisgraph_registry","title":"<code>contango.trading.analysis.graph_registry</code>","text":""},{"location":"reference/trading/analysis/graph_registry/#contango.trading.analysis.graph_registry","title":"contango.trading.analysis.graph_registry","text":""},{"location":"reference/trading/analysis/graph_registry/#contango.trading.analysis.graph_registry.register_graph","title":"register_graph","text":"<pre><code>register_graph(name: str) -&gt; Callable[[type[GraphType[C]]], type[GraphType[C]]]\n</code></pre> <p>Registers a graph type with a name into registry.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The designated name for the graph type.</p> required Source code in <code>src\\contango\\trading\\analysis\\graph_registry.py</code> <pre><code>def register_graph(name: str) -&gt; Callable[[type[GraphType[C]]], type[GraphType[C]]]:\n    \"\"\"\n    Registers a graph type with a name into registry.\n\n    Args:\n        name: The designated name for the graph type.\n    \"\"\"\n    def decorator(cls: type[GraphType[C]]) -&gt; type[GraphType[C]]:\n        if name in GRAPH_REGISTRY:\n            raise ValueError(f\"Graph '{name}' already registered\")\n\n        cls.graph_name = name\n        GRAPH_REGISTRY[name] = cls\n        return cls\n    return decorator\n</code></pre>"},{"location":"reference/trading/analysis/graph_type/","title":"Graph Type","text":""},{"location":"reference/trading/analysis/graph_type/#contangotradinganalysisgraph_type","title":"<code>contango.trading.analysis.graph_type</code>","text":""},{"location":"reference/trading/analysis/graph_type/#contango.trading.analysis.graph_type","title":"contango.trading.analysis.graph_type","text":""},{"location":"reference/trading/analysis/graph_type/#contango.trading.analysis.graph_type.GraphType","title":"GraphType","text":"<pre><code>GraphType(config: C)\n</code></pre> <p>               Bases: <code>ABC</code>, <code>Generic[C]</code></p> <p>A generic abstract class that all graphs should implement.</p> <p>Attributes:</p> Name Type Description <code>C</code> <p>The <code>GraphConfig</code> for the class.</p> <code>graph_name</code> <code>str</code> <p>The unique name for the graph.</p> <p>Initializes <code>GraphType</code>.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>C</code> <p>The matching configuration for the graph type.</p> required Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>def __init__(self, config: C) -&gt; None:\n    \"\"\"\n    Initializes `GraphType`.\n\n    Args:\n        config: The matching configuration for the graph type.\n    \"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/trading/analysis/graph_type/#contango.trading.analysis.graph_type.GraphType.config","title":"config  <code>property</code>","text":"<pre><code>config: C\n</code></pre> <p>The config for the graph type.</p>"},{"location":"reference/trading/analysis/graph_type/#contango.trading.analysis.graph_type.GraphType.build","title":"build  <code>abstractmethod</code>","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds the graph type.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The pre-validated experiments dataframe.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>go.Figure: A plotly graph figure.</p> Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>@abstractmethod\ndef build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds the graph type.\n\n    Args:\n        df: The pre-validated experiments dataframe.\n\n    Returns:\n        go.Figure: A plotly graph figure.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/trading/analysis/data/data_prep/","title":"Data Prep","text":""},{"location":"reference/trading/analysis/data/data_prep/#contangotradinganalysisdatadata_prep","title":"<code>contango.trading.analysis.data.data_prep</code>","text":""},{"location":"reference/trading/analysis/data/data_prep/#contango.trading.analysis.data.data_prep","title":"contango.trading.analysis.data.data_prep","text":""},{"location":"reference/trading/analysis/data/data_prep/#contango.trading.analysis.data.data_prep.get_param_columns","title":"get_param_columns","text":"<pre><code>get_param_columns(df: DataFrame, exclude_constant: bool = True) -&gt; list[str]\n</code></pre> <p>Returns the columns in <code>df</code> that represent swept strategy parameters.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame produced by <code>results_to_dataframe</code>.</p> required <code>exclude_constant</code> <code>bool</code> <p>If True (default), drop parameters that only take a               single unique value across all rows.</p> <code>True</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of parameter column names.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\data_prep.py</code> <pre><code>def get_param_columns(df: pd.DataFrame, exclude_constant: bool = True) -&gt; list[str]:\n    \"\"\"\n    Returns the columns in `df` that represent swept strategy parameters.\n\n    Args:\n        df: DataFrame produced by `results_to_dataframe`.\n        exclude_constant: If True (default), drop parameters that only take a\n                          single unique value across all rows.\n\n    Returns:\n        List of parameter column names.\n    \"\"\"\n    param_columns = [c for c in df.columns if c not in _METRIC_COLUMNS]\n    param_columns = [\n        c for c in param_columns\n        if df[c].map(is_hashable).all()\n    ]\n\n    if exclude_constant:\n        param_columns = [c for c in param_columns if df[c].nunique(dropna=False) &gt; 1]\n\n    return param_columns\n</code></pre>"},{"location":"reference/trading/analysis/data/data_prep/#contango.trading.analysis.data.data_prep.results_to_dataframe","title":"results_to_dataframe","text":"<pre><code>results_to_dataframe(experiments: list[GraphingExperiment]) -&gt; pd.DataFrame\n</code></pre> <p>Flattens a list of GraphingExperiment instances into a DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>experiments</code> <code>list[GraphingExperiment]</code> <p>The list of GraphingExperiment instances.</p> required <p>Returns:</p> Type Description <code>DataFrame</code> <p>pd.DataFrame: A pandas DataFrame with one row per experiment.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\data_prep.py</code> <pre><code>def results_to_dataframe(experiments: list[GraphingExperiment]) -&gt; pd.DataFrame:\n    \"\"\"\n    Flattens a list of GraphingExperiment instances into a DataFrame.\n\n    Args:\n        experiments: The list of GraphingExperiment instances.\n\n    Returns:\n        pd.DataFrame: A pandas DataFrame with one row per experiment.\n    \"\"\"\n    rows: list[dict[str, Any]] = []\n\n    for experiment in experiments:\n        metrics = experiment.metrics\n        name = experiment.name\n\n        row: dict[str, Any] = {}\n        for key, value in experiment.parameters.items():\n            row[key] = value\n\n        row[\"total_return\"] = metrics.returns.total_return\n        row[\"monthly_returns\"] = metrics.returns.monthly_returns\n        row[\"equity_curve\"] = metrics.returns.equity_curve\n\n        row[\"annual_return\"] = metrics.risk.annual_return\n        row[\"monthly_volatility\"] = metrics.risk.monthly_volatility\n        row[\"sharpe_ratio\"] = metrics.risk.sharpe_ratio\n        row[\"calmar_ratio\"] = metrics.risk.calmar_ratio\n\n        row[\"max_drawdown\"] = metrics.drawdowns.max_drawdown\n        row[\"average_drawdown\"] = metrics.drawdowns.average_drawdown\n\n        row[\"trade_count\"] = metrics.trades.trade_count\n        row[\"win_rate\"] = metrics.trades.win_rate\n        row[\"profit_factor\"] = metrics.trades.profit_factor\n        row[\"expectancy\"] = metrics.trades.expectancy\n        row[\"average_win\"] = metrics.trades.average_win\n        row[\"average_loss\"] = metrics.trades.average_loss\n        row[\"average_holding_period\"] = metrics.trades.average_holding_period\n\n        row[\"name\"] = name\n\n        rows.append(row)\n\n    return pd.DataFrame(rows)\n</code></pre>"},{"location":"reference/trading/analysis/data/graph_runner/","title":"Graph Runner","text":""},{"location":"reference/trading/analysis/data/graph_runner/#contangotradinganalysisdatagraph_runner","title":"<code>contango.trading.analysis.data.graph_runner</code>","text":""},{"location":"reference/trading/analysis/data/graph_runner/#contango.trading.analysis.data.graph_runner","title":"contango.trading.analysis.data.graph_runner","text":""},{"location":"reference/trading/analysis/data/graph_runner/#contango.trading.analysis.data.graph_runner.GraphRunner","title":"GraphRunner","text":"<pre><code>GraphRunner(specs: list[GraphSpec] = DEFAULT_GRAPH_SPECS, template: Template | None = DARK_GRAY_TEMPLATE)\n</code></pre> <p>Manages a set of graph specs, instantiating them once the experiment DataFrame is available.</p> <p>Initializes <code>GraphRunner</code>.</p> <p>Parameters:</p> Name Type Description Default <code>specs</code> <code>list[GraphSpec]</code> <p>A list of specs for each graph type.</p> <code>DEFAULT_GRAPH_SPECS</code> <code>template</code> <code>Template | None</code> <p>The plotly template for all graphs to follow.</p> <code>DARK_GRAY_TEMPLATE</code> Source code in <code>src\\contango\\trading\\analysis\\data\\graph_runner.py</code> <pre><code>def __init__(\n    self, \n    specs: list[GraphSpec] = DEFAULT_GRAPH_SPECS,\n    template: go.layout.Template | None = DARK_GRAY_TEMPLATE\n) -&gt; None:\n    \"\"\"\n    Initializes `GraphRunner`.\n\n    Args:\n        specs: A list of specs for each graph type.\n        template: The plotly template for all graphs to follow.\n    \"\"\"\n    self._specs: list[GraphSpec] = list(specs)\n    self._graphs: list[GraphType[Any]] = []\n    self._template = template\n</code></pre>"},{"location":"reference/trading/analysis/data/graph_runner/#contango.trading.analysis.data.graph_runner.GraphRunner.graphs","title":"graphs  <code>property</code>","text":"<pre><code>graphs: list[GraphType[Any]]\n</code></pre> <p>The currently active graph instances (empty until <code>build_all</code> has run at least once, since some configs depend on the data).</p>"},{"location":"reference/trading/analysis/data/graph_runner/#contango.trading.analysis.data.graph_runner.GraphRunner.add_graph","title":"add_graph","text":"<pre><code>add_graph(name: str, config: Union[GraphConfig, GraphConfigFactory]) -&gt; None\n</code></pre> <p>Registers a graph spec to be instantiated on the next <code>build_all</code>.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\graph_runner.py</code> <pre><code>def add_graph(self, name: str, config: Union[GraphConfig, GraphConfigFactory]) -&gt; None:\n    \"\"\"\n    Registers a graph spec to be instantiated on the next `build_all`.\n    \"\"\"\n    if name not in GRAPH_REGISTRY:\n        raise KeyError(f\"No graph registered under '{name}'. Known: {list(GRAPH_REGISTRY)}\")\n    self._specs.append(GraphSpec(name, config))\n</code></pre>"},{"location":"reference/trading/analysis/data/graph_runner/#contango.trading.analysis.data.graph_runner.GraphRunner.build_all","title":"build_all","text":"<pre><code>build_all(experiments: list[GraphingExperiment]) -&gt; dict[str, go.Figure]\n</code></pre> <p>Resolves every spec's config against the data, instantiates the graphs, and builds all of their figures.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\graph_runner.py</code> <pre><code>def build_all(self, experiments: list[GraphingExperiment]) -&gt; dict[str, go.Figure]:\n    \"\"\"\n    Resolves every spec's config against the data, instantiates the\n    graphs, and builds all of their figures.\n    \"\"\"\n    df = results_to_dataframe(experiments)\n\n    self._graphs = []\n    for spec in self._specs:\n        config = _resolve(spec.config, df)\n        graph_cls = GRAPH_REGISTRY[spec.name]\n        self._graphs.append(graph_cls(config))\n\n    figures = {graph.graph_name: graph.build(df) for graph in self._graphs}\n\n    if self._template is not None:\n        for fig in figures.values():\n            fig.update_layout(template=self._template)  # type: ignore[unknownMemberType]\n\n    return figures\n</code></pre>"},{"location":"reference/trading/analysis/data/graph_runner/#contango.trading.analysis.data.graph_runner.GraphRunner.remove_by_name","title":"remove_by_name","text":"<pre><code>remove_by_name(name: str) -&gt; None\n</code></pre> <p>Removes all graphs with the same name.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\graph_runner.py</code> <pre><code>def remove_by_name(self, name: str) -&gt; None:\n    \"\"\"\n    Removes all graphs with the same name.\n    \"\"\"\n    self._specs = [s for s in self._specs if s.name != name]\n    self._graphs = [g for g in self._graphs if g.graph_name != name]\n</code></pre>"},{"location":"reference/trading/analysis/data/graph_runner/#contango.trading.analysis.data.graph_runner.GraphRunner.remove_graph","title":"remove_graph","text":"<pre><code>remove_graph(graph: GraphType[Any]) -&gt; None\n</code></pre> <p>Removes all graphs by instance.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\graph_runner.py</code> <pre><code>def remove_graph(self, graph: GraphType[Any]) -&gt; None:\n    \"\"\"\n    Removes all graphs by instance.\n    \"\"\"\n    self._graphs.remove(graph)\n    self._specs = [s for s in self._specs if s.name != graph.graph_name]\n</code></pre>"},{"location":"reference/trading/analysis/data/graph_runner/#contango.trading.analysis.data.graph_runner.GraphSpec","title":"GraphSpec  <code>dataclass</code>","text":"<pre><code>GraphSpec(name: str, config: Union[GraphConfig, GraphConfigFactory])\n</code></pre> <p>A named graph type paired with its config \u2014 either a concrete config, or a factory that builds one once the experiment DataFrame is available.</p>"},{"location":"reference/trading/analysis/data/graphing_experiment/","title":"Graphing Experiment","text":""},{"location":"reference/trading/analysis/data/graphing_experiment/#contangotradinganalysisdatagraphing_experiment","title":"<code>contango.trading.analysis.data.graphing_experiment</code>","text":""},{"location":"reference/trading/analysis/data/graphing_experiment/#contango.trading.analysis.data.graphing_experiment","title":"contango.trading.analysis.data.graphing_experiment","text":""},{"location":"reference/trading/analysis/data/graphing_experiment/#contango.trading.analysis.data.graphing_experiment.GraphingExperiment","title":"GraphingExperiment","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single experiment when graphing.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The name for the experiment.</p> <code>metrics</code> <code>Metrics</code> <p>The metrics for the experiment.</p> <code>parameters</code> <code>Mapping[str, Any]</code> <p>A dict mapping strings to the parameters for the experiment.</p>"},{"location":"reference/trading/analysis/data/graphing_experiment/#contango.trading.analysis.data.graphing_experiment.GraphingExperiment.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Returns a string representation of the experiment.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\graphing_experiment.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"\n    Returns a string representation of the experiment.\n    \"\"\"\n    return (\n        f\"Name: {self.name}\\n\"\n        f\"Parameters: {dict(self.parameters)}\\n\"\n        f\"Metrics: \\n{self.metrics}\\n\"\n    )\n</code></pre>"},{"location":"reference/trading/analysis/data/graphing_experiment/#contango.trading.analysis.data.graphing_experiment.GraphingExperiment.from_experiment_result","title":"from_experiment_result  <code>classmethod</code>","text":"<pre><code>from_experiment_result(result: ExperimentResult) -&gt; GraphingExperiment\n</code></pre> <p>Returns a <code>GraphingExperiment</code> from an <code>ExperimentResult</code>.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\graphing_experiment.py</code> <pre><code>@classmethod\ndef from_experiment_result(cls, result: ExperimentResult) -&gt; GraphingExperiment:\n    \"\"\"\n    Returns a `GraphingExperiment` from an `ExperimentResult`.\n    \"\"\"\n    return GraphingExperiment(result.label, result.metrics, result.kwargs)\n</code></pre>"},{"location":"reference/trading/analysis/data/graphing_experiment/#contango.trading.analysis.data.graphing_experiment.GraphingExperiment.from_experiment_results","title":"from_experiment_results  <code>classmethod</code>","text":"<pre><code>from_experiment_results(results: list[ExperimentResult]) -&gt; list[GraphingExperiment]\n</code></pre> <p>Returns a list of <code>GraphingExperiment</code> instances from a list of <code>ExperimentResult</code> instances.</p> Source code in <code>src\\contango\\trading\\analysis\\data\\graphing_experiment.py</code> <pre><code>@classmethod\ndef from_experiment_results(cls, results: list[ExperimentResult]) -&gt; list[GraphingExperiment]:\n    \"\"\"\n    Returns a list of `GraphingExperiment` instances from a list of `ExperimentResult` instances.\n    \"\"\"\n    return [cls.from_experiment_result(result) for result in results]\n</code></pre>"},{"location":"reference/trading/analysis/graphing/downsample/","title":"Downsample","text":""},{"location":"reference/trading/analysis/graphing/downsample/#contangotradinganalysisgraphingdownsample","title":"<code>contango.trading.analysis.graphing.downsample</code>","text":""},{"location":"reference/trading/analysis/graphing/downsample/#contango.trading.analysis.graphing.downsample","title":"contango.trading.analysis.graphing.downsample","text":""},{"location":"reference/trading/analysis/graphing/downsample/#contango.trading.analysis.graphing.downsample.downsample_curve","title":"downsample_curve","text":"<pre><code>downsample_curve(curve: tuple[tuple[int, float], ...], max_points: int = 1500) -&gt; tuple[tuple[int, float], ...]\n</code></pre> <p>Reduces a  curve to at most <code>max_points</code> points via fixed stride sampling, always keeping the final point.</p> <p>Parameters:</p> Name Type Description Default <code>curve</code> <code>tuple[tuple[int, float], ...]</code> <p>The raw (timestamp, value) points, in order.</p> required <code>max_points</code> <code>int</code> <p>The maximum number of points to keep.</p> <code>1500</code> <p>Returns:</p> Type Description <code>tuple[tuple[int, float], ...]</code> <p>The downsampled curve. Returned unchanged if already within max_points.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\downsample.py</code> <pre><code>def downsample_curve(\n    curve: tuple[tuple[int, float], ...],\n    max_points: int = 1500,\n) -&gt; tuple[tuple[int, float], ...]:\n    \"\"\"\n    Reduces a  curve to at most `max_points` points via\n    fixed stride sampling, always keeping the final point.\n\n    Args:\n        curve: The raw (timestamp, value) points, in order.\n        max_points: The maximum number of points to keep.\n\n    Returns:\n        The downsampled curve. Returned unchanged if already within max_points.\n    \"\"\"\n    if len(curve) &lt;= max_points:\n        return curve\n\n    step = len(curve) / max_points\n    indices = [int(i * step) for i in range(max_points)]\n    if indices[-1] != len(curve) - 1:\n        indices.append(len(curve) - 1)\n\n    return tuple(curve[i] for i in indices)\n</code></pre>"},{"location":"reference/trading/analysis/graphing/equity_curve_overlay/","title":"Equity Curve Overlay","text":""},{"location":"reference/trading/analysis/graphing/equity_curve_overlay/#contangotradinganalysisgraphingequity_curve_overlay","title":"<code>contango.trading.analysis.graphing.equity_curve_overlay</code>","text":""},{"location":"reference/trading/analysis/graphing/equity_curve_overlay/#contango.trading.analysis.graphing.equity_curve_overlay","title":"contango.trading.analysis.graphing.equity_curve_overlay","text":""},{"location":"reference/trading/analysis/graphing/equity_curve_overlay/#contango.trading.analysis.graphing.equity_curve_overlay.EquityCurveConfig","title":"EquityCurveConfig  <code>dataclass</code>","text":"<pre><code>EquityCurveConfig(target_metric: str, top_n: int, max_downsample_points: int = 1500)\n</code></pre> <p>               Bases: <code>GraphConfig</code></p> <p>A graph configuration specific to the equity curve graph.</p> <p>Attributes:</p> Name Type Description <code>target_metric</code> <code>str</code> <p>The name for the targeted metric to graph.</p> <code>top_n</code> <code>int</code> <p>The top amount of values to keep for the graph.</p> <code>max_downsample_points</code> <code>int</code> <p>The maximum amount of downsample points to graph.                    Changing this value directly alters performance; however,                    it should usually not be changed for most uses.</p>"},{"location":"reference/trading/analysis/graphing/equity_curve_overlay/#contango.trading.analysis.graphing.equity_curve_overlay.EquityCurveOverlay","title":"EquityCurveOverlay","text":"<pre><code>EquityCurveOverlay(config: EquityCurveConfig)\n</code></pre> <p>               Bases: <code>GraphType[EquityCurveConfig]</code></p> <p>A graph type that shows the equity over time for a specified metric.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\equity_curve_overlay.py</code> <pre><code>def __init__(self, config: EquityCurveConfig) -&gt; None:\n    super().__init__(config)\n</code></pre>"},{"location":"reference/trading/analysis/graphing/equity_curve_overlay/#contango.trading.analysis.graphing.equity_curve_overlay.EquityCurveOverlay.build","title":"build","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds an equity curve overlay for the top top_n experiments.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The experiment DataFrame.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>A plotly Figure.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\equity_curve_overlay.py</code> <pre><code>def build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds an equity curve overlay for the top top_n experiments.\n\n    Args:\n        df: The experiment DataFrame.\n\n    Returns:\n        A plotly Figure.\n    \"\"\"\n    plot_df = (\n        df.dropna(subset=[\"equity_curve\"])\n        .sort_values(self.config.target_metric, ascending=False)\n        .head(self.config.top_n)\n        .reset_index(drop=True)\n    )\n\n    fig = go.Figure()\n    for _, row in plot_df.iterrows():\n        curve = downsample_curve(row[\"equity_curve\"], self.config.max_downsample_points)\n        timestamps: list[pd.Timestamp] = [pd.to_datetime(t, unit=\"ms\") for t, _ in curve]\n        values = [v for _, v in curve]\n        fig.add_trace(  # type: ignore[unknownMemberType]\n            go.Scatter(\n                x=timestamps,\n                y=values,\n                mode=\"lines\",\n                name=f\"{row['name']} ({self.config.target_metric}={row[self.config.target_metric]:.2f})\",\n            )\n        )\n\n    fig.update_layout(  # type: ignore[unknownMemberType]\n        title=f\"Equity Curve Overlay: top {len(plot_df)} by {self.config.target_metric}\",\n        xaxis_title=\"Time\",\n        yaxis_title=\"Account Value (USD)\",\n    )\n    return fig\n</code></pre>"},{"location":"reference/trading/analysis/graphing/metric_distribution/","title":"Metric Distribution","text":""},{"location":"reference/trading/analysis/graphing/metric_distribution/#contangotradinganalysisgraphingmetric_distribution","title":"<code>contango.trading.analysis.graphing.metric_distribution</code>","text":""},{"location":"reference/trading/analysis/graphing/metric_distribution/#contango.trading.analysis.graphing.metric_distribution","title":"contango.trading.analysis.graphing.metric_distribution","text":""},{"location":"reference/trading/analysis/graphing/metric_distribution/#contango.trading.analysis.graphing.metric_distribution.MetricDistribution","title":"MetricDistribution","text":"<pre><code>MetricDistribution(config: C)\n</code></pre> <p>               Bases: <code>GraphType[MetricDistributionConfig]</code></p> <p>A graph type that shows the distribution of a metric grouped by a parameter.</p> Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>def __init__(self, config: C) -&gt; None:\n    \"\"\"\n    Initializes `GraphType`.\n\n    Args:\n        config: The matching configuration for the graph type.\n    \"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/trading/analysis/graphing/metric_distribution/#contango.trading.analysis.graphing.metric_distribution.MetricDistribution.build","title":"build","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds a distribution plot of target_metric grouped by group_by.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The experiment DataFrame.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>A plotly Figure.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\metric_distribution.py</code> <pre><code>def build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds a distribution plot of target_metric grouped by group_by.\n\n    Args:\n        df: The experiment DataFrame.\n\n    Returns:\n        A plotly Figure.\n    \"\"\"\n    plot_df = df.dropna(subset=[self.config.group_by, self.config.target_metric]).copy()\n    plot_df[self.config.group_by] = plot_df[self.config.group_by].astype(str)\n\n    fig_fn = px.box if self.config.plot_type == \"box\" else px.violin  # type: ignore[unknownMemberType]\n    fig = fig_fn(\n        plot_df,\n        x=self.config.group_by,\n        y=self.config.target_metric,\n        points=\"all\",\n        title=f\"Distribution of {self.config.target_metric} by {self.config.group_by}\",\n        labels={self.config.group_by: self.config.group_by, self.config.target_metric: self.config.target_metric},\n    )\n    fig.update_layout()  # type: ignore[unknownMemberType]\n    return fig\n</code></pre>"},{"location":"reference/trading/analysis/graphing/metric_distribution/#contango.trading.analysis.graphing.metric_distribution.MetricDistributionConfig","title":"MetricDistributionConfig  <code>dataclass</code>","text":"<pre><code>MetricDistributionConfig(group_by: str, target_metric: str = 'total_return', plot_type: str = 'box')\n</code></pre> <p>               Bases: <code>GraphConfig</code></p> <p>A graph configuration specific to the metric distribution graph.</p> <p>Attributes:</p> Name Type Description <code>group_by</code> <code>str</code> <p>The column to group by, including a parameter column, or a       version of one for continuous parameters with various       distinct values.</p> <code>target_metric</code> <code>str</code> <p>The name for the targeted metric to graph.</p> <code>plot_type</code> <code>str</code> <p>The distribution plot style, either \"box\" or \"violin\".</p>"},{"location":"reference/trading/analysis/graphing/pairwise_heatmap_grid/","title":"Pairwise Heatmap Grid","text":""},{"location":"reference/trading/analysis/graphing/pairwise_heatmap_grid/#contangotradinganalysisgraphingpairwise_heatmap_grid","title":"<code>contango.trading.analysis.graphing.pairwise_heatmap_grid</code>","text":""},{"location":"reference/trading/analysis/graphing/pairwise_heatmap_grid/#contango.trading.analysis.graphing.pairwise_heatmap_grid","title":"contango.trading.analysis.graphing.pairwise_heatmap_grid","text":""},{"location":"reference/trading/analysis/graphing/pairwise_heatmap_grid/#contango.trading.analysis.graphing.pairwise_heatmap_grid.PairwiseHeatmapGrid","title":"PairwiseHeatmapGrid","text":"<pre><code>PairwiseHeatmapGrid(config: C)\n</code></pre> <p>               Bases: <code>GraphType[PairwiseHeatmapGridConfig]</code></p> <p>A graph type that shows a grid of 2D heatmaps, one panel per facet value.</p> Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>def __init__(self, config: C) -&gt; None:\n    \"\"\"\n    Initializes `GraphType`.\n\n    Args:\n        config: The matching configuration for the graph type.\n    \"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/trading/analysis/graphing/pairwise_heatmap_grid/#contango.trading.analysis.graphing.pairwise_heatmap_grid.PairwiseHeatmapGrid.build","title":"build","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds a grid of 2D heatmaps: param_x vs. param_y, one panel per unique value of facet_param.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The experiment DataFrame.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>A plotly Figure with one subplot per facet_param value.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\pairwise_heatmap_grid.py</code> <pre><code>def build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds a grid of 2D heatmaps: param_x vs. param_y, one panel per unique\n    value of facet_param.\n\n    Args:\n        df: The experiment DataFrame.\n\n    Returns:\n        A plotly Figure with one subplot per facet_param value.\n    \"\"\"\n    plot_df = df.dropna(subset=[self.config.param_x, self.config.param_y, self.config.facet_param, self.config.target_metric])\n    facet_values = sorted(plot_df[self.config.facet_param].unique())\n\n    n = len(facet_values)\n    cols = min(n, self.config.max_cols)\n\n    if cols == 0:\n        raise ValueError(\"No trades were made! Could not graph the pairwise heatmap grid.\")\n\n    rows = math.ceil(n / cols)\n\n    fig = make_subplots(\n        rows=rows,\n        cols=cols,\n        subplot_titles=[f\"{self.config.facet_param}={v}\" for v in facet_values],\n        shared_xaxes=True,\n        shared_yaxes=True,\n    )\n\n    zmin, zmax = plot_df[self.config.target_metric].min(), plot_df[self.config.target_metric].max()\n\n    for i, val in enumerate(facet_values):\n        mask: pd.Series = plot_df[self.config.facet_param] == val\n        subset: pd.DataFrame = plot_df[mask]\n        pivot = subset.pivot_table(\n            index=self.config.param_y,\n            columns=self.config.param_x,\n            values=self.config.target_metric,\n            aggfunc=\"mean\",\n        )\n\n        fig.add_trace(  # type: ignore[unknownMemberType]\n            go.Heatmap(\n                z=pivot.values,\n                x=pivot.columns,\n                y=pivot.index,\n                coloraxis=\"coloraxis\",\n            ),\n            row=(i // cols) + 1,\n            col=(i % cols) + 1,\n        )\n\n    fig.update_layout(  # type: ignore[unknownMemberType]\n        title=f\"Pairwise Heatmap Grid: {self.config.param_x} \u00d7 {self.config.param_y}, faceted by {self.config.facet_param} (color = {self.config.target_metric})\",\n        coloraxis={\"colorscale\": \"RdYlGn\", \"cmin\": zmin, \"cmax\": zmax},\n        height=320 * rows,\n    )\n    return fig\n</code></pre>"},{"location":"reference/trading/analysis/graphing/pairwise_heatmap_grid/#contango.trading.analysis.graphing.pairwise_heatmap_grid.PairwiseHeatmapGridConfig","title":"PairwiseHeatmapGridConfig  <code>dataclass</code>","text":"<pre><code>PairwiseHeatmapGridConfig(param_x: str, param_y: str, facet_param: str, target_metric: str = 'calmar_ratio', max_cols: int = 4)\n</code></pre> <p>               Bases: <code>GraphConfig</code></p> <p>A graph configuration specific to the pairwise heatmap grid graph.</p> <p>Attributes:</p> Name Type Description <code>param_x</code> <code>str</code> <p>The parameter column for the heatmap x-axis.</p> <code>param_y</code> <code>str</code> <p>The parameter column for the heatmap y-axis.</p> <code>facet_param</code> <code>str</code> <p>The parameter column to facet panels by.</p> <code>target_metric</code> <code>str</code> <p>The name for the targeted metric used for cell color.</p> <code>max_cols</code> <code>int</code> <p>The maximum amount of panels per row before wrapping.</p>"},{"location":"reference/trading/analysis/graphing/parallel_coordinates/","title":"Parallel Coordinates","text":""},{"location":"reference/trading/analysis/graphing/parallel_coordinates/#contangotradinganalysisgraphingparallel_coordinates","title":"<code>contango.trading.analysis.graphing.parallel_coordinates</code>","text":""},{"location":"reference/trading/analysis/graphing/parallel_coordinates/#contango.trading.analysis.graphing.parallel_coordinates","title":"contango.trading.analysis.graphing.parallel_coordinates","text":""},{"location":"reference/trading/analysis/graphing/parallel_coordinates/#contango.trading.analysis.graphing.parallel_coordinates.ParallelCoordinates","title":"ParallelCoordinates","text":"<pre><code>ParallelCoordinates(config: C)\n</code></pre> <p>               Bases: <code>GraphType[ParallelCoordinatesConfig]</code></p> <p>A graph type that shows parameter combinations across all swept parameters.</p> Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>def __init__(self, config: C) -&gt; None:\n    \"\"\"\n    Initializes `GraphType`.\n\n    Args:\n        config: The matching configuration for the graph type.\n    \"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/trading/analysis/graphing/parallel_coordinates/#contango.trading.analysis.graphing.parallel_coordinates.ParallelCoordinates.build","title":"build","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds the parallel coordinates plot across all swept parameters.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The experiment DataFrame.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>A plotly Figure.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\parallel_coordinates.py</code> <pre><code>def build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds the parallel coordinates plot across all swept parameters.\n\n    Args:\n        df: The experiment DataFrame.\n\n    Returns:\n        A plotly Figure.\n    \"\"\"\n    plot_df = df.dropna(subset=self.config.param_columns + [self.config.target_metric]).copy()\n\n    if self.config.sample_size is not None and len(plot_df) &gt; self.config.sample_size:\n        plot_df = plot_df.sample(self.config.sample_size, random_state=42)\n\n    dimensions = self.config.param_columns + [self.config.target_metric]\n\n    fig = px.parallel_coordinates(  # type: ignore[unknownMemberType]\n        plot_df,\n        dimensions=dimensions,\n        color=self.config.target_metric,\n        color_continuous_scale=px.colors.diverging.RdYlGn,\n        title=f\"Parameter Combinations: Parallel Coordinates colored by {self.config.target_metric}\",\n    )\n    fig.update_layout()  # type: ignore[unknownMemberType]\n    return fig\n</code></pre>"},{"location":"reference/trading/analysis/graphing/parallel_coordinates/#contango.trading.analysis.graphing.parallel_coordinates.ParallelCoordinatesConfig","title":"ParallelCoordinatesConfig  <code>dataclass</code>","text":"<pre><code>ParallelCoordinatesConfig(param_columns: list[str], target_metric: str = 'calmar_ratio', sample_size: int | None = None)\n</code></pre> <p>               Bases: <code>GraphConfig</code></p> <p>A graph configuration specific to the parallel coordinates graph.</p> <p>Attributes:</p> Name Type Description <code>param_columns</code> <code>list[str]</code> <p>The parameter columns to include as axes.</p> <code>target_metric</code> <code>str</code> <p>The name for the targeted metric used as the final axis            and the color scale.</p> <code>sample_size</code> <code>int | None</code> <p>The maximum amount of experiments to plot.</p>"},{"location":"reference/trading/analysis/graphing/parameter_importance/","title":"Parameter Importance","text":""},{"location":"reference/trading/analysis/graphing/parameter_importance/#contangotradinganalysisgraphingparameter_importance","title":"<code>contango.trading.analysis.graphing.parameter_importance</code>","text":""},{"location":"reference/trading/analysis/graphing/parameter_importance/#contango.trading.analysis.graphing.parameter_importance","title":"contango.trading.analysis.graphing.parameter_importance","text":""},{"location":"reference/trading/analysis/graphing/parameter_importance/#contango.trading.analysis.graphing.parameter_importance.ParameterImportance","title":"ParameterImportance","text":"<pre><code>ParameterImportance(config: C)\n</code></pre> <p>               Bases: <code>GraphType[ParameterImportanceConfig]</code></p> <p>A graph type that shows the sensitivity of a metric to each swept parameter.</p> Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>def __init__(self, config: C) -&gt; None:\n    \"\"\"\n    Initializes `GraphType`.\n\n    Args:\n        config: The matching configuration for the graph type.\n    \"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/trading/analysis/graphing/parameter_importance/#contango.trading.analysis.graphing.parameter_importance.ParameterImportance.build","title":"build","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds the parameter importance bar chart.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The tidy experiment DataFrame.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>A plotly Figure.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\parameter_importance.py</code> <pre><code>def build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds the parameter importance bar chart.\n\n    Args:\n        df: The tidy experiment DataFrame.\n\n    Returns:\n        A plotly Figure.\n    \"\"\"\n    importance_df = compute_parameter_importance(df, self.config.target_metric, self.config.param_columns)\n\n    fig = px.bar(   # type: ignore[unknownMemberType]\n        importance_df,\n        x=\"importance\",\n        y=\"parameter\",\n        orientation=\"h\",\n        title=f\"Parameter Importance: sensitivity of {self.config.target_metric} to each parameter\",\n        labels={\"importance\": \"Relative Importance (eta\u00b2)\", \"parameter\": \"Parameter\"},\n    )\n    fig.update_layout(yaxis={\"categoryorder\": \"total ascending\"})  # type: ignore[unknownMemberType]\n    return fig\n</code></pre>"},{"location":"reference/trading/analysis/graphing/parameter_importance/#contango.trading.analysis.graphing.parameter_importance.ParameterImportanceConfig","title":"ParameterImportanceConfig  <code>dataclass</code>","text":"<pre><code>ParameterImportanceConfig(param_columns: list[str], target_metric: str = 'calmar_ratio')\n</code></pre> <p>               Bases: <code>GraphConfig</code></p> <p>A graph configuration specific to the parameter importance graph.</p> <p>Attributes:</p> Name Type Description <code>param_columns</code> <code>list[str]</code> <p>The parameter columns to score.</p> <code>target_metric</code> <code>str</code> <p>The name for the targeted metric to explain.</p>"},{"location":"reference/trading/analysis/graphing/parameter_importance/#contango.trading.analysis.graphing.parameter_importance.compute_parameter_importance","title":"compute_parameter_importance","text":"<pre><code>compute_parameter_importance(df: DataFrame, target_metric: str, param_columns: list[str]) -&gt; pd.DataFrame\n</code></pre> <p>Scores each parameter's importance to target_metric using an eta-squared measure.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>Experiment DataFrame.</p> required <code>target_metric</code> <code>str</code> <p>The metric column to explain (e.g. \"calmar_ratio\").</p> required <code>param_columns</code> <code>list[str]</code> <p>Parameter columns to score.</p> required <p>Returns:</p> Type Description <code>DataFrame</code> <p>DataFrame with columns [\"parameter\", \"importance\"], sorted descending.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\parameter_importance.py</code> <pre><code>def compute_parameter_importance(\n    df: pd.DataFrame,\n    target_metric: str,\n    param_columns: list[str],\n) -&gt; pd.DataFrame:\n    \"\"\"\n    Scores each parameter's importance to target_metric using an\n    eta-squared measure.\n\n    Args:\n        df: Experiment DataFrame.\n        target_metric: The metric column to explain (e.g. \"calmar_ratio\").\n        param_columns: Parameter columns to score.\n\n    Returns:\n        DataFrame with columns [\"parameter\", \"importance\"], sorted descending.\n    \"\"\"\n    plot_df = df.dropna(subset=[target_metric])\n    grand_mean = plot_df[target_metric].mean()\n    ss_total = ((plot_df[target_metric] - grand_mean) ** 2).sum()\n\n    scores: list[dict[str, str | float]] = []\n    for param in param_columns:\n        group_stats = plot_df.groupby(param)[target_metric].agg([\"mean\", \"count\"])\n        ss_between = (group_stats[\"count\"] * (group_stats[\"mean\"] - grand_mean) ** 2).sum()\n        importance = float(ss_between / ss_total) if ss_total &gt; 0 else 0.0\n        scores.append({\"parameter\": param, \"importance\": importance})\n\n    return pd.DataFrame(scores).sort_values(\"importance\", ascending=False).reset_index(drop=True)\n</code></pre>"},{"location":"reference/trading/analysis/graphing/risk_return_overview/","title":"Risk Return Overview","text":""},{"location":"reference/trading/analysis/graphing/risk_return_overview/#contangotradinganalysisgraphingrisk_return_overview","title":"<code>contango.trading.analysis.graphing.risk_return_overview</code>","text":""},{"location":"reference/trading/analysis/graphing/risk_return_overview/#contango.trading.analysis.graphing.risk_return_overview","title":"contango.trading.analysis.graphing.risk_return_overview","text":""},{"location":"reference/trading/analysis/graphing/risk_return_overview/#contango.trading.analysis.graphing.risk_return_overview.RiskReturnOverview","title":"RiskReturnOverview","text":"<pre><code>RiskReturnOverview(config: C)\n</code></pre> <p>               Bases: <code>GraphType[RiskReturnOverviewConfig]</code></p> <p>A graph type that shows the risk/return overview scatter plot.</p> Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>def __init__(self, config: C) -&gt; None:\n    \"\"\"\n    Initializes `GraphType`.\n\n    Args:\n        config: The matching configuration for the graph type.\n    \"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/trading/analysis/graphing/risk_return_overview/#contango.trading.analysis.graphing.risk_return_overview.RiskReturnOverview.build","title":"build","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds the risk/return overview scatter plot.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The experiment DataFrame.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>A plotly Figure.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\risk_return_overview.py</code> <pre><code>def build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds the risk/return overview scatter plot.\n\n    Args:\n        df: The experiment DataFrame.\n\n    Returns:\n        A plotly Figure.\n    \"\"\"\n    plot_df = df.dropna(subset=[\"annual_return\", \"sharpe_ratio\", \"max_drawdown\", self.config.color_by]).copy()\n    plot_df[\"drawdown_severity\"] = plot_df[\"max_drawdown\"].abs()\n\n    is_numeric_color = pd.api.types.is_numeric_dtype(plot_df[self.config.color_by])\n\n    fig = px.scatter(   # type: ignore[unknownMemberType]\n        plot_df,\n        x=\"annual_return\",\n        y=\"sharpe_ratio\",\n        size=\"drawdown_severity\",\n        color=self.config.color_by,\n        hover_name=\"name\",\n        hover_data={\n            \"max_drawdown\": \":.2%\",\n            \"calmar_ratio\": \":.2f\",\n            \"trade_count\": True,\n            \"drawdown_severity\": False,\n        },\n        color_continuous_scale=\"Viridis\" if is_numeric_color else None,\n        title=\"Risk return overview: annual return vs. sharpe ratio, bubble size = drawdown severity\",\n        labels={\"annual_return\": \"Annual Return (CAGR)\", \"sharpe_ratio\": \"Sharpe Ratio\"},\n    )\n    fig.update_layout(xaxis_tickformat=\".1%\")  # type: ignore[unknownMemberType]\n    return fig\n</code></pre>"},{"location":"reference/trading/analysis/graphing/risk_return_overview/#contango.trading.analysis.graphing.risk_return_overview.RiskReturnOverviewConfig","title":"RiskReturnOverviewConfig  <code>dataclass</code>","text":"<pre><code>RiskReturnOverviewConfig(color_by: str = 'calmar_ratio')\n</code></pre> <p>               Bases: <code>GraphConfig</code></p> <p>A graph configuration specific to the risk/return overview graph.</p> <p>Attributes:</p> Name Type Description <code>color_by</code> <code>str</code> <p>The column to color points by.</p>"},{"location":"reference/trading/analysis/graphing/trade_quality_scatter/","title":"Trade Quality Scatter","text":""},{"location":"reference/trading/analysis/graphing/trade_quality_scatter/#contangotradinganalysisgraphingtrade_quality_scatter","title":"<code>contango.trading.analysis.graphing.trade_quality_scatter</code>","text":""},{"location":"reference/trading/analysis/graphing/trade_quality_scatter/#contango.trading.analysis.graphing.trade_quality_scatter","title":"contango.trading.analysis.graphing.trade_quality_scatter","text":""},{"location":"reference/trading/analysis/graphing/trade_quality_scatter/#contango.trading.analysis.graphing.trade_quality_scatter.TradeQualityScatter","title":"TradeQualityScatter","text":"<pre><code>TradeQualityScatter(config: C)\n</code></pre> <p>               Bases: <code>GraphType[TradeQualityScatterConfig]</code></p> <p>A graph type that shows the trade-quality scatter plot.</p> Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>def __init__(self, config: C) -&gt; None:\n    \"\"\"\n    Initializes `GraphType`.\n\n    Args:\n        config: The matching configuration for the graph type.\n    \"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/trading/analysis/graphing/trade_quality_scatter/#contango.trading.analysis.graphing.trade_quality_scatter.TradeQualityScatter.build","title":"build","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds the trade-quality scatter plot.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The experiment DataFrame.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>A plotly Figure.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\trade_quality_scatter.py</code> <pre><code>def build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds the trade-quality scatter plot.\n\n    Args:\n        df: The experiment DataFrame.\n\n    Returns:\n        A plotly Figure.\n    \"\"\"\n    plot_df = df.dropna(subset=[\"win_rate\", self.config.y_metric, \"trade_count\", self.config.color_by]).copy()\n\n    fig = px.scatter(   # type: ignore[unknownMemberType]\n        plot_df,\n        x=\"win_rate\",\n        y=self.config.y_metric,\n        size=\"trade_count\",\n        color=self.config.color_by,\n        hover_name=\"name\",\n        title=f\"Trade quality: win rate vs. {self.config.y_metric}, bubble size = trade count\",\n        labels={\"win_rate\": \"Win Rate\"},\n    )\n    fig.update_layout(xaxis_tickformat=\".0%\")  # type: ignore[unknownMemberType]\n    return fig\n</code></pre>"},{"location":"reference/trading/analysis/graphing/trade_quality_scatter/#contango.trading.analysis.graphing.trade_quality_scatter.TradeQualityScatterConfig","title":"TradeQualityScatterConfig  <code>dataclass</code>","text":"<pre><code>TradeQualityScatterConfig(y_metric: str = 'profit_factor', color_by: str = 'calmar_ratio')\n</code></pre> <p>               Bases: <code>GraphConfig</code></p> <p>A graph configuration specific to the trade quality scatter graph.</p> <p>Attributes:</p> Name Type Description <code>y_metric</code> <code>str</code> <p>The metric for the y-axis.</p> <code>color_by</code> <code>str</code> <p>The column to color points by, defaults to \"calmar_ratio\".</p>"},{"location":"reference/trading/analysis/graphing/underwater_drawdown/","title":"Underwater Drawdown","text":""},{"location":"reference/trading/analysis/graphing/underwater_drawdown/#contangotradinganalysisgraphingunderwater_drawdown","title":"<code>contango.trading.analysis.graphing.underwater_drawdown</code>","text":""},{"location":"reference/trading/analysis/graphing/underwater_drawdown/#contango.trading.analysis.graphing.underwater_drawdown","title":"contango.trading.analysis.graphing.underwater_drawdown","text":""},{"location":"reference/trading/analysis/graphing/underwater_drawdown/#contango.trading.analysis.graphing.underwater_drawdown.UnderwaterDrawdown","title":"UnderwaterDrawdown","text":"<pre><code>UnderwaterDrawdown(config: C)\n</code></pre> <p>               Bases: <code>GraphType[UnderwaterDrawdownConfig]</code></p> <p>A graph type that shows drawdown-from-peak over time for a shortlist of experiments.</p> Source code in <code>src\\contango\\trading\\analysis\\graph_type.py</code> <pre><code>def __init__(self, config: C) -&gt; None:\n    \"\"\"\n    Initializes `GraphType`.\n\n    Args:\n        config: The matching configuration for the graph type.\n    \"\"\"\n    self._config = config\n</code></pre>"},{"location":"reference/trading/analysis/graphing/underwater_drawdown/#contango.trading.analysis.graphing.underwater_drawdown.UnderwaterDrawdown.build","title":"build","text":"<pre><code>build(df: DataFrame) -&gt; go.Figure\n</code></pre> <p>Builds an underwater plot (drawdown-from-peak over time) for a shortlist of experiments.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>The experiment DataFrame.</p> required <p>Returns:</p> Type Description <code>Figure</code> <p>A plotly Figure.</p> Source code in <code>src\\contango\\trading\\analysis\\graphing\\underwater_drawdown.py</code> <pre><code>def build(self, df: pd.DataFrame) -&gt; go.Figure:\n    \"\"\"\n    Builds an underwater plot (drawdown-from-peak over time) for a shortlist\n    of experiments.\n\n    Args:\n        df: The experiment DataFrame.\n\n    Returns:\n        A plotly Figure.\n    \"\"\"\n    plot_df = df.dropna(subset=[\"equity_curve\"]).copy()\n\n    if self.config.experiment_ids is not None:\n        plot_df = plot_df[plot_df[\"experiment_id\"].isin(self.config.experiment_ids)]\n    else:\n        plot_df = (\n            plot_df.sort_values(self.config.rank_by, ascending=False)\n            .head(self.config.top_n)\n            .reset_index(drop=True)\n        )\n\n    fig = go.Figure()\n    for _, row in plot_df.iterrows():\n        timestamps, drawdowns = _compute_underwater_series(row[\"equity_curve\"], self.config.max_points_per_curve)\n        fig.add_trace(  # type: ignore[unknownMemberType]\n            go.Scatter(\n                x=timestamps,\n                y=drawdowns,\n                mode=\"lines\",\n                name=row[\"name\"],\n                fill=\"tozeroy\",\n            )\n        )\n\n    fig.update_layout(  # type: ignore[unknownMemberType]\n        title=\"Underwater plot: drawdown from peak over time\",\n        xaxis_title=\"Time\",\n        yaxis_title=\"Drawdown From Peak\",\n        yaxis_tickformat=\".1%\",\n    )\n    return fig\n</code></pre>"},{"location":"reference/trading/analysis/graphing/underwater_drawdown/#contango.trading.analysis.graphing.underwater_drawdown.UnderwaterDrawdownConfig","title":"UnderwaterDrawdownConfig  <code>dataclass</code>","text":"<pre><code>UnderwaterDrawdownConfig(experiment_ids: list[str] | None = None, top_n: int = 8, rank_by: str = 'calmar_ratio', max_points_per_curve: int = 1500)\n</code></pre> <p>               Bases: <code>GraphConfig</code></p> <p>A graph configuration specific to the underwater drawdown graph.</p> <p>Attributes:</p> Name Type Description <code>experiment_ids</code> <code>list[str] | None</code> <p>The list of experiment_id values to plot.</p> <code>top_n</code> <code>int</code> <p>The top amount of experiments to plot when auto-ranking.</p> <code>rank_by</code> <code>str</code> <p>The name for the targeted metric to rank by when auto-selecting      the shortlist.</p> <code>max_points_per_curve</code> <code>int</code> <p>The maximum amount of points kept per curve after                   downsampling.</p>"},{"location":"reference/trading/execution/backtest/config/","title":"Config","text":""},{"location":"reference/trading/execution/backtest/config/#contangotradingexecutionbacktestconfig","title":"<code>contango.trading.execution.backtest.config</code>","text":""},{"location":"reference/trading/execution/backtest/config/#contango.trading.execution.backtest.config","title":"contango.trading.execution.backtest.config","text":""},{"location":"reference/trading/execution/backtest/config/#contango.trading.execution.backtest.config.BacktesterConfig","title":"BacktesterConfig","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Holds the simulation settings for the backtester.</p> <p>Attributes:</p> Name Type Description <code>initial_cash</code> <code>USD</code> <p>Starting account equity in USD.</p> <code>initial_position</code> <code>units</code> <p>Starting position (amount of units).</p> <code>fill</code> <code>FillBehavior</code> <p>Execution timing behavior for simulated fills.</p> <code>slippage</code> <code>percent</code> <p>The percent difference beteween the expected price &amp; filled price.</p> <code>commission_per_unit</code> <code>USD</code> <p>The price taxed per unit bought.</p>"},{"location":"reference/trading/execution/backtest/config/#contango.trading.execution.backtest.config.FillBehavior","title":"FillBehavior","text":"<p>               Bases: <code>Enum</code></p> <p>Defines when simulated orders are considered filled.</p> <p>Attributes:</p> Name Type Description <code>INSTANT</code> <p>Orders will be filled instantly when made for the current bars close.</p>"},{"location":"reference/trading/execution/backtest/strategy_backtester/","title":"Strategy Backtester","text":""},{"location":"reference/trading/execution/backtest/strategy_backtester/#contangotradingexecutionbackteststrategy_backtester","title":"<code>contango.trading.execution.backtest.strategy_backtester</code>","text":""},{"location":"reference/trading/execution/backtest/strategy_backtester/#contango.trading.execution.backtest.strategy_backtester","title":"contango.trading.execution.backtest.strategy_backtester","text":""},{"location":"reference/trading/execution/backtest/strategy_backtester/#contango.trading.execution.backtest.strategy_backtester.StrategyBacktester","title":"StrategyBacktester","text":"<pre><code>StrategyBacktester(feed: MarketDataFeed, order_api: OrderAPI, order_filler: OrderFiller, stoploss_order_manager: StoplossOrderManager, portfolio: Portfolio, strategy: Strategy, strategy_injector: StrategyInjector, results_collector: ResultsCollector, event_bus: EventBus)\n</code></pre> <p>Backtester that coordinates a Strategy object over OHLCV data at a high level. Allows for iteration of data with historical data rather than a live feed. Subscribes all handlers to an <code>EventBus</code>, calls lifecycle methods for the provided <code>Strategy</code>, injects data into the <code>Strategy</code>, and returns a <code>ExecutionData</code> object for analysis.</p> <p>Initializes <code>StrategyBacktester</code> &amp; subscribes all event methods into the event bus.</p> <p>Parameters:</p> Name Type Description Default <code>feed</code> <code>MarketDataFeed</code> <p>The feed to derive <code>MarketDataEvent</code> instances from for every bar.</p> required <code>order_api</code> <code>OrderAPI</code> <p>Allows the user can make <code>OrderEvent</code> instances in the event bus.</p> required <code>order_filler</code> <code>OrderFiller</code> <p>Fills any <code>OrderEvent</code> instances and publishes them as filled events.</p> required <code>stoploss_order_manager</code> <code>StoplossOrderManager</code> <p>Publishes <code>OrderEvent</code> instances and handles checking stoplosses upon market events.</p> required <code>portfolio</code> <code>Portfolio</code> <p>Takes filled events, tracks positions &amp; money. Publishes <code>PortfolioSnapshotEvent</code> instances.</p> required <code>strategy</code> <code>Strategy</code> <p>The user's strategy with lifecycle hooks to test.</p> required <code>strategy_injector</code> <code>StrategyInjector</code> <p>Injects snapshot events into the strategy upon them being published.</p> required <code>results_collector</code> <code>ResultsCollector</code> <p>Collects all events &amp; computes results at the end of the backtest.</p> required <code>event_bus</code> <code>EventBus</code> <p>The tool that allows events to be published &amp; broadcasted to multiple modules at once.</p> required Source code in <code>src\\contango\\trading\\execution\\backtest\\strategy_backtester.py</code> <pre><code>def __init__(\n    self, \n    feed: MarketDataFeed, \n    order_api: OrderAPI,\n    order_filler: OrderFiller,\n    stoploss_order_manager: StoplossOrderManager,\n    portfolio: Portfolio,\n    strategy: Strategy,\n    strategy_injector: StrategyInjector,\n    results_collector: ResultsCollector,\n    event_bus: EventBus\n) -&gt; None:\n    \"\"\"\n    Initializes `StrategyBacktester` &amp; subscribes all event methods into the event bus.\n\n    Args:\n        feed: The feed to derive `MarketDataEvent` instances from for every bar.\n        order_api: Allows the user can make `OrderEvent` instances in the event bus.\n        order_filler: Fills any `OrderEvent` instances and publishes them as filled events.\n        stoploss_order_manager: Publishes `OrderEvent` instances and handles checking stoplosses upon market events.\n        portfolio: Takes filled events, tracks positions &amp; money. Publishes `PortfolioSnapshotEvent` instances.\n        strategy: The user's strategy with lifecycle hooks to test.\n        strategy_injector: Injects snapshot events into the strategy upon them being published.\n        results_collector: Collects all events &amp; computes results at the end of the backtest.\n        event_bus: The tool that allows events to be published &amp; broadcasted to multiple modules at once.\n    \"\"\"\n    self._feed = feed\n    self._strategy = strategy\n    self._results_collector = results_collector\n\n    event_bus.subscribe(MarketDataEvent, order_filler.collect_market_data, priority=4)\n    event_bus.subscribe(MarketDataEvent, stoploss_order_manager.check_if_stoploss_met, priority=3)\n    event_bus.subscribe(MarketDataEvent, portfolio.update_equity, priority=2)\n    event_bus.subscribe(MarketDataEvent, strategy.on_market_event, priority=1)\n    event_bus.subscribe(MarketDataEvent, results_collector.collect_market_data_event, priority=0)\n\n    event_bus.subscribe(OrderEvent, strategy.on_order_event, priority=2)\n    event_bus.subscribe(OrderEvent, order_filler.fill_order_event, priority=1)\n    event_bus.subscribe(OrderEvent, results_collector.collect_order_event, priority=0)\n\n    event_bus.subscribe(StoplossOrderEvent, stoploss_order_manager.collect_stoploss_order_event, priority=0)\n\n    event_bus.subscribe(AcceptedFillEvent, strategy.on_accepted_fill_event, priority=2)\n    event_bus.subscribe(AcceptedFillEvent, portfolio.apply_accepted_fill, priority=1)\n    event_bus.subscribe(AcceptedFillEvent, results_collector.collect_accepted_fill_event, priority=0)\n\n    event_bus.subscribe(RejectedFillEvent, strategy.on_rejected_fill_event, priority=1)\n    event_bus.subscribe(RejectedFillEvent, results_collector.collect_rejected_fill_event, priority=0)\n\n    event_bus.subscribe(PortfolioSnapshotEvent, order_filler.collect_portfolio_snapshot, priority=3)\n    event_bus.subscribe(PortfolioSnapshotEvent, stoploss_order_manager.collect_portfolio_snapshot, priority=2)\n    event_bus.subscribe(PortfolioSnapshotEvent, strategy_injector.inject_portfolio_event, priority=1)\n    event_bus.subscribe(PortfolioSnapshotEvent, results_collector.collect_portfolio_snapshot_event, priority=0)\n\n    initial_market_event = feed.get_initial_event()\n    self._initial_portfolio_snapshot_event = portfolio.get_initial_snapshot(initial_market_event)\n\n    # Inject the initial state of the fields in slots.\n    strategy.portfolio_snapshot = self._initial_portfolio_snapshot_event\n    strategy.order_api = order_api\n</code></pre>"},{"location":"reference/trading/execution/backtest/strategy_backtester/#contango.trading.execution.backtest.strategy_backtester.StrategyBacktester.backtest","title":"backtest  <code>classmethod</code>","text":"<pre><code>backtest(ohlcv_data: list[MarketDataEvent], strategy: Strategy, config: BacktesterConfig) -&gt; ExecutionData\n</code></pre> <p>Runs a full backtest for a single ticker &amp; returns the data generated from the backtest. The provided data is validated for every backtest, &amp; will raise if in the incorrect format.</p> <p>Parameters:</p> Name Type Description Default <code>ohlcv_data</code> <code>list[MarketDataEvent]</code> <p>The bar data for the backtest.</p> required <code>strategy</code> <code>Strategy</code> <p>The strategy subclass for the backtest (determining when to trade).</p> required <code>config</code> <code>BacktesterConfig</code> <p>The configuration for the backtest.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If schema is invalid or data types cannot be derived from the provided dataframe.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\strategy_backtester.py</code> <pre><code>@classmethod\ndef backtest(\n    cls,\n    ohlcv_data: list[MarketDataEvent],\n    strategy: Strategy,\n    config: BacktesterConfig\n) -&gt; ExecutionData:\n    \"\"\"\n    Runs a full backtest for a single ticker &amp; returns the data generated from the backtest.\n    The provided data is validated for every backtest, &amp; will raise if in the incorrect format.\n\n    Args:\n        ohlcv_data: The bar data for the backtest.\n        strategy: The strategy subclass for the backtest (determining when to trade).\n        config: The configuration for the backtest.\n\n    Raises:\n        ValueError: If schema is invalid or data types cannot be derived from the provided dataframe.\n    \"\"\"\n    bus = EventBus()\n    backtester = StrategyBacktester(\n        feed=MarketDataFeed(ohlcv_data, bus),\n        order_api=OrderAPI(bus),\n        order_filler=OrderFiller(config, bus),\n        stoploss_order_manager=StoplossOrderManager(bus),\n        portfolio=Portfolio(bus, config, config.initial_cash, config.initial_position),\n        strategy=strategy,\n        strategy_injector=StrategyInjector(strategy),\n        results_collector=ResultsCollector(),\n        event_bus=bus\n    )\n    return backtester._run()\n</code></pre>"},{"location":"reference/trading/execution/backtest/fill_behavior/fill_policy/","title":"Fill Policy","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/fill_policy/#contangotradingexecutionbacktestfill_behaviorfill_policy","title":"<code>contango.trading.execution.backtest.fill_behavior.fill_policy</code>","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/fill_policy/#contango.trading.execution.backtest.fill_behavior.fill_policy","title":"contango.trading.execution.backtest.fill_behavior.fill_policy","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/partial_position_rule/","title":"Partial Position Rule","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/partial_position_rule/#contangotradingexecutionbacktestfill_behaviorrulespartial_position_rule","title":"<code>contango.trading.execution.backtest.fill_behavior.rules.partial_position_rule</code>","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/partial_position_rule/#contango.trading.execution.backtest.fill_behavior.rules.partial_position_rule","title":"contango.trading.execution.backtest.fill_behavior.rules.partial_position_rule","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/partial_position_rule/#contango.trading.execution.backtest.fill_behavior.rules.partial_position_rule.PartialPositionRule","title":"PartialPositionRule","text":"<p>               Bases: <code>FillRule[RuleContext]</code></p> <p>Rule that only allows full trades (no partial trades). This means that you must sell all of the shares you bought in a previous order every time.</p>"},{"location":"reference/trading/execution/backtest/fill_behavior/rules/partial_position_rule/#contango.trading.execution.backtest.fill_behavior.rules.partial_position_rule.PartialPositionRule.validate","title":"validate","text":"<pre><code>validate(context: RuleContext) -&gt; bool\n</code></pre> <p>Returns True if there is currently a position &amp; the desired quantity is negative, and adding the  current posiiton and desired quantity correctly results in no position.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\fill_behavior\\rules\\partial_position_rule.py</code> <pre><code>def validate(self, context: RuleContext) -&gt; bool:\n    \"\"\"\n    Returns True if there is currently a position &amp; the desired quantity is negative, and adding the \n    current posiiton and desired quantity correctly results in no position.\n    \"\"\"\n    if context.desired_quantity &gt;= 0:\n        return True\n\n    return (\n        context.current_position &gt; 0\n        and context.current_position + context.desired_quantity == 0\n    )\n</code></pre>"},{"location":"reference/trading/execution/backtest/fill_behavior/rules/pyramiding_rule/","title":"Pyramiding Rule","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/pyramiding_rule/#contangotradingexecutionbacktestfill_behaviorrulespyramiding_rule","title":"<code>contango.trading.execution.backtest.fill_behavior.rules.pyramiding_rule</code>","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/pyramiding_rule/#contango.trading.execution.backtest.fill_behavior.rules.pyramiding_rule","title":"contango.trading.execution.backtest.fill_behavior.rules.pyramiding_rule","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/pyramiding_rule/#contango.trading.execution.backtest.fill_behavior.rules.pyramiding_rule.PyramidingRule","title":"PyramidingRule","text":"<p>               Bases: <code>FillRule[RuleContext]</code></p> <p>Rule that does not allow two subsequent positive orders.</p>"},{"location":"reference/trading/execution/backtest/fill_behavior/rules/pyramiding_rule/#contango.trading.execution.backtest.fill_behavior.rules.pyramiding_rule.PyramidingRule.validate","title":"validate","text":"<pre><code>validate(context: RuleContext) -&gt; bool\n</code></pre> <p>Returns True upon correctly selling shares when the last order was a buy.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\fill_behavior\\rules\\pyramiding_rule.py</code> <pre><code>def validate(self, context: RuleContext) -&gt; bool:\n    \"\"\"\n    Returns True upon correctly selling shares when the last order was a buy.\n    \"\"\"\n    return not (\n        context.current_position &gt; 0\n        and context.desired_quantity &gt; 0\n    )\n</code></pre>"},{"location":"reference/trading/execution/backtest/fill_behavior/rules/rule_context/","title":"Rule Context","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/rule_context/#contangotradingexecutionbacktestfill_behaviorrulesrule_context","title":"<code>contango.trading.execution.backtest.fill_behavior.rules.rule_context</code>","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/rule_context/#contango.trading.execution.backtest.fill_behavior.rules.rule_context","title":"contango.trading.execution.backtest.fill_behavior.rules.rule_context","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/rule_context/#contango.trading.execution.backtest.fill_behavior.rules.rule_context.RuleContext","title":"RuleContext","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The context for all rules.</p> <p>Attributes:</p> Name Type Description <code>desired_quantity</code> <code>int</code> <p>The desired quantity of units for the order.</p> <code>current_position</code> <code>int</code> <p>The current position in units for the order.</p> <code>total_cost</code> <code>float</code> <p>The total cost for the order.</p> <code>current_cash</code> <code>float</code> <p>The current cash available in the portfolio.</p>"},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_cash_rule/","title":"Sufficient Cash Rule","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_cash_rule/#contangotradingexecutionbacktestfill_behaviorrulessufficient_cash_rule","title":"<code>contango.trading.execution.backtest.fill_behavior.rules.sufficient_cash_rule</code>","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_cash_rule/#contango.trading.execution.backtest.fill_behavior.rules.sufficient_cash_rule","title":"contango.trading.execution.backtest.fill_behavior.rules.sufficient_cash_rule","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_cash_rule/#contango.trading.execution.backtest.fill_behavior.rules.sufficient_cash_rule.SufficientCashRule","title":"SufficientCashRule","text":"<p>               Bases: <code>FillRule[RuleContext]</code></p> <p>Rule that only allows trades with sufficient cash.</p>"},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_cash_rule/#contango.trading.execution.backtest.fill_behavior.rules.sufficient_cash_rule.SufficientCashRule.validate","title":"validate","text":"<pre><code>validate(context: RuleContext) -&gt; bool\n</code></pre> <p>Returns True if the desired quantity does not result in the position becoming negative.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\fill_behavior\\rules\\sufficient_cash_rule.py</code> <pre><code>def validate(self, context: RuleContext) -&gt; bool:\n    \"\"\"\n    Returns True if the desired quantity does not result in the position becoming negative.\n    \"\"\"\n    return context.total_cost &lt;= context.current_cash\n</code></pre>"},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_position_rule/","title":"Sufficient Position Rule","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_position_rule/#contangotradingexecutionbacktestfill_behaviorrulessufficient_position_rule","title":"<code>contango.trading.execution.backtest.fill_behavior.rules.sufficient_position_rule</code>","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_position_rule/#contango.trading.execution.backtest.fill_behavior.rules.sufficient_position_rule","title":"contango.trading.execution.backtest.fill_behavior.rules.sufficient_position_rule","text":""},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_position_rule/#contango.trading.execution.backtest.fill_behavior.rules.sufficient_position_rule.SufficientPositionRule","title":"SufficientPositionRule","text":"<p>               Bases: <code>FillRule[RuleContext]</code></p> <p>Rule that only allows a sufficient position, disallowing selling more position than available.</p>"},{"location":"reference/trading/execution/backtest/fill_behavior/rules/sufficient_position_rule/#contango.trading.execution.backtest.fill_behavior.rules.sufficient_position_rule.SufficientPositionRule.validate","title":"validate","text":"<pre><code>validate(context: RuleContext) -&gt; bool\n</code></pre> <p>Returns True if the desired quantity does not result in the position becoming negative.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\fill_behavior\\rules\\sufficient_position_rule.py</code> <pre><code>def validate(self, context: RuleContext) -&gt; bool:\n    \"\"\"\n    Returns True if the desired quantity does not result in the position becoming negative.\n    \"\"\"\n    return (context.desired_quantity + context.current_position) &gt;= 0\n</code></pre>"},{"location":"reference/trading/execution/backtest/market/feed/","title":"Feed","text":""},{"location":"reference/trading/execution/backtest/market/feed/#contangotradingexecutionbacktestmarketfeed","title":"<code>contango.trading.execution.backtest.market.feed</code>","text":""},{"location":"reference/trading/execution/backtest/market/feed/#contango.trading.execution.backtest.market.feed","title":"contango.trading.execution.backtest.market.feed","text":""},{"location":"reference/trading/execution/backtest/market/feed/#contango.trading.execution.backtest.market.feed.MarketDataFeed","title":"MarketDataFeed","text":"<pre><code>MarketDataFeed(data: list[MarketDataEvent], event_bus: EventBus)\n</code></pre> <p>Allows an OHLCV dataframe to be sequentially iterated over and published.</p> <p>Initializes <code>MarketDataFeed</code>.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>list[MarketDataEvent]</code> <p>The OHLCV list of <code>MarketDataEvent</code> to iterate over.</p> required <code>event_bus</code> <code>EventBus</code> <p>The event bus to publish <code>MarketDataEvent</code> instances to.</p> required Source code in <code>src\\contango\\trading\\execution\\backtest\\market\\feed.py</code> <pre><code>def __init__(self, data: list[MarketDataEvent], event_bus: EventBus):\n    \"\"\"\n    Initializes `MarketDataFeed`.\n\n    Args:\n        data: The OHLCV list of `MarketDataEvent` to iterate over.\n        event_bus: The event bus to publish `MarketDataEvent` instances to.\n    \"\"\"\n    self._data = data\n    self._event_bus = event_bus\n</code></pre>"},{"location":"reference/trading/execution/backtest/market/feed/#contango.trading.execution.backtest.market.feed.MarketDataFeed.get_initial_event","title":"get_initial_event","text":"<pre><code>get_initial_event() -&gt; MarketDataEvent\n</code></pre> <p>Returns the first <code>MarketDataEvent</code> in the OHLCV data.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\market\\feed.py</code> <pre><code>def get_initial_event(self) -&gt; MarketDataEvent:\n    \"\"\"\n    Returns the first `MarketDataEvent` in the OHLCV data.\n    \"\"\"\n    return self._data[0]\n</code></pre>"},{"location":"reference/trading/execution/backtest/market/feed/#contango.trading.execution.backtest.market.feed.MarketDataFeed.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Iterates through the provided data &amp; publishes 'MarketDataEvent` instances for each bar.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\market\\feed.py</code> <pre><code>def run(self) -&gt; None:\n    \"\"\"\n    Iterates through the provided data &amp; publishes 'MarketDataEvent` instances for each bar.\n    \"\"\"\n    for event in self._data:\n        self._event_bus.publish(event)\n</code></pre>"},{"location":"reference/trading/execution/backtest/orders/order_filler/","title":"Order Filler","text":""},{"location":"reference/trading/execution/backtest/orders/order_filler/#contangotradingexecutionbacktestordersorder_filler","title":"<code>contango.trading.execution.backtest.orders.order_filler</code>","text":""},{"location":"reference/trading/execution/backtest/orders/order_filler/#contango.trading.execution.backtest.orders.order_filler","title":"contango.trading.execution.backtest.orders.order_filler","text":""},{"location":"reference/trading/execution/backtest/orders/order_filler/#contango.trading.execution.backtest.orders.order_filler.OrderFiller","title":"OrderFiller","text":"<pre><code>OrderFiller(config: BacktesterConfig, event_bus: EventBus)\n</code></pre> <p>Fills all orders made by accepting <code>OrderEvent</code> instances and publishing filled events.</p> <p>Initializes <code>ExecutionEngine</code>.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>BacktesterConfig</code> <p>The <code>BacktesterConfig</code> to determine fill behavior from.</p> required <code>event_bus</code> <code>EventBus</code> <p>The event bus to publish filled events to.</p> required Source code in <code>src\\contango\\trading\\execution\\backtest\\orders\\order_filler.py</code> <pre><code>def __init__(\n    self,\n    config: BacktesterConfig,\n    event_bus: EventBus\n) -&gt; None:\n    \"\"\"\n    Initializes `ExecutionEngine`.\n\n    Args:\n        config: The `BacktesterConfig` to determine fill behavior from.\n        event_bus: The event bus to publish filled events to.\n    \"\"\"\n    self._config = config\n    self._event_bus = event_bus\n\n    self._last_market_event: MarketDataEvent | None = None\n    self._last_portfolio_snapshot: PortfolioSnapshotEvent | None = None\n</code></pre>"},{"location":"reference/trading/execution/backtest/orders/order_filler/#contango.trading.execution.backtest.orders.order_filler.OrderFiller.collect_market_data","title":"collect_market_data","text":"<pre><code>collect_market_data(market_data: MarketDataEvent) -&gt; None\n</code></pre> <p>Collects &amp; stores the current market event upon the event being published.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\orders\\order_filler.py</code> <pre><code>def collect_market_data(self, market_data: MarketDataEvent) -&gt; None:\n    \"\"\"\n    Collects &amp; stores the current market event upon the event being published.\n    \"\"\"\n    self._last_market_event = market_data\n</code></pre>"},{"location":"reference/trading/execution/backtest/orders/order_filler/#contango.trading.execution.backtest.orders.order_filler.OrderFiller.collect_portfolio_snapshot","title":"collect_portfolio_snapshot","text":"<pre><code>collect_portfolio_snapshot(event: PortfolioSnapshotEvent) -&gt; None\n</code></pre> <p>Collects &amp; stores the current portfolio snapshot upon the event being published.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\orders\\order_filler.py</code> <pre><code>def collect_portfolio_snapshot(self, event: PortfolioSnapshotEvent) -&gt; None:\n    \"\"\"\n    Collects &amp; stores the current portfolio snapshot upon the event being published.\n    \"\"\"\n    self._last_portfolio_snapshot = event\n</code></pre>"},{"location":"reference/trading/execution/backtest/orders/order_filler/#contango.trading.execution.backtest.orders.order_filler.OrderFiller.fill_order_event","title":"fill_order_event","text":"<pre><code>fill_order_event(order_event: OrderEvent) -&gt; None\n</code></pre> <p>Publishes an accepted or rejected filled event order whenever an <code>OrderEvent</code> is created.</p> <p>Parameters:</p> Name Type Description Default <code>order_event</code> <code>OrderEvent</code> <p>The order event to process &amp; create a <code>FillOrder</code> for.</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Upon an order being made before required events being injected into the <code>OrderFiller</code>.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\orders\\order_filler.py</code> <pre><code>def fill_order_event(self, order_event: OrderEvent) -&gt; None:\n    \"\"\"\n    Publishes an accepted or rejected filled event order whenever an `OrderEvent` is created.\n\n    Args:\n        order_event: The order event to process &amp; create a `FillOrder` for.\n\n    Raises:\n        RuntimeError: Upon an order being made before required events being injected into the `OrderFiller`.\n    \"\"\"\n    market_event = self._last_market_event\n    portfolio_snapshot = self._last_portfolio_snapshot\n\n    if market_event is None:\n        raise RuntimeError(\"Tried to execute orders before market event was registered in the order filler.\")\n    if portfolio_snapshot is None:\n        raise RuntimeError(\"Tried to execute orders before a portfolio snapshot was registered in the order filler.\")\n\n    fill_price = self._resolve_fill_price(market_event)\n    price_with_slippage = self._apply_slippage(order_event, fill_price)\n    commission = self._calculate_commission(order_event)\n\n    order_cost = price_with_slippage * order_event.quantity\n    total_cost = order_cost + commission\n\n    validation_result = BACKTESTER_FILL_POLICY.validate_rules(RuleContext(\n        desired_quantity=order_event.quantity,\n        current_position=portfolio_snapshot.position,\n        total_cost=total_cost,\n        current_cash=portfolio_snapshot.cash\n    ))\n\n    if validation_result is not None:\n        fill_event = RejectedFillEvent(\n            timestamp=market_event.timestamp,\n            market_event=market_event,\n            order_event=order_event,\n            reason=validation_result\n        )\n    else:\n        fill_event = AcceptedFillEvent(\n            timestamp=market_event.timestamp,\n            market_event=market_event,\n            order_event=order_event,\n            fill_price=price_with_slippage,\n            total_cost=total_cost\n        )\n\n    self._event_bus.publish(fill_event)\n</code></pre>"},{"location":"reference/trading/execution/backtest/orders/stoploss_order_manager/","title":"Stoploss Order Manager","text":""},{"location":"reference/trading/execution/backtest/orders/stoploss_order_manager/#contangotradingexecutionbacktestordersstoploss_order_manager","title":"<code>contango.trading.execution.backtest.orders.stoploss_order_manager</code>","text":""},{"location":"reference/trading/execution/backtest/orders/stoploss_order_manager/#contango.trading.execution.backtest.orders.stoploss_order_manager","title":"contango.trading.execution.backtest.orders.stoploss_order_manager","text":""},{"location":"reference/trading/execution/backtest/orders/stoploss_order_manager/#contango.trading.execution.backtest.orders.stoploss_order_manager.StoplossOrderManager","title":"StoplossOrderManager","text":"<pre><code>StoplossOrderManager(event_bus: EventBus)\n</code></pre> <p>Holds pending stoploss order events &amp; publishes order events if the stoploss is met. If the stoploss event is exited manually (all shares are sold), the stoploss has no effect. Assumes only one ticker to be traded &amp; no pyramiding.</p> <p>Initializes <code>StoplossOrderManager</code>.</p> <p>Parameters:</p> Name Type Description Default <code>event_bus</code> <code>EventBus</code> <p>The event bus to publish order events to upon the stoploss price being met.</p> required Source code in <code>src\\contango\\trading\\execution\\backtest\\orders\\stoploss_order_manager.py</code> <pre><code>def __init__(self, event_bus: EventBus) -&gt; None:\n    \"\"\"\n    Initializes `StoplossOrderManager`.\n\n    Args:\n        event_bus: The event bus to publish order events to upon the stoploss price being met.\n    \"\"\"\n    self._event_bus = event_bus\n\n    self._pending_stoploss_order: StoplossOrderEvent | None = None\n    self._current_portfolio_snapshot: PortfolioSnapshotEvent | None = None\n    self._previous_portfolio_snapshot: PortfolioSnapshotEvent | None = None\n</code></pre>"},{"location":"reference/trading/execution/backtest/orders/stoploss_order_manager/#contango.trading.execution.backtest.orders.stoploss_order_manager.StoplossOrderManager.check_if_stoploss_met","title":"check_if_stoploss_met","text":"<pre><code>check_if_stoploss_met(market_data: MarketDataEvent) -&gt; None\n</code></pre> <p>Executes the pending stoploss order when the market bar breaches the stop price. Publishes an <code>OrderEvent</code> upon the stoploss being reached.</p> <p>Parameters:</p> Name Type Description Default <code>market_data</code> <code>MarketDataEvent</code> <p>The current market data event (bar) for the OHLCV data.</p> required Source code in <code>src\\contango\\trading\\execution\\backtest\\orders\\stoploss_order_manager.py</code> <pre><code>def check_if_stoploss_met(self, market_data: MarketDataEvent) -&gt; None:\n    \"\"\"\n    Executes the pending stoploss order when the market bar breaches the stop price.\n    Publishes an `OrderEvent` upon the stoploss being reached.\n\n    Args:\n        market_data: The current market data event (bar) for the OHLCV data.\n    \"\"\"\n    stoploss_order = self._pending_stoploss_order\n    previous_portfolio = self._previous_portfolio_snapshot\n    current_portfolio = self._current_portfolio_snapshot\n\n    if previous_portfolio is None or current_portfolio is None:\n        return\n\n    # If the position is sold off in the strategy elsewhere\n    if previous_portfolio.position != 0 and current_portfolio.position == 0:\n        self._pending_stoploss_order = None\n        return\n\n    if stoploss_order is None:\n        return\n\n    if market_data.close &lt; stoploss_order.stop_price:\n        quantity_to_sell = -stoploss_order.quantity\n        order = OrderEvent(\n            timestamp=market_data.timestamp,\n            quantity=quantity_to_sell,\n            reason=\"Stoploss was met.\"\n        )\n\n        self._event_bus.publish(order)\n        self._pending_stoploss_order = None\n</code></pre>"},{"location":"reference/trading/execution/backtest/orders/stoploss_order_manager/#contango.trading.execution.backtest.orders.stoploss_order_manager.StoplossOrderManager.collect_portfolio_snapshot","title":"collect_portfolio_snapshot","text":"<pre><code>collect_portfolio_snapshot(snapshot: PortfolioSnapshotEvent) -&gt; None\n</code></pre> <p>Sets the latest &amp; previous portfolio snapshots so the manager can tell whether the stop is still relevant.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\orders\\stoploss_order_manager.py</code> <pre><code>def collect_portfolio_snapshot(self, snapshot: PortfolioSnapshotEvent) -&gt; None:\n    \"\"\"\n    Sets the latest &amp; previous portfolio snapshots so the manager can tell whether the stop is still relevant.\n    \"\"\"\n    self._previous_portfolio_snapshot = self._current_portfolio_snapshot\n    self._current_portfolio_snapshot = snapshot\n</code></pre>"},{"location":"reference/trading/execution/backtest/orders/stoploss_order_manager/#contango.trading.execution.backtest.orders.stoploss_order_manager.StoplossOrderManager.collect_stoploss_order_event","title":"collect_stoploss_order_event","text":"<pre><code>collect_stoploss_order_event(stoploss_order: StoplossOrderEvent) -&gt; None\n</code></pre> <p>Stores the latest pending stoploss order.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\orders\\stoploss_order_manager.py</code> <pre><code>def collect_stoploss_order_event(self, stoploss_order: StoplossOrderEvent) -&gt; None:\n    \"\"\"\n    Stores the latest pending stoploss order.\n    \"\"\"\n    self._pending_stoploss_order = stoploss_order\n</code></pre>"},{"location":"reference/trading/execution/backtest/portfolio/portfolio/","title":"Portfolio","text":""},{"location":"reference/trading/execution/backtest/portfolio/portfolio/#contangotradingexecutionbacktestportfolioportfolio","title":"<code>contango.trading.execution.backtest.portfolio.portfolio</code>","text":""},{"location":"reference/trading/execution/backtest/portfolio/portfolio/#contango.trading.execution.backtest.portfolio.portfolio","title":"contango.trading.execution.backtest.portfolio.portfolio","text":""},{"location":"reference/trading/execution/backtest/portfolio/portfolio/#contango.trading.execution.backtest.portfolio.portfolio.Portfolio","title":"Portfolio","text":"<pre><code>Portfolio(event_bus: EventBus, config: BacktesterConfig, initial_cash: USD = 0.0, initial_position: units = 0)\n</code></pre> <p>Tracks cash, position, and equity during backtesting for one symbol.</p> <p>Initializes <code>Portfolio</code>.</p> <p>Parameters:</p> Name Type Description Default <code>event_bus</code> <code>EventBus</code> <p>The event bus to publish <code>PortfolioSnapshotEvent</code> instances to.</p> required <code>config</code> <code>BacktesterConfig</code> <p>The backtester config to derive slippage &amp; commission from.</p> required <code>initial_cash</code> <code>USD</code> <p>The initial cash (USD) to be stored into the portfolio.</p> <code>0.0</code> <code>initial_position</code> <code>units</code> <p>The initial position (units) to be stored into the portfolio.</p> <code>0</code> Source code in <code>src\\contango\\trading\\execution\\backtest\\portfolio\\portfolio.py</code> <pre><code>def __init__(\n    self, \n    event_bus: EventBus, \n    config: BacktesterConfig,\n    initial_cash: USD = 0.0, \n    initial_position: units = 0\n) -&gt; None:\n    \"\"\"\n    Initializes `Portfolio`.\n\n    Args:\n        event_bus: The event bus to publish `PortfolioSnapshotEvent` instances to.\n        config: The backtester config to derive slippage &amp; commission from.\n        initial_cash: The initial cash (USD) to be stored into the portfolio.\n        initial_position: The initial position (units) to be stored into the portfolio.\n    \"\"\"\n    self._cash = initial_cash\n    self._config = config\n    self._position = initial_position\n\n    self._event_bus = event_bus\n</code></pre>"},{"location":"reference/trading/execution/backtest/portfolio/portfolio/#contango.trading.execution.backtest.portfolio.portfolio.Portfolio.apply_accepted_fill","title":"apply_accepted_fill","text":"<pre><code>apply_accepted_fill(trade: AcceptedFillEvent) -&gt; None\n</code></pre> <p>Applies an <code>AcceptedFillEvent</code> to the internal portfolio &amp; publishes a <code>PortfolioSnapshotEvent</code> for external access.</p> <p>Parameters:</p> Name Type Description Default <code>trade</code> <code>AcceptedFillEvent</code> <p>The <code>AcceptedFillEvent</code> to be processed into the portfolio.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>Upon the portfolio cash going below 0 USD (should never happen).</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\portfolio\\portfolio.py</code> <pre><code>def apply_accepted_fill(self, trade: AcceptedFillEvent) -&gt; None:\n    \"\"\"\n    Applies an `AcceptedFillEvent` to the internal portfolio &amp; publishes a `PortfolioSnapshotEvent` for external access.\n\n    Args:\n        trade: The `AcceptedFillEvent` to be processed into the portfolio.\n\n    Raises:\n        ValueError: Upon the portfolio cash going below 0 USD (should never happen).\n    \"\"\"\n    self._cash -= trade.total_cost\n\n    if self._cash &lt; 0:\n        raise ValueError(\"Portfolio cash incorrectly reached below 0 USD.\")\n\n    self._position += trade.order_event.quantity\n\n    snapshot = PortfolioSnapshotEvent(\n        timestamp=trade.timestamp,\n        cash=self._cash,\n        position=self._position,\n        equity=self._get_equity(trade.market_event.close)\n    )\n    self._event_bus.publish(snapshot)\n</code></pre>"},{"location":"reference/trading/execution/backtest/portfolio/portfolio/#contango.trading.execution.backtest.portfolio.portfolio.Portfolio.get_initial_snapshot","title":"get_initial_snapshot","text":"<pre><code>get_initial_snapshot(initial_market_event: MarketDataEvent) -&gt; PortfolioSnapshotEvent\n</code></pre> <p>Creates an initial portfolio snapshot based on the current cash, units, &amp; equity. The initial snapshot will derive the timestamp from the first market event.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\portfolio\\portfolio.py</code> <pre><code>def get_initial_snapshot(self, initial_market_event: MarketDataEvent) -&gt; PortfolioSnapshotEvent:\n    \"\"\"\n    Creates an initial portfolio snapshot based on the current cash, units, &amp; equity.\n    The initial snapshot will derive the timestamp from the first market event.\n    \"\"\"\n    return PortfolioSnapshotEvent(\n        timestamp=initial_market_event.timestamp,\n        cash=self._cash,\n        position=self._position,\n        equity=self._get_equity(initial_market_event.close)\n    )\n</code></pre>"},{"location":"reference/trading/execution/backtest/portfolio/portfolio/#contango.trading.execution.backtest.portfolio.portfolio.Portfolio.update_equity","title":"update_equity","text":"<pre><code>update_equity(market_data: MarketDataEvent) -&gt; None\n</code></pre> <p>Updates the equity for the portfolio with a <code>MarketDataEvent</code> &amp; publishes a new <code>PortfolioSnapshotEvent</code>.</p> <p>Parameters:</p> Name Type Description Default <code>market_data</code> <code>MarketDataEvent</code> <p>The <code>MarketDataEvent</code> to be processed into the equity.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>Upon the portfolio cash going below 0 USD.</p> Source code in <code>src\\contango\\trading\\execution\\backtest\\portfolio\\portfolio.py</code> <pre><code>def update_equity(self, market_data: MarketDataEvent) -&gt; None:\n    \"\"\"\n    Updates the equity for the portfolio with a `MarketDataEvent` &amp; publishes a new `PortfolioSnapshotEvent`.\n\n    Args:\n        market_data: The `MarketDataEvent` to be processed into the equity.\n\n    Raises:\n        ValueError: Upon the portfolio cash going below 0 USD.\n    \"\"\"\n    snapshot = PortfolioSnapshotEvent(\n        timestamp=market_data.timestamp,\n        cash=self._cash,\n        position=self._position,\n        equity=self._get_equity(market_data.close)\n    )\n    self._event_bus.publish(snapshot)\n</code></pre>"},{"location":"reference/trading/execution/engine/strategy_injector/","title":"Strategy Injector","text":""},{"location":"reference/trading/execution/engine/strategy_injector/#contangotradingexecutionenginestrategy_injector","title":"<code>contango.trading.execution.engine.strategy_injector</code>","text":""},{"location":"reference/trading/execution/engine/strategy_injector/#contango.trading.execution.engine.strategy_injector","title":"contango.trading.execution.engine.strategy_injector","text":""},{"location":"reference/trading/execution/engine/strategy_injector/#contango.trading.execution.engine.strategy_injector.StrategyInjector","title":"StrategyInjector","text":"<pre><code>StrategyInjector(strategy: Strategy)\n</code></pre> <p>Injects snapshot events into the strategy upon them being updated.</p> <p>Initializes <code>StrategyInjector</code>.</p> <p>Parameters:</p> Name Type Description Default <code>strategy</code> <code>Strategy</code> <p>The underlying <code>Strategy</code> instance to inject to.</p> required Source code in <code>src\\contango\\trading\\execution\\engine\\strategy_injector.py</code> <pre><code>def __init__(self, strategy: Strategy) -&gt; None:\n    \"\"\"\n    Initializes `StrategyInjector`.\n\n    Args:\n        strategy: The underlying `Strategy` instance to inject to.\n    \"\"\"\n    self._strategy = strategy\n</code></pre>"},{"location":"reference/trading/execution/engine/strategy_injector/#contango.trading.execution.engine.strategy_injector.StrategyInjector.inject_portfolio_event","title":"inject_portfolio_event","text":"<pre><code>inject_portfolio_event(event: PortfolioSnapshotEvent) -&gt; None\n</code></pre> <p>Injects a <code>PortfolioSnapshotEvent</code> into the strategy upon publication.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\strategy_injector.py</code> <pre><code>def inject_portfolio_event(self, event: PortfolioSnapshotEvent) -&gt; None:\n    \"\"\"\n    Injects a `PortfolioSnapshotEvent` into the strategy upon publication.\n    \"\"\"\n    self._strategy.portfolio_snapshot = event\n</code></pre>"},{"location":"reference/trading/execution/engine/events/event_bus/","title":"Event Bus","text":""},{"location":"reference/trading/execution/engine/events/event_bus/#contangotradingexecutionengineeventsevent_bus","title":"<code>contango.trading.execution.engine.events.event_bus</code>","text":""},{"location":"reference/trading/execution/engine/events/event_bus/#contango.trading.execution.engine.events.event_bus","title":"contango.trading.execution.engine.events.event_bus","text":""},{"location":"reference/trading/execution/engine/events/event_bus/#contango.trading.execution.engine.events.event_bus.EventBus","title":"EventBus","text":"<pre><code>EventBus()\n</code></pre> <p>Allows for methods to subscribe whenever a specific event is published.</p> <p>Initializes <code>EventBus</code> with empty subscriptions.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\events\\event_bus.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"\n    Initializes `EventBus` with empty subscriptions.\n    \"\"\"\n    self._handlers: dict[type, list[_handler_type]] = defaultdict(list)\n    self._priorities: dict[type, list[int]] = defaultdict(list)\n</code></pre>"},{"location":"reference/trading/execution/engine/events/event_bus/#contango.trading.execution.engine.events.event_bus.EventBus.publish","title":"publish","text":"<pre><code>publish(event: object) -&gt; None\n</code></pre> <p>Publishes an event to all handlers registered for its type.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>object</code> <p>The underlying event to publish.</p> required Source code in <code>src\\contango\\trading\\execution\\engine\\events\\event_bus.py</code> <pre><code>def publish(self, event: object) -&gt; None:\n    \"\"\"\n    Publishes an event to all handlers registered for its type.\n\n    Args:\n        event: The underlying event to publish.\n    \"\"\"\n    handlers = self._handlers.get(type(event))\n\n    if handlers is None:\n        return\n\n    for handler in handlers:\n        handler(event)\n</code></pre>"},{"location":"reference/trading/execution/engine/events/event_bus/#contango.trading.execution.engine.events.event_bus.EventBus.subscribe","title":"subscribe","text":"<pre><code>subscribe(event_type: type, handler: _handler_type, priority: int) -&gt; None\n</code></pre> <p>Subscribes a handler to an event type. Higher priority handlers execute first.</p> <p>Parameters:</p> Name Type Description Default <code>event_type</code> <code>type</code> <p>The type for the event to be subscribed to.</p> required <code>handler</code> <code>_handler_type</code> <p>The method that recieves the event.</p> required <code>priority</code> <code>int</code> <p>The priority (highest first) for the subscription.</p> required Source code in <code>src\\contango\\trading\\execution\\engine\\events\\event_bus.py</code> <pre><code>def subscribe(\n    self,\n    event_type: type,\n    handler: _handler_type,\n    priority: int,\n) -&gt; None:\n    \"\"\"\n    Subscribes a handler to an event type. Higher priority handlers execute first.\n\n    Args:\n        event_type: The type for the event to be subscribed to.\n        handler: The method that recieves the event.\n        priority: The priority (highest first) for the subscription.\n    \"\"\"\n    if priority in self._priorities[event_type]:\n        raise ValueError(\"Two subscriptions must not have the same priority!\")\n\n    self._priorities[event_type].append(priority)\n    self._handlers[event_type].append(handler)\n\n    # Sort both lists together by priority descending\n    paired = sorted(\n        zip(self._priorities[event_type], self._handlers[event_type]),\n        reverse=True\n    )\n    self._priorities[event_type] = [p for p, _ in paired]\n    self._handlers[event_type] = [h for _, h in paired]\n</code></pre>"},{"location":"reference/trading/execution/engine/events/types/","title":"Types","text":""},{"location":"reference/trading/execution/engine/events/types/#contangotradingexecutionengineeventstypes","title":"<code>contango.trading.execution.engine.events.types</code>","text":""},{"location":"reference/trading/execution/engine/events/types/#contango.trading.execution.engine.events.types","title":"contango.trading.execution.engine.events.types","text":""},{"location":"reference/trading/execution/engine/events/types/#contango.trading.execution.engine.events.types.AcceptedFillEvent","title":"AcceptedFillEvent","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single filled trade event (an order that has been processed) that has been accepted.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>When the trade was filled (unix ms).</p> <code>market_event</code> <code>MarketDataEvent</code> <p>The market event in which the trade was filled at.</p> <code>order_event</code> <code>OrderEvent</code> <p>The underlying order that was filled.</p> <code>fill_price</code> <code>USD</code> <p>The price that the order was filled at (per unit).</p> <code>total_cost</code> <code>USD</code> <p>The total cost (including slippage &amp; commission) for the entire trade.</p>"},{"location":"reference/trading/execution/engine/events/types/#contango.trading.execution.engine.events.types.MarketDataEvent","title":"MarketDataEvent","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Immutable event snapshot of OHLCV data for a single bar.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>Time unix (ms) of the bar.</p> <code>symbol</code> <code>str</code> <p>Ticker symbol.</p> <code>open</code> <code>USD</code> <p>Opening price in USD.</p> <code>high</code> <code>USD</code> <p>Highest price in USD.</p> <code>low</code> <code>USD</code> <p>Lowest price in USD.</p> <code>close</code> <code>USD</code> <p>Closing price in USD.</p> <code>volume</code> <code>units</code> <p>Number of units traded.</p>"},{"location":"reference/trading/execution/engine/events/types/#contango.trading.execution.engine.events.types.OrderEvent","title":"OrderEvent","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single trade event emitted by a strategy.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>The time (unix ms) which the order event was created.</p> <code>quantity</code> <code>units</code> <p>Number of units (i.e. shares) to trade (signed for buys versus sells).</p> <code>reason</code> <code>str | None</code> <p>Optional description on why the trade was made.</p>"},{"location":"reference/trading/execution/engine/events/types/#contango.trading.execution.engine.events.types.PortfolioSnapshotEvent","title":"PortfolioSnapshotEvent","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single event snapshot of an account portfolio.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>The timestamp in which the portfolio snapshot was made (unix ms).</p> <code>cash</code> <code>USD</code> <p>The cash available for trading in the account.</p> <code>position</code> <code>units</code> <p>The current amount of units held in the account.</p> <code>equity</code> <code>USD | None</code> <p>The total equity (cash + units) in the account, or None if it cannot be derived (e.g. initial snapshot).</p>"},{"location":"reference/trading/execution/engine/events/types/#contango.trading.execution.engine.events.types.RejectedFillEvent","title":"RejectedFillEvent","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single filled trade event (an order that has been processed) that has been rejected.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>When the rejected trade was attempted to be filled (unix ms).</p> <code>market_event</code> <code>MarketDataEvent</code> <p>The market event in which the rejected trade was made at.</p> <code>order_event</code> <code>OrderEvent</code> <p>The underlying order that was rejected.</p> <code>reason</code> <code>str | None</code> <p>The reasoning for the rejection.</p>"},{"location":"reference/trading/execution/engine/events/types/#contango.trading.execution.engine.events.types.StoplossOrderEvent","title":"StoplossOrderEvent","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single trade event with a stop loss emitted by a strategy.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>The time (unix ms) which the order event was created.</p> <code>quantity</code> <code>units</code> <p>Number of units (i.e. shares) to trade (signed for buys versus sells).</p> <code>stop_price</code> <code>USD</code> <code>reason</code> <code>str | None</code> <p>Optional description on why the trade was made.</p>"},{"location":"reference/trading/execution/engine/fills/fill_policy/","title":"Fill Policy","text":""},{"location":"reference/trading/execution/engine/fills/fill_policy/#contangotradingexecutionenginefillsfill_policy","title":"<code>contango.trading.execution.engine.fills.fill_policy</code>","text":""},{"location":"reference/trading/execution/engine/fills/fill_policy/#contango.trading.execution.engine.fills.fill_policy","title":"contango.trading.execution.engine.fills.fill_policy","text":""},{"location":"reference/trading/execution/engine/fills/fill_policy/#contango.trading.execution.engine.fills.fill_policy.FillPolicy","title":"FillPolicy","text":"<pre><code>FillPolicy(rules: tuple[FillRule[T], ...])\n</code></pre> <p>               Bases: <code>Generic[T]</code></p> <p>The policy for a certain mode of trading to pre-validate that all trading rules are met.</p> <p>Initializes <code>FillPolicy</code>.</p> <p>Parameters:</p> Name Type Description Default <code>rules</code> <code>tuple[FillRule[T], ...]</code> <p>An iterable of fill rules for the policy.</p> required Source code in <code>src\\contango\\trading\\execution\\engine\\fills\\fill_policy.py</code> <pre><code>def __init__(self, rules: tuple[FillRule[T], ...]) -&gt; None:\n    \"\"\"\n    Initializes `FillPolicy`.\n\n    Args:\n        rules: An iterable of fill rules for the policy.\n    \"\"\"\n    self._rules = rules\n</code></pre>"},{"location":"reference/trading/execution/engine/fills/fill_policy/#contango.trading.execution.engine.fills.fill_policy.FillPolicy.validate_rules","title":"validate_rules","text":"<pre><code>validate_rules(context: T) -&gt; str | None\n</code></pre> <p>Validates all rules for the policy.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: The violation reason for the first violated rule, or <code>None</code> if none were violated.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\fills\\fill_policy.py</code> <pre><code>def validate_rules(self, context: T) -&gt; str | None:\n    \"\"\"\n    Validates all rules for the policy.\n\n    Returns:\n        str | None: The violation reason for the first violated rule, or `None` if none were violated.\n    \"\"\"\n    for rule in self._rules:\n        passing = rule.validate(context)\n\n        if not passing:\n            return rule.reason\n\n    return None\n</code></pre>"},{"location":"reference/trading/execution/engine/fills/fill_rule/","title":"Fill Rule","text":""},{"location":"reference/trading/execution/engine/fills/fill_rule/#contangotradingexecutionenginefillsfill_rule","title":"<code>contango.trading.execution.engine.fills.fill_rule</code>","text":""},{"location":"reference/trading/execution/engine/fills/fill_rule/#contango.trading.execution.engine.fills.fill_rule","title":"contango.trading.execution.engine.fills.fill_rule","text":""},{"location":"reference/trading/execution/engine/fills/fill_rule/#contango.trading.execution.engine.fills.fill_rule.FillRule","title":"FillRule","text":"<p>               Bases: <code>ABC</code>, <code>Generic[T]</code></p> <p>A single rule for validation when attempting to fill an order.</p>"},{"location":"reference/trading/execution/engine/fills/fill_rule/#contango.trading.execution.engine.fills.fill_rule.FillRule.reason","title":"reason  <code>abstractmethod</code> <code>property</code>","text":"<pre><code>reason: str\n</code></pre> <p>Returns the reason displayed upon the rule failing.</p>"},{"location":"reference/trading/execution/engine/fills/fill_rule/#contango.trading.execution.engine.fills.fill_rule.FillRule.validate","title":"validate  <code>abstractmethod</code>","text":"<pre><code>validate(context: T) -&gt; bool\n</code></pre> <p>Validates the state of the given rule for a policy.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Whether or not the rule passed validation.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\fills\\fill_rule.py</code> <pre><code>@abstractmethod\ndef validate(self, context: T) -&gt; bool:\n    \"\"\"\n    Validates the state of the given rule for a policy.\n\n    Returns:\n        Whether or not the rule passed validation.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/trading/execution/engine/orders/order_api/","title":"Order API","text":""},{"location":"reference/trading/execution/engine/orders/order_api/#contangotradingexecutionengineordersorder_api","title":"<code>contango.trading.execution.engine.orders.order_api</code>","text":""},{"location":"reference/trading/execution/engine/orders/order_api/#contango.trading.execution.engine.orders.order_api","title":"contango.trading.execution.engine.orders.order_api","text":""},{"location":"reference/trading/execution/engine/orders/order_api/#contango.trading.execution.engine.orders.order_api.OrderAPI","title":"OrderAPI","text":"<pre><code>OrderAPI(event_bus: EventBus)\n</code></pre> <p>API that allows <code>OrderEvent</code> instances to be published to the execution engine.</p> <p>Initializes <code>OrderAPI</code>.</p> <p>Parameters:</p> Name Type Description Default <code>event_bus</code> <code>EventBus</code> <p>The event bus to publish <code>OrderEvent</code> objects to.</p> required Source code in <code>src\\contango\\trading\\execution\\engine\\orders\\order_api.py</code> <pre><code>def __init__(self, event_bus: EventBus) -&gt; None:\n    \"\"\"\n    Initializes `OrderAPI`.\n\n    Args:\n        event_bus: The event bus to publish `OrderEvent` objects to.\n    \"\"\"\n    self._event_bus = event_bus\n</code></pre>"},{"location":"reference/trading/execution/engine/orders/order_api/#contango.trading.execution.engine.orders.order_api.OrderAPI.submit_order","title":"submit_order","text":"<pre><code>submit_order(market_data: MarketDataEvent, symbol: str, quantity: units, reason: str | None = None) -&gt; None\n</code></pre> <p>Submits a market order object to the engine.</p> <p>Attributes:</p> Name Type Description <code>market_data</code> <p>The market data event for the order.</p> <code>symbol</code> <p>The symbol to create the order for.</p> <code>quantity</code> <p>The amount of units of the symbol to trade (positive or negative depending on buy or sell).</p> <code>reason</code> <p>The optional reason for the trade.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\orders\\order_api.py</code> <pre><code>def submit_order(\n    self, \n    market_data: MarketDataEvent, \n    symbol: str, \n    quantity: units,\n    reason: str | None = None,\n) -&gt; None:\n    \"\"\"\n    Submits a market order object to the engine.\n\n    Attributes:\n        market_data: The market data event for the order.\n        symbol: The symbol to create the order for.\n        quantity: The amount of units of the symbol to trade (positive or negative depending on buy or sell).\n        reason: The optional reason for the trade.\n    \"\"\"\n    order = OrderEvent(\n        market_data.timestamp,\n        quantity=quantity,\n        reason=reason\n    )\n    self._event_bus.publish(order)\n</code></pre>"},{"location":"reference/trading/execution/engine/orders/order_api/#contango.trading.execution.engine.orders.order_api.OrderAPI.submit_stoploss_order","title":"submit_stoploss_order","text":"<pre><code>submit_stoploss_order(market_data: MarketDataEvent, symbol: str, quantity: units, stoploss_price: USD, reason: str | None = None) -&gt; None\n</code></pre> <p>Submits a stoploss order object to the engine.</p> <p>Attributes:</p> Name Type Description <code>market_data</code> <p>The market data event for the order.</p> <code>symbol</code> <p>The symbol to create the order for.</p> <code>quantity</code> <p>The amount of units of the symbol to trade (positive or negative depending on buy or sell).</p> <code>stoploss_price</code> <p>The price in which the quantity amount of shares will be sold upon being met.</p> <code>reason</code> <p>The optional reason for the trade.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\orders\\order_api.py</code> <pre><code>def submit_stoploss_order(\n    self, \n    market_data: MarketDataEvent, \n    symbol: str, \n    quantity: units,\n    stoploss_price: USD,\n    reason: str | None = None,\n) -&gt; None:\n    \"\"\"\n    Submits a stoploss order object to the engine.\n\n    Attributes:\n        market_data: The market data event for the order.\n        symbol: The symbol to create the order for.\n        quantity: The amount of units of the symbol to trade (positive or negative depending on buy or sell).\n        stoploss_price: The price in which the quantity amount of shares will be sold upon being met.\n        reason: The optional reason for the trade.\n    \"\"\"\n    order = StoplossOrderEvent(\n        market_data.timestamp,\n        quantity=quantity,\n        stop_price=stoploss_price,\n        reason=reason\n    )\n    self._event_bus.publish(order)\n</code></pre>"},{"location":"reference/trading/execution/engine/results/execution_data/","title":"Execution Data","text":""},{"location":"reference/trading/execution/engine/results/execution_data/#contangotradingexecutionengineresultsexecution_data","title":"<code>contango.trading.execution.engine.results.execution_data</code>","text":""},{"location":"reference/trading/execution/engine/results/execution_data/#contango.trading.execution.engine.results.execution_data","title":"contango.trading.execution.engine.results.execution_data","text":""},{"location":"reference/trading/execution/engine/results/execution_data/#contango.trading.execution.engine.results.execution_data.ExecutionData","title":"ExecutionData","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The accumulated execution data for analysis.</p> <p>Attributes:</p> Name Type Description <code>accepted_fill_events</code> <code>tuple[AcceptedFillEvent, ...]</code> <p>All the accumulated accepted fill events (order from start -&gt; finish) during execution.</p> <code>rejected_fill_events</code> <code>tuple[RejectedFillEvent, ...]</code> <p>All the accumulated rejected fill events (order from start -&gt; finish) during execution.</p> <code>order_events</code> <code>tuple[OrderEvent, ...]</code> <p>All the accumulated order events (order from start -&gt; finish) during execution.</p> <code>market_data_events</code> <code>tuple[MarketDataEvent, ...]</code> <p>All the accumulated market data (order from start -&gt; finish) during execution.</p> <code>initial_portfolio_snapshot_event</code> <code>PortfolioSnapshotEvent</code> <p>The first portfolio snapshot event before any trading.</p> <code>portfolio_snapshot_events</code> <code>tuple[PortfolioSnapshotEvent, ...]</code> <p>All the accumulated portfolio snapshots (order from start -&gt; finish) during execution.</p>"},{"location":"reference/trading/execution/engine/results/results_collector/","title":"Results Collector","text":""},{"location":"reference/trading/execution/engine/results/results_collector/#contangotradingexecutionengineresultsresults_collector","title":"<code>contango.trading.execution.engine.results.results_collector</code>","text":""},{"location":"reference/trading/execution/engine/results/results_collector/#contango.trading.execution.engine.results.results_collector","title":"contango.trading.execution.engine.results.results_collector","text":""},{"location":"reference/trading/execution/engine/results/results_collector/#contango.trading.execution.engine.results.results_collector.ResultsCollector","title":"ResultsCollector","text":"<pre><code>ResultsCollector()\n</code></pre> <p>Collects all results from events &amp; stores them for later analysis.</p> <p>Initializes <code>ResultsCollector</code> with empty lists for all events.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\results\\results_collector.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"\n    Initializes `ResultsCollector` with empty lists for all events.\n    \"\"\"\n    self.accepted_fill_events: list[AcceptedFillEvent] = []\n    self.rejected_fill_events: list[RejectedFillEvent] = []\n    self.order_events: list[OrderEvent] = []\n    self.market_data_events: list[MarketDataEvent] = []\n    self.portfolio_snapshot_events: list[PortfolioSnapshotEvent] = []\n</code></pre>"},{"location":"reference/trading/execution/engine/results/results_collector/#contango.trading.execution.engine.results.results_collector.ResultsCollector.collect_accepted_fill_event","title":"collect_accepted_fill_event","text":"<pre><code>collect_accepted_fill_event(event: AcceptedFillEvent) -&gt; None\n</code></pre> <p>Stores an <code>AcceptedFillEvent</code> in an internal list.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\results\\results_collector.py</code> <pre><code>def collect_accepted_fill_event(self, event: AcceptedFillEvent) -&gt; None:\n    \"\"\"\n    Stores an `AcceptedFillEvent` in an internal list.\n    \"\"\"\n    self.accepted_fill_events.append(event)\n</code></pre>"},{"location":"reference/trading/execution/engine/results/results_collector/#contango.trading.execution.engine.results.results_collector.ResultsCollector.collect_market_data_event","title":"collect_market_data_event","text":"<pre><code>collect_market_data_event(event: MarketDataEvent) -&gt; None\n</code></pre> <p>Stores a <code>MarketDataEvent</code> in an internal list.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\results\\results_collector.py</code> <pre><code>def collect_market_data_event(self, event: MarketDataEvent) -&gt; None:\n    \"\"\"\n    Stores a `MarketDataEvent` in an internal list.\n    \"\"\"\n    self.market_data_events.append(event)\n</code></pre>"},{"location":"reference/trading/execution/engine/results/results_collector/#contango.trading.execution.engine.results.results_collector.ResultsCollector.collect_order_event","title":"collect_order_event","text":"<pre><code>collect_order_event(event: OrderEvent) -&gt; None\n</code></pre> <p>Stores an <code>OrderEvent</code> in an internal list.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\results\\results_collector.py</code> <pre><code>def collect_order_event(self, event: OrderEvent) -&gt; None:\n    \"\"\"\n    Stores an `OrderEvent` in an internal list.\n    \"\"\"\n    self.order_events.append(event)\n</code></pre>"},{"location":"reference/trading/execution/engine/results/results_collector/#contango.trading.execution.engine.results.results_collector.ResultsCollector.collect_portfolio_snapshot_event","title":"collect_portfolio_snapshot_event","text":"<pre><code>collect_portfolio_snapshot_event(event: PortfolioSnapshotEvent) -&gt; None\n</code></pre> <p>Stores a <code>PortfolioSnapshotEvent</code> in an internal list.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\results\\results_collector.py</code> <pre><code>def collect_portfolio_snapshot_event(self, event: PortfolioSnapshotEvent) -&gt; None:\n    \"\"\"\n    Stores a `PortfolioSnapshotEvent` in an internal list.\n    \"\"\"\n    self.portfolio_snapshot_events.append(event)\n</code></pre>"},{"location":"reference/trading/execution/engine/results/results_collector/#contango.trading.execution.engine.results.results_collector.ResultsCollector.collect_rejected_fill_event","title":"collect_rejected_fill_event","text":"<pre><code>collect_rejected_fill_event(event: RejectedFillEvent) -&gt; None\n</code></pre> <p>Stores an <code>RejectedFillEvent</code> in an internal list.</p> Source code in <code>src\\contango\\trading\\execution\\engine\\results\\results_collector.py</code> <pre><code>def collect_rejected_fill_event(self, event: RejectedFillEvent) -&gt; None:\n    \"\"\"\n    Stores an `RejectedFillEvent` in an internal list.\n    \"\"\"\n    self.rejected_fill_events.append(event)\n</code></pre>"},{"location":"reference/trading/metrics/builder/","title":"Builder","text":""},{"location":"reference/trading/metrics/builder/#contangotradingmetricsbuilder","title":"<code>contango.trading.metrics.builder</code>","text":""},{"location":"reference/trading/metrics/builder/#contango.trading.metrics.builder","title":"contango.trading.metrics.builder","text":""},{"location":"reference/trading/metrics/builder/#contango.trading.metrics.builder.OpenPosition","title":"OpenPosition  <code>dataclass</code>","text":"<pre><code>OpenPosition(quantity: units = 0, entry_time: datetime | None = None, entry_price: USD | None = None)\n</code></pre> <p>A single open position when building trade points.</p> <p>Attributes:</p> Name Type Description <code>quantity</code> <code>units</code> <p>The units for the position.</p> <code>entry_time</code> <code>datetime | None</code> <p>The time in which the trade was entered.</p> <code>entry_price</code> <code>USD | None</code> <p>The price in which the trade was entered at.</p>"},{"location":"reference/trading/metrics/builder/#contango.trading.metrics.builder.build_context","title":"build_context","text":"<pre><code>build_context(data: ExecutionData) -&gt; AnalysisContext\n</code></pre> <p>Builds an <code>AnalysisContext</code> for a single backtest.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>ExecutionData</code> <p>The <code>ExecutionData</code> results from a strategy.</p> required <p>Returns:</p> Name Type Description <code>AnalysisContext</code> <code>AnalysisContext</code> <p>The context to compute metrics for the backtest.</p> Source code in <code>src\\contango\\trading\\metrics\\builder.py</code> <pre><code>def build_context(data: ExecutionData) -&gt; AnalysisContext:\n    \"\"\"\n    Builds an `AnalysisContext` for a single backtest.\n\n    Args:\n        data: The `ExecutionData` results from a strategy.\n\n    Returns:\n        AnalysisContext: The context to compute metrics for the backtest.\n    \"\"\"\n    equity_curve = _build_equity_curve(data)\n    returns = _build_returns(equity_curve)\n    drawdowns = _build_drawdowns(equity_curve)\n    trades = _build_trades(data)\n\n    return AnalysisContext(\n        equity_curve=equity_curve,\n        returns=returns,\n        drawdowns=drawdowns,\n        trades=trades\n    )\n</code></pre>"},{"location":"reference/trading/metrics/calculate/","title":"Calculate","text":""},{"location":"reference/trading/metrics/calculate/#contangotradingmetricscalculate","title":"<code>contango.trading.metrics.calculate</code>","text":""},{"location":"reference/trading/metrics/calculate/#contango.trading.metrics.calculate","title":"contango.trading.metrics.calculate","text":""},{"location":"reference/trading/metrics/calculate/#contango.trading.metrics.calculate.calculate_metrics","title":"calculate_metrics","text":"<pre><code>calculate_metrics(data: ExecutionData) -&gt; Metrics\n</code></pre> <p>Calculates readable metrics for backtest data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>ExecutionData</code> <p>The backtest data to derive metrics from.</p> required <p>Returns:</p> Name Type Description <code>Metrics</code> <code>Metrics</code> <p>A metrics object representing returns, drawdown, risks, and trade information about the strategy.</p> Source code in <code>src\\contango\\trading\\metrics\\calculate.py</code> <pre><code>def calculate_metrics(data: ExecutionData) -&gt; Metrics:\n    \"\"\"\n    Calculates readable metrics for backtest data.\n\n    Args:\n        data: The backtest data to derive metrics from.\n\n    Returns:\n        Metrics: A metrics object representing returns, drawdown, risks, and trade information about the strategy.\n    \"\"\"\n    analysis_context = build_context(data)\n    return_metrics = get_return_metrics(analysis_context)\n    drawdown_metrics = get_drawdown_metrics(analysis_context)\n    risk_metrics = get_risk_metrics(analysis_context, return_metrics.monthly_returns, drawdown_metrics.max_drawdown)\n    trade_metrics = get_trade_metrics(analysis_context)\n    return Metrics(\n        returns=return_metrics,\n        risk=risk_metrics,\n        drawdowns=drawdown_metrics,\n        trades=trade_metrics\n    )\n</code></pre>"},{"location":"reference/trading/metrics/context/","title":"Context","text":""},{"location":"reference/trading/metrics/context/#contangotradingmetricscontext","title":"<code>contango.trading.metrics.context</code>","text":""},{"location":"reference/trading/metrics/context/#contango.trading.metrics.context","title":"contango.trading.metrics.context","text":""},{"location":"reference/trading/metrics/context/#contango.trading.metrics.context.AnalysisContext","title":"AnalysisContext","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The analysis data necessary to compute the metrics for a backtest.</p> <p>Attributes:</p> Name Type Description <code>equity_curve</code> <code>tuple[EquityPoint, ...]</code> <p>The equity (value of units + cash) in USD for every portfolio event.</p> <code>returns</code> <code>tuple[ReturnPoint, ...]</code> <p>The total returns (in percent) for every equity point.</p> <code>drawdowns</code> <code>tuple[DrawdownPoint, ...]</code> <p>The peak (USD) &amp; drawdown (in percent) for every equity point.</p> <code>trades</code> <code>tuple[TradePoint, ...]</code> <p>The trades made during the execution of the backtester.</p>"},{"location":"reference/trading/metrics/context/#contango.trading.metrics.context.DrawdownPoint","title":"DrawdownPoint","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single drawdown point in time.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>The time for the drawdown point (unix ms).</p> <code>peak</code> <code>time_unix_ms</code> <p>The highest equity up to and including this point.</p> <code>drawdown_percent</code> <code>percent</code> <p>The current percentage decline of the equity from its peak value to its lowest point.</p>"},{"location":"reference/trading/metrics/context/#contango.trading.metrics.context.EquityPoint","title":"EquityPoint","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single equity point in an equity curve.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>The time for the equity point (unix ms).</p> <code>equity</code> <code>USD</code> <p>The price for the equity point at its timestamp.</p>"},{"location":"reference/trading/metrics/context/#contango.trading.metrics.context.ReturnPoint","title":"ReturnPoint","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single return point in time.</p> <p>Attributes:</p> Name Type Description <code>timestamp</code> <code>time_unix_ms</code> <p>The time for the return point (unix ms).</p> <code>return_percent</code> <code>percent</code> <p>The percent increase / decrease from the previous point.</p>"},{"location":"reference/trading/metrics/context/#contango.trading.metrics.context.TradePoint","title":"TradePoint","text":"<p>               Bases: <code>NamedTuple</code></p> <p>A single completed trade (long-only).</p> <p>Attributes:</p> Name Type Description <code>entry_time</code> <code>time_unix_ms</code> <p>When the position was opened (unix ms).</p> <code>exit_time</code> <code>time_unix_ms</code> <p>When the position was closed (unix ms).</p> <code>entry_price</code> <code>USD</code> <p>Price at entry.</p> <code>exit_price</code> <code>USD</code> <p>Price at exit.</p> <code>quantity</code> <code>units</code> <p>Units held.</p>"},{"location":"reference/trading/metrics/metrics/","title":"Metrics","text":""},{"location":"reference/trading/metrics/metrics/#contangotradingmetricsmetrics","title":"<code>contango.trading.metrics.metrics</code>","text":""},{"location":"reference/trading/metrics/metrics/#contango.trading.metrics.metrics","title":"contango.trading.metrics.metrics","text":""},{"location":"reference/trading/metrics/metrics/#contango.trading.metrics.metrics.DrawdownMetrics","title":"DrawdownMetrics","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The drawdown metrics for a backtest. Holds the worst possible risk and volatility of a strategy.</p> <p>Attributes:</p> Name Type Description <code>max_drawdown</code> <code>percent | None</code> <p>The worst single historical loss of the strategy.</p> <code>average_drawdown</code> <code>percent | None</code> <p>The average of historical losses of the strategy.</p>"},{"location":"reference/trading/metrics/metrics/#contango.trading.metrics.metrics.Metrics","title":"Metrics","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The metrics of a single backtest, used to identify the effectiveness of a strategy.</p> <p>Attributes:</p> Name Type Description <code>returns</code> <code>ReturnMetrics</code> <p>The return (cash, units) metrics for the strategy.</p> <code>risk</code> <code>RiskMetrics</code> <p>The risk (reliability) metrics for the strategy.</p> <code>drawdowns</code> <code>DrawdownMetrics</code> <p>The drawdown (losses) metrics for the strategy.</p> <code>trades</code> <code>TradeMetrics</code> <p>The number of trades made, losses, wins, profits, etc.</p>"},{"location":"reference/trading/metrics/metrics/#contango.trading.metrics.metrics.Metrics.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Returns a string representation of the metrics.</p> Source code in <code>src\\contango\\trading\\metrics\\metrics.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"\n    Returns a string representation of the metrics.\n    \"\"\"\n    def fmt_f(val: float | None) -&gt; str:\n        return f\"{val:.2f}\" if val is not None else \"N/A\"\n\n    return (\n        \"Backtest Metrics:\\n\"\n        f\"  Total Return: {fmt_f(self.returns.total_return)}\\n\"\n        f\"  Annual Return: {fmt_f(self.risk.annual_return)}\\n\"\n        f\"  Volatility: {fmt_f(self.risk.monthly_volatility)}\\n\"\n        f\"  Sharpe Ratio: {fmt_f(self.risk.sharpe_ratio)}\\n\"\n        f\"  Calmar Ratio: {fmt_f(self.risk.calmar_ratio)}\\n\"\n        f\"  Max Drawdown: {fmt_f(self.drawdowns.max_drawdown)}\\n\"\n        f\"  Average Drawdown: {fmt_f(self.drawdowns.average_drawdown)}\\n\"\n        f\"  Trades: {self.trades.trade_count}\\n\"\n        f\"  Win Rate: {fmt_f(self.trades.win_rate)}\\n\"\n        f\"  Profit Factor: {fmt_f(self.trades.profit_factor)}\\n\"\n        f\"  Expectancy: {fmt_f(self.trades.expectancy)}\\n\"\n        f\"  Average Holding Period: {fmt_f(self.trades.average_holding_period)}\"\n    )\n</code></pre>"},{"location":"reference/trading/metrics/metrics/#contango.trading.metrics.metrics.ReturnMetrics","title":"ReturnMetrics","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The resulting returns from the backtest. Holds the actual returned property and value from a strategy over time.</p> <p>Attributes:</p> Name Type Description <code>total_return</code> <code>percent | None</code> <p>The percent returned from the backtest at the end of execution.</p> <code>monthly_returns</code> <code>tuple[tuple[time_unix_ms, percent], ...] | None</code> <p>A sequence of the total returns per month of the strategy.</p> <code>equity_curve</code> <code>tuple[tuple[time_unix_ms, USD], ...]</code> <p>The account value over time (total value of cash + units).</p>"},{"location":"reference/trading/metrics/metrics/#contango.trading.metrics.metrics.RiskMetrics","title":"RiskMetrics","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The risk factor metrics from a backtest. Determines how reliable a strategy was over the backtest.</p> <p>Attributes:</p> Name Type Description <code>annual_return</code> <code>percent | None</code> <p>The CAGR (annualized compound return) for the backtest.</p> <code>monthly_volatility</code> <code>percent | None</code> <p>Measure of how intensely returns move per month.</p> <code>sharpe_ratio</code> <code>float | None</code> <p>Measure of how much excess return received per unit of risk taken (annaul).</p> <code>calmar_ratio</code> <code>float | None</code> <p>Measure of much return an investment has generated relative to its worst possible loss.</p>"},{"location":"reference/trading/metrics/metrics/#contango.trading.metrics.metrics.TradeMetrics","title":"TradeMetrics","text":"<p>               Bases: <code>NamedTuple</code></p> <p>The basic trading metrics for a backtest. Holds the actual number of trades made, wins, losses, and profits.</p> Attrubtes <p>trade_count: The total number of trades the strategy made during the backtest. win_rate: The percent of trades that the strategy was profitable. profit_factor: The gross profit from winning trades to gross losses from losing trades. expectancy: The expected profit per trade with the strategy. average_win: The average percent made per win. average_loss: The average percent lost per loss. average_holding_period: The average amount of time that the strategy holds a trade for.</p>"},{"location":"reference/trading/metrics/calculators/drawdown/","title":"Drawdown","text":""},{"location":"reference/trading/metrics/calculators/drawdown/#contangotradingmetricscalculatorsdrawdown","title":"<code>contango.trading.metrics.calculators.drawdown</code>","text":""},{"location":"reference/trading/metrics/calculators/drawdown/#contango.trading.metrics.calculators.drawdown","title":"contango.trading.metrics.calculators.drawdown","text":""},{"location":"reference/trading/metrics/calculators/drawdown/#contango.trading.metrics.calculators.drawdown.get_drawdown_metrics","title":"get_drawdown_metrics","text":"<pre><code>get_drawdown_metrics(context: AnalysisContext) -&gt; DrawdownMetrics\n</code></pre> <p>Calculates the drawdown metrics for a strategy backtest.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>AnalysisContext</code> <p>The <code>AnalysisContext</code> object to derive data from.</p> required <p>Returns:</p> Type Description <code>DrawdownMetrics</code> <p>A <code>DrawdownMetrics</code> object with all the metrics relating to returns.</p> Source code in <code>src\\contango\\trading\\metrics\\calculators\\drawdown.py</code> <pre><code>def get_drawdown_metrics(context: AnalysisContext) -&gt; DrawdownMetrics:\n    \"\"\"\n    Calculates the drawdown metrics for a strategy backtest.\n\n    Args:\n        context: The `AnalysisContext` object to derive data from.\n\n    Returns:\n        A `DrawdownMetrics` object with all the metrics relating to returns.\n    \"\"\"\n    max_drawdown = _get_max_drawdown(context.drawdowns)\n    average_drawdown = _get_average_drawdown(context.drawdowns)\n    return DrawdownMetrics(\n        max_drawdown=max_drawdown,\n        average_drawdown=average_drawdown\n    )\n</code></pre>"},{"location":"reference/trading/metrics/calculators/returns/","title":"Returns","text":""},{"location":"reference/trading/metrics/calculators/returns/#contangotradingmetricscalculatorsreturns","title":"<code>contango.trading.metrics.calculators.returns</code>","text":""},{"location":"reference/trading/metrics/calculators/returns/#contango.trading.metrics.calculators.returns","title":"contango.trading.metrics.calculators.returns","text":""},{"location":"reference/trading/metrics/calculators/returns/#contango.trading.metrics.calculators.returns.get_return_metrics","title":"get_return_metrics","text":"<pre><code>get_return_metrics(context: AnalysisContext) -&gt; ReturnMetrics\n</code></pre> <p>Returns the <code>ReturnMetrics</code> for the given context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>AnalysisContext</code> <p>The <code>AnalysisContext</code> object to derive data from.</p> required <p>Returns:</p> Type Description <code>ReturnMetrics</code> <p>A <code>ReturnMetrics</code> object with all the metrics relating to returns.</p> Source code in <code>src\\contango\\trading\\metrics\\calculators\\returns.py</code> <pre><code>def get_return_metrics(context: AnalysisContext) -&gt; ReturnMetrics:\n    \"\"\"\n    Returns the `ReturnMetrics` for the given context.\n\n    Args:\n        context: The `AnalysisContext` object to derive data from.\n\n    Returns:\n        A `ReturnMetrics` object with all the metrics relating to returns.\n    \"\"\"\n    total_return = _get_total_return(context.equity_curve)\n    monthly_returns = _get_monthly_returns(context.equity_curve)\n    equity_curve = context.equity_curve\n    return ReturnMetrics(\n        total_return=total_return,\n        monthly_returns=monthly_returns,\n        equity_curve=equity_curve\n    )\n</code></pre>"},{"location":"reference/trading/metrics/calculators/risk/","title":"Risk","text":""},{"location":"reference/trading/metrics/calculators/risk/#contangotradingmetricscalculatorsrisk","title":"<code>contango.trading.metrics.calculators.risk</code>","text":""},{"location":"reference/trading/metrics/calculators/risk/#contango.trading.metrics.calculators.risk","title":"contango.trading.metrics.calculators.risk","text":""},{"location":"reference/trading/metrics/calculators/risk/#contango.trading.metrics.calculators.risk.get_risk_metrics","title":"get_risk_metrics","text":"<pre><code>get_risk_metrics(context: AnalysisContext, monthly_returns: tuple[tuple[time_unix_ms, percent], ...] | None, max_drawdown: percent | None) -&gt; RiskMetrics\n</code></pre> <p>Returns the <code>RiskMetrics</code> for a strategy backtest.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>AnalysisContext</code> <p>The <code>AnalysisContext</code> object to derive data from.</p> required <code>monthly_returns</code> <code>tuple[tuple[time_unix_ms, percent], ...] | None</code> <p>The returns (in percent) per month from latest to most recent.</p> required <code>max_drawdown</code> <code>percent | None</code> <p>The worst single historical loss of the strategy.</p> required Source code in <code>src\\contango\\trading\\metrics\\calculators\\risk.py</code> <pre><code>def get_risk_metrics(\n    context: AnalysisContext,\n    monthly_returns: tuple[tuple[time_unix_ms, percent], ...] | None,\n    max_drawdown: percent | None\n) -&gt; RiskMetrics:\n    \"\"\"\n    Returns the `RiskMetrics` for a strategy backtest.\n\n    Args:\n        context: The `AnalysisContext` object to derive data from.\n        monthly_returns: The returns (in percent) per month from latest to most recent.\n        max_drawdown: The worst single historical loss of the strategy.\n    \"\"\"\n    annual_return = _get_annual_return(context.equity_curve)\n    monthly_volatility = _get_monthly_volatility(monthly_returns)\n    sharpe_ratio = _get_sharpe_ratio(monthly_returns, monthly_volatility)\n    calmar_ratio = _get_calmar_ratio(annual_return, max_drawdown)\n    return RiskMetrics(\n        annual_return=annual_return,\n        monthly_volatility=monthly_volatility,\n        sharpe_ratio=sharpe_ratio,\n        calmar_ratio=calmar_ratio\n    )\n</code></pre>"},{"location":"reference/trading/metrics/calculators/trades/","title":"Trades","text":""},{"location":"reference/trading/metrics/calculators/trades/#contangotradingmetricscalculatorstrades","title":"<code>contango.trading.metrics.calculators.trades</code>","text":""},{"location":"reference/trading/metrics/calculators/trades/#contango.trading.metrics.calculators.trades","title":"contango.trading.metrics.calculators.trades","text":""},{"location":"reference/trading/metrics/calculators/trades/#contango.trading.metrics.calculators.trades.get_trade_metrics","title":"get_trade_metrics","text":"<pre><code>get_trade_metrics(context: AnalysisContext) -&gt; TradeMetrics\n</code></pre> <p>Returns the <code>TradeMetrics</code> for the given context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>AnalysisContext</code> <p>The <code>AnalysisContext</code> object to derive data from.</p> required <p>Returns:</p> Type Description <code>TradeMetrics</code> <p>A <code>TradeMetrics</code> object with all the metrics relating to trades.</p> Source code in <code>src\\contango\\trading\\metrics\\calculators\\trades.py</code> <pre><code>def get_trade_metrics(context: AnalysisContext) -&gt; TradeMetrics:\n    \"\"\"\n    Returns the `TradeMetrics` for the given context.\n\n    Args:\n        context: The `AnalysisContext` object to derive data from.\n\n    Returns:\n        A `TradeMetrics` object with all the metrics relating to trades.\n    \"\"\"\n    trade_count = _get_trade_count(context.trades)\n    win_rate = _get_win_rate(context.trades)\n    profit_factor = _get_profit_factor(context.trades)\n    expectancy = _get_expectancy(context.trades)\n    average_win = _get_average_win(context.trades)\n    average_loss = _get_average_loss(context.trades)\n    average_holding_period = _get_average_holding_period(context.trades)\n    return TradeMetrics(\n        trade_count=trade_count,\n        win_rate=win_rate,\n        profit_factor=profit_factor,\n        expectancy=expectancy,\n        average_win=average_win,\n        average_loss=average_loss,\n        average_holding_period=average_holding_period\n    )\n</code></pre>"},{"location":"runners/grid_search_runner/","title":"Grid Search Runner","text":"<p>The grid search runner runs all possible combinations for a strategy instance, given lists of the parameters to run. This is a pure cartesian search, meaning that all combinations (order does not matter) are tested amonst themselves.</p>"},{"location":"runners/grid_search_runner/#usage","title":"Usage","text":"<p>First, data must be retrieved from a specified source. Either <code>fetch_data</code> (for one ticker) or <code>fetch_data_multi</code> (for multiple tickers) may be called to fetch the data to test.</p> <p>Next, the <code>run()</code> method should be called to perform the backtest &amp; retrieve the results for each parameter combination:</p> <pre><code>def run(\n    ohlcv_data: Mapping[str, list[MarketDataEvent]],\n    strategy_cls: type[StrategyT],\n    param_grid: Mapping[str, Sequence[Any]],\n    backtester_config: BacktesterConfig,\n    base_label: str | None = None,\n    verbose: bool = True\n) -&gt; list[ExperimentResult]:\n</code></pre> <p>Most of the parameters are self-explanatory. The <code>param_grid</code> is a mapping (dictionary) of parameter names for the strategy class to the parameters you want to test. For example:</p> <pre><code>results = GridSearchRunner.run(\n    ohlcv_data=data,\n    strategy_cls=BollingerBandMeanReversion,\n    param_grid={\n        \"bollinger_bands_period\": [15, 20, 25],\n        \"bollinger_bands_stdev\": [1.0, 1.5, 2.0, 2.5],\n        \"allocation\": [0.25, 0.5, 0.75],\n        \"symbol\": tickers\n    },\n    ...\n)\n</code></pre> <p>Each string (i.e. \"bollinger_bands_period\") is a parameter kwarg inside of the <code>BollingerBandMeanReversion</code> class, and each of those parameters in the corresponding list will be tested. In this specific case, for example, 3 * 4 * 3 * len(tickers) will be tested.</p> <p>See grid search reference</p>"},{"location":"runners/static_runner/","title":"Static Runner","text":"<p>The static runner is designed to run a singular strategy instance that will not be parameterized over different parameter combinations. This is useful for singular tests for already-established strategies rather than fine-tuning strategies.</p>"},{"location":"runners/static_runner/#usage","title":"Usage","text":"<p>First, data must be retrieved from a specified source. Either <code>fetch_data</code> (for one ticker) or <code>fetch_data_multi</code> (for multiple tickers) may be called to fetch the data to test.</p> <p>Next, the <code>run()</code> method should be called to perform the backtest &amp; retrieve the results for each parameter combination:</p> <pre><code>def run(\n    ohlcv_data: list[MarketDataEvent], \n    strategy: Strategy, \n    backtester_config: BacktesterConfig\n) -&gt; Metrics:\n</code></pre> <p>This, provided a singualar strategy instance and backtester config, runs the strategy through the backtester once and returns the metrics for that strategy instance.</p> <p>See static runner reference</p>"},{"location":"strategy/strategy/","title":"Strategies","text":"<p>The <code>Strategy</code> subclass is the most dynamic and least opinionated method of creating strategies; however, it has the cost of being verbose. All logic must be implemented by the user, essentially.</p> <p>Subclasses of <code>Strategy</code> can implement event handlers to initialize state, process market data, and react to order/trade events. The possible hooks / fields to be overriden or accessed respectively are:</p> <ul> <li><code>on_start()</code>: called once before any data is processed.</li> <li><code>on_end()</code>: called once after all data has been processed.</li> <li><code>on_market_event()</code>: triggered upon a market event (bar).</li> <li><code>on_order_event()</code>: triggered when an order is submitted.</li> <li> <p><code>on_fill_event()</code>: triggered when an order is filled.</p> </li> <li> <p><code>order_api</code>: allows for communication with the engine to make orders.</p> </li> <li><code>portfolio_snapshot</code>: the current snapshot of the account that holds all positions &amp; cash.</li> </ul> <p>See strategy reference</p>"},{"location":"strategy/strategy/#example-design-process","title":"Example &amp; design process","text":"<p>Firstly, you want to determine what state your strategy actually needs to maintain. Unlike a rule-based strategy, a normal <code>Strategy</code> subclass does not provide streams, rules, intents, or position sizing automatically. All of this logic is left to the strategy itself.</p> <p>For a Bollinger Band Mean Reversion strategy, we need access to the Bollinger Bands indicator, the previous Bollinger Band snapshot, and the previous market event. We also need to know whether the portfolio is currently holding a position:</p> <pre><code>self._bollinger_bands = BollingerBands(\n    bollinger_bands_period, \n    bollinger_bands_stdev\n)\n\nself._previous_snapshot: BollingerBandSnapshot | None = None\nself._previous_event: MarketDataEvent | None = None\n</code></pre> <p>Next, we need to determine the actual conditions for buying and selling. Since a normal <code>Strategy</code> has no concept of rules or predicates, this logic can be implemented however the developer prefers. For our strategy, we can separate the buy and sell conditions into their own methods:</p> <pre><code>def _should_buy(\n    self, \n    prev_close: float,\n    current_close: float,\n    previous: BollingerBandSnapshot, \n    current: BollingerBandSnapshot, \n    holding: bool\n) -&gt; bool:\n    return (\n        prev_close &gt;= previous.lower\n        and current_close &lt; current.lower\n        and not holding\n    )\n\ndef _should_sell(\n    self, \n    prev_close: float,\n    current_close: float,\n    previous: BollingerBandSnapshot, \n    current: BollingerBandSnapshot, \n    holding: bool\n) -&gt; bool:\n    return (\n        prev_close &lt;= previous.middle\n        and current_close &gt; current.middle\n        and holding\n    )\n</code></pre> <p>These methods essentially perform the same job as the rules in a rule-based strategy. If the previous close was above the lower band and the current close is below it, we want to buy. If the previous close was below the middle band and the current close is above it, we want to sell.</p> <p>However, because this is a normal <code>Strategy</code> subclass, we must also handle updating indicators, maintaining historical state, determining position sizes, and submitting orders ourselves.</p> <p>The <code>on_market_event()</code> method is where the majority of this logic can be implemented. First, the Bollinger Bands need to be updated with the current market event:</p> <pre><code>close = event.close\nsnapshot = self._bollinger_bands.update(event)\n</code></pre> <p>We then need to make sure that we have enough historical information to compare the current market event against the previous one. If the indicator is not ready, or this is the first event received, we simply save the current state and wait for the next event:</p> <pre><code>if self._previous_event is None or self._previous_snapshot is None or snapshot is None:\n    self._previous_event = event\n    self._previous_snapshot = snapshot\n    return\n</code></pre> <p>Once the required state is available, we can determine whether the strategy is currently holding a position and evaluate the buy/sell conditions:</p> <pre><code>prev_close = self._previous_event.close\nholding = self.portfolio_snapshot.position &gt; 0\nprev_snapshot = self._previous_snapshot\n\nif self._should_buy(prev_close, close, prev_snapshot, snapshot, holding):\n    ...\nelif self._should_sell(prev_close, close, prev_snapshot, snapshot, holding):\n    ...\n</code></pre> <p>If a buy signal occurs, the strategy must also determine how many units to purchase. In this example, the strategy uses an allocation percentage of the currently available cash:</p> <pre><code>units_to_buy = int(self._allocation * self.portfolio_snapshot.cash / close)\nself.order_api.submit_order(event, self._symbol, units_to_buy)\n</code></pre> <p>Likewise, when a sell signal occurs, the strategy can directly submit an order for the entire current position:</p> <pre><code>units_to_sell = self.portfolio_snapshot.position\nself.order_api.submit_order(event, self._symbol, -units_to_sell)\n</code></pre> <p>Finally, the previous market event and indicator snapshot need to be updated so they are available during the next call to <code>on_market_event()</code>:</p> <pre><code>self._previous_snapshot = snapshot\nself._previous_event = event\n</code></pre> <p>One additional consideration is what happens when the strategy reaches the end of the data. Since this strategy is intended to hold no position after the backtest has finished, <code>on_end()</code> can close any remaining position. Rule-based strategies do this implicitly, but it must be expicitly done here:</p> <pre><code>def on_end(self) -&gt; None:\n    holding = self.portfolio_snapshot.position &gt; 0\n    final_event = self._previous_event\n\n    if final_event is None:\n        raise ValueError(\"The final market data event was never set.\")\n\n    if holding:\n        units_to_sell = self.portfolio_snapshot.position\n        self.order_api.submit_order(final_event, self._symbol, -units_to_sell)\n</code></pre> <p>A full strategy for a Bollinger Band Mean Reversion would look like this:</p> <pre><code>class BollingerBandMeanReversion(Strategy):\n    def __init__(\n        self,\n        bollinger_bands_period: int,\n        bollinger_bands_stdev: float,\n        allocation: float,\n        symbol: str\n    ) -&gt; None:\n        self._bollinger_bands = BollingerBands(\n            bollinger_bands_period,\n            bollinger_bands_stdev\n        )\n        self._allocation = allocation\n        self._symbol = symbol\n\n        self._previous_snapshot: BollingerBandSnapshot | None = None\n        self._previous_event: MarketDataEvent | None = None\n\n    def _should_buy(\n        self, \n        prev_close: float,\n        current_close: float,\n        previous: BollingerBandSnapshot, \n        current: BollingerBandSnapshot, \n        holding: bool\n    ) -&gt; bool:\n        return (\n            prev_close &gt;= previous.lower\n            and current_close &lt; current.lower\n            and not holding\n        )\n\n    def _should_sell(\n        self, \n        prev_close: float,\n        current_close: float,\n        previous: BollingerBandSnapshot, \n        current: BollingerBandSnapshot, \n        holding: bool\n    ) -&gt; bool:\n        return (\n            prev_close &lt;= previous.middle\n            and current_close &gt; current.middle\n            and holding\n        )\n\n    def on_market_event(self, event: MarketDataEvent) -&gt; None:\n        close = event.close\n        snapshot = self._bollinger_bands.update(event)\n\n        if self._previous_event is None or self._previous_snapshot is None or snapshot is None:\n            self._previous_event = event\n            self._previous_snapshot = snapshot\n            return\n\n        prev_close = self._previous_event.close\n        holding = self.portfolio_snapshot.position &gt; 0\n        prev_snapshot = self._previous_snapshot\n\n        if self._should_buy(prev_close, close, prev_snapshot, snapshot, holding):\n            units_to_buy = int(self._allocation * self.portfolio_snapshot.cash / close)\n            self.order_api.submit_order(event, self._symbol, units_to_buy)\n        elif self._should_sell(prev_close, close, prev_snapshot, snapshot, holding):\n            units_to_sell = self.portfolio_snapshot.position\n            self.order_api.submit_order(event, self._symbol, -units_to_sell)\n\n        self._previous_snapshot = snapshot\n        self._previous_event = event\n\n    def on_end(self) -&gt; None:\n        holding = self.portfolio_snapshot.position &gt; 0\n        final_event = self._previous_event\n\n        if final_event is None:\n            raise ValueError(\"The final market data event was never set.\")\n\n        if holding:\n            units_to_sell = self.portfolio_snapshot.position\n            self.order_api.submit_order(final_event, self._symbol, -units_to_sell)\n</code></pre>"},{"location":"strategy/rule-based/actions/","title":"Action","text":"<p>An actions is something that executes a single behavior through the <code>execute</code> method, which recieves a context type.</p> <p>See Action Reference</p>"},{"location":"strategy/rule-based/actions/#emit","title":"Emit","text":"<p>Emit is a simple action that emits a singular <code>Intent</code> type upon being executed. No context is used, although it is still passed into the <code>execute</code> method. For example, an Emit action may simply emit an <code>EnterLongIntent</code> intent type, and that would be it's sole purpose.</p> <p>See Emit Reference</p>"},{"location":"strategy/rule-based/conditions/","title":"Conditions","text":"<p>A condition is any boolean when creating a strategy. The definition is intentionally very broad, as a condition can be literally anything, whether it be a change in price or an indicator crosses above another.</p> <p>Conditions can be composed together. This means that the dunder <code>__and__</code>, <code>__or__</code>, and <code>__invert__</code> are overriden, so two conditions can be composed together using the key words <code>and</code>, <code>or</code>, and <code>not</code>. </p> <p>See Conditions Reference</p>"},{"location":"strategy/rule-based/conditions/#predicate","title":"Predicate","text":"<p>A predicate is simply a condition that is supplied a lambda function that returns a boolean. Upon being evaluated, that boolean is freshly generated. Essentially, a Predicate is the simplest possible <code>Condition</code>, and should be used in most use cases.</p> <p>See Predicate Reference</p>"},{"location":"strategy/rule-based/contexts/","title":"Contexts","text":"<p>In a general sense, any context is an immutable value that is supplied to various objects in a rule-based strategy. Specifically, concepts such as position sizers &amp; possibly rules need to be supplied contexts every iteration. A context implementation must contain all of the necessary values for these modules to operate correctly.</p> <p>See Context Reference</p>"},{"location":"strategy/rule-based/contexts/#trading-context","title":"Trading Context","text":"<p><code>TradingContext</code> is the primary context that almost all modules related to rule-based strategy creation use. It simply contains the current market event (OHCLV bar) and the current snapshot of the portfolio, which is generally enough to compute most essential things in a strategy. The majority, if not all, of the tools included in rule-based strategy creation use exclusively <code>TradingContext</code>.</p> <p>See Trading Context Reference</p>"},{"location":"strategy/rule-based/intents/","title":"Intents","text":"<p>An intent is any emitted behavior for a strategy. For example, entering a specific trade would be an intent. Intents are abstracted in an <code>Intent</code> base class so that rule-based strategies can rely on a modular system to communicate with the <code>Strategy</code> base class.</p> <p>See Intents Reference</p>"},{"location":"strategy/rule-based/intents/#enter-long-intent","title":"Enter Long Intent","text":"<p>The <code>EnterLongIntent</code> intent indicates for the rule based strategy to emit a long order for a provided symbol. The amount of shares to buy is calculated by a <code>PositionSizer</code>.</p> <p>See Enter Long Intent Reference</p>"},{"location":"strategy/rule-based/intents/#exit-long-intent","title":"Exit Long Intent","text":"<p>Similarly, the <code>ExitLongIntent</code> intent indicates for the rule based strategy to emit a sell order for a provided symbol. The amount of shares to sell is calculated by a <code>PositionSizer</code>.</p> <p>See Exit Long Intent Reference</p>"},{"location":"strategy/rule-based/position_sizers/","title":"Position Sizers","text":"<p>A position sizer decides how many units to either buy or sell upon an intent (<code>EnterLongIntent</code> or <code>ExitLongIntent</code>) being published. It will buy and sell the same amount of units, as pyramiding is uniformly unsupported throughout the codebase.</p> <p>See Position Sizer Reference</p>"},{"location":"strategy/rule-based/position_sizers/#allocation-position-sizer","title":"Allocation Position Sizer","text":"<p>An allocation position sizer trades a fixed portion of the portfolio upon buying and selling. For example, if 0.3 (30%) is specified, it will attempt in trading 30% of the portfolio value upon an enter long order, and -30% of the portfolio value upon an exit long order.</p> <p>See Allocation Position Sizer Reference</p>"},{"location":"strategy/rule-based/position_sizers/#fixed-position-sizer","title":"Fixed Position Sizer","text":"<p>A fixed position sizer trades a fixed amount of units upon buying and selling. For example, if 3 units is specified, it will attempt in trading 3 units upon an enter long order, and -3 units upon an exit long order.</p> <p>See Fixed Position Sizer Reference</p>"},{"location":"strategy/rule-based/position_sizers/#volatility-position-sizer","title":"Volatility Position Sizer","text":"<p>A volatility position sizer scales the amount of units to buy or sell inversely to volatility. Hardcoded position sizers assume a favorable velocity, but fail on highly volatile or too-little volatile periods. The volatility position sizer uses the same algorithm as TSMOM - if you want to use this, I implore you to go check it out.</p> <p>See Volatility Position Sizer Reference</p>"},{"location":"strategy/rule-based/rule_based_strategy/","title":"Rule-Based Strategies","text":"<p>A rule-based strategy provides a higher-level interface for building strategies from streams, rules, intents, and position sizing. It has similar, if not identical capabilities to a typical <code>Strategy</code> subclass with less boilerplate and more modularity. Essentially, it makes it easier to create strategies if rapidly iterating to find a profitable one.</p> <p>The rule-based strategy system, however, is slightly more opinionated than a normal <code>Strategy</code> subclass. It inherently wraps some logic and may have small limitations; however, given that all of the base classes are subclassable, it should be quite easy to add to the framework without touching the codebase internals.</p> <p>See rule-based strategy reference</p>"},{"location":"strategy/rule-based/rule_based_strategy/#how-to-create-rule-based-strategies","title":"How to create rule-based strategies","text":"<p>Every rule-based strategy must have the following, as illustrated in the <code>__init__</code> method for the class:</p> <pre><code>def __init__(\n    self,\n    streams: Sequence[Stream[Any]],\n    rules: Sequence[Rule[TradingContext, Intent | None]],\n    position_sizer: PositionSizer[TradingContext],\n)\n</code></pre> <p>It should have some source of streams (anything that needs to be updated with <code>MarketDataEvent</code> instances as they're published), rules that determine when to buy &amp; sell in a strategy, and a position sizer to determine how many units to buy upon a buy/sell intent being published.</p>"},{"location":"strategy/rule-based/rule_based_strategy/#example-design-process","title":"Example &amp; design process","text":"<p>Firstly, you want declare what streams your strategy may need. Does it need historical access to market events (most strategies do)? Does it need specific indicators to function? These are all questions that you should clarify before building the actual strategy logic. For a Bollinger Band Mean Reversion strategy, we need historical access to previous market events to detect previous crosses in price and, of course, access to the Bollinger Bands indicator:</p> <pre><code>self.bollinger = HistoricalIndicator(\n    source=BollingerBands(\n        bollinger_bands_period, \n        bollinger_bands_stdev\n    ), \n    window=2\n)\nself.events = HistoricalMarketEvents(window=2)\n</code></pre> <p>Next, we need to create the actual rules for the strategy. For our Bollinger Band Mean Reversion strategy, we want to buy upon the close crossing below the lower band, and sell upon the close crossing back above the middle band. This can be expressed like English:</p> <pre><code>rules: Sequence[Rule[TradingContext, Intent]] = [\n    Rule(\n        when=Predicate(self.price_crosses_below_lower_band),\n        then=Emit(EnterLongIntent(symbol=symbol))\n    ),\n    Rule(\n        when=Predicate(self.price_crosses_above_middle_band),\n        then=Emit(ExitLongIntent(symbol=symbol)),\n    )\n]\n</code></pre> <p>Essentially, it reads exactly like psuedocode: when the close crosses the lower band, then emit an <code>EnterLongIntent</code> for a given symbol. When the close goes back above the middle band, then emit an <code>ExitLongIntent</code> for a given symbol. This is the beauty of rule-based strategies: they're incredibly easy to reason about and understand, even for new developers.</p> <p>Because RuleBasedStrategy performs the framework initialization in its constructor, subclasses must call <code>super().__init__()</code> with their streams, rules, and position sizer.</p> <pre><code>super().__init__(\n    streams=[self.bollinger, self.events],\n    rules=rules,\n    position_sizer=AllocationPositionSizer(allocation),\n)\n</code></pre> <p>Bundling everything together, a full strategy for a Bollinger Band Mean Reversion would look like this:</p> <pre><code>class BollingerBandMeanReversion(RuleBasedStrategy):\n    def __init__(\n        self, \n        bollinger_bands_period: int, \n        bollinger_bands_stdev: float,\n        allocation: float,\n        symbol: str\n    ) -&gt; None:\n        self.bollinger = HistoricalIndicator(\n            source=BollingerBands(\n                bollinger_bands_period, \n                bollinger_bands_stdev\n            ), \n            window=2\n        )\n        self.events = HistoricalMarketEvents(window=2)\n\n        rules: Sequence[Rule[TradingContext, Intent]] = [\n            Rule(\n                when=Predicate(self.price_crosses_below_lower_band),\n                then=Emit(EnterLongIntent(symbol=symbol))\n            ),\n            Rule(\n                when=Predicate(self.price_crosses_above_middle_band),\n                then=Emit(ExitLongIntent(symbol=symbol)),\n            )\n        ]\n\n        super().__init__(\n            streams=[self.bollinger, self.events],\n            rules=rules,\n            position_sizer=AllocationPositionSizer(allocation),\n        )\n\n    def price_crosses_below_lower_band(self, context: TradingContext) -&gt; bool:\n        current_bb, prev_bb = self.bollinger.value, self.bollinger.previous()\n        current_event, prev_event = context.event, self.events.previous()\n\n        if current_bb is None or prev_bb is None or prev_event is None:\n            return False\n\n        return prev_event.close &gt;= prev_bb.lower and current_event.close &lt; current_bb.lower\n\n    def price_crosses_above_middle_band(self, context: TradingContext) -&gt; bool:\n        current_bb, prev_bb = self.bollinger.value, self.bollinger.previous()\n        current_event, prev_event = context.event, self.events.previous()\n\n        if current_bb is None or prev_bb is None or prev_event is None:\n            return False\n\n        return prev_event.close &lt;= prev_bb.middle and current_event.close &gt; current_bb.middle\n</code></pre>"},{"location":"strategy/rule-based/rules/","title":"Rule","text":"<p>A rule simply combines a <code>Condition</code> and an <code>Action</code> into one entity. Upon a <code>Condition</code> being true, when the rule is evaluated, an <code>Action</code> will be executed. This essentially allows you to create a rule that does whatever you need it to - enter a long intent upon an indicator reaching a threshold, selling upon a custom source giving a signal, etc.</p> <p>See Rule Reference</p>"},{"location":"stream/streams/","title":"Streams","text":"<p>A stream can be defined as anything that needs a constant feed of <code>MarketDataEvent</code> instances as they come. This is particularly useful for indicators or historical caches that need constant flows of data. </p>"},{"location":"stream/streams/#usage","title":"Usage","text":"<p>Streams can only be used with rule-based strategies, as it is not set up anywhere else. If used elsewhere (such as with static strategies), all streams must be updated manually via the <code>.update</code> method.</p> <p>To create your own stream, inheret from the <code>Stream</code> abstract class:</p> <pre><code>class MyStream(Generic[R]):\n    \"\"\"\n    A stream that accepts market data events as they are released and produces some type of output.\n\n    Attributes:\n        R: The output type for the stream.\n    \"\"\"\n    def update(self, event: MarketDataEvent) -&gt; R:\n        \"\"\"\n        Updates the state of the stream given a market data event.\n\n        Returns:    \n            The updated value.\n        \"\"\"\n        # Some implementation here\n        ...\n\n    @property\n    @abstractmethod\n    def value(self) -&gt; R:\n        \"\"\"\n        Returns the most recent value of the stream.\n        \"\"\"\n        # Some implementation here\n        ...\n</code></pre> <p>See stream reference</p>"},{"location":"stream/streams/#historical-streams","title":"Historical Streams","text":"<p>A historical stream maintains a lookback, given a <code>Stream</code> implementation. It maintains the n amount of previous values efficiently and allows them to be accessed through accessors. </p> <p>One example of this is the historical market events stream, which maintains the last n amount of market events by creating a dummy stream specifically to store market events.</p> <p>See historical stream reference</p>"},{"location":"stream/indicators/indicators/","title":"Indicator Calculations","text":"<p>Indicators are generally used for statistical analysis (using OHLCV data to determine when to trade). Although this list will not go into depth on what each indicator means, it will briefly mention some of the indicators available in the codebase:</p> <ul> <li>Average True Range (ATR): Measures market volatility by averaging the true range (largest of the current high-low range, or gaps from the prior close) over a set period.</li> <li>Bollinger Bands: Plots bands above and below a moving average based on standard deviation, used to gauge volatility and potential overbought/oversold conditions.</li> <li>Exponential Moving Average (EMA): A moving average that weights recent prices more heavily, making it more responsive to new price changes than a simple average.</li> <li>Relative Strength Index (RSI): A momentum oscillator that measures the speed and magnitude of recent price changes to identify overbought or oversold conditions.</li> <li>Simple Moving Average (SMA): The unweighted average of a security's price over a specified number of periods.</li> <li>True Range: The greatest of the current high minus low, current high minus previous close, or current low minus previous close, used as the basis for volatility measures like ATR.</li> <li>Volume Weighted Average Price (VWAP): The average price of a security weighted by trading volume over a given period, often used as a benchmark for trade execution quality.</li> <li>Wilder Average: A smoothing technique (developed by J. Welles Wilder) that applies more weight to recent data while retaining a longer memory of past values, used as the basis for indicators like ATR and RSI.</li> </ul>"},{"location":"stream/indicators/indicators/#indicator-state","title":"Indicator State","text":"<p>Often, strategies use indicator states (price below or above an indicator) rather than looking for specific values. State machines for each of the indicators are included. For example, a few states:</p> <p>EMA -&gt; States ABOVE and BELOW Bollinger Bands -&gt; States BELOW_LOWER, BETWEEN_LOWER_AND_MIDDLE, BETWEEN_MIDDLE_AND_HIGHER, and ABOVE_HIGHER.</p>"},{"location":"trading/analysis/graphing/","title":"Graphing","text":"<p>The generated graphs from the metrics reveal the quality of trades, profitability, and risk of the parameters tested for a strategy. The graphs were carefully chosen to all tell something different about the execution of a strategy - this documentation will tell you exactly how to interpret them.</p> <p>Note: All of the examples here use the demo parameterized bollinger band mean reversion strategy, which is included in the research/ folder. It was tested from the start of 2000 to the start of 2026 using daily bars.</p>"},{"location":"trading/analysis/graphing/#risk-return-overview","title":"Risk Return Overview","text":""},{"location":"trading/analysis/graphing/#what-is-it","title":"What is it?","text":"<p>The risk return overview compares the <code>sharpe ratio</code>, <code>calmar ratio</code>, <code>annual return (CAGR)</code>, and <code>max drawdown</code> amongst the parameterized backtests. </p> <p>The x-axis holds the annual return (CAGR), and the y-axis holds the <code>sharpe ratio</code>. Larger points indicate a higher max drawdown, and a brighter yellow <code>calmar ratio</code> color indicates a higher <code>calmar ratio</code>.</p>"},{"location":"trading/analysis/graphing/#what-should-you-be-looking-for","title":"What should you be looking for?","text":"<p>The \"optimal\" point would be a small point with a brighter yellow color at the top right of the graph; but be warned, this alone does not signify a good strategy. You want to look for clusters and consistencies between parameters (so either clumps or patterns between the points in the graph).</p>"},{"location":"trading/analysis/graphing/#things-to-watch-out-for","title":"Things to watch out for:","text":"<ul> <li>Singular outlier points that outperform the rest - this is likely overfitting for certain circumstances rather than a genuine edge.</li> <li>Low trade count - hover over the points. If you see a low trade count, you may not want to look at further graphs because your trade count is too low for realistic metrics and results, unless if this is intended.</li> </ul>"},{"location":"trading/analysis/graphing/#example","title":"Example:","text":"<p>Let's interpret this graph. We see scattered, warm points on the top right of the graph - this does not signify consistency. On the bottom left, we see cooler points clustered together - this is where most of the parameters ended up. The maximum <code>calmar ratio</code> reached only ~0.2, so this suggests that the strategy is too risky for the profits to make sense.</p> <p>Looking at the CAGR (annual return), we see that the clusters are all below a 4.0% annual return, which is low. </p> <p>The <code>sharpe ratio</code> isn't much better, sitting at 0 to 0.3 for the majority of the points, telling us that the drawdown does not make sense for the little profit the backtests made.</p> <p></p> <p>Most of the points are large. Hovering over, we see that a lot of them are at 50-55% drawdown, which is a bad sign (very high risk).</p> <p>Finally, we see that the trade count sits at around ~200, which is a realistic amount of trades for the 26 years that the strategy traded. There is sufficient amount of data to make conclusions about the strategy.</p> <p>Just from this first graph, we can conclude that the strategy as is unprofitable (assuming good parameters were picked for the test). The risk and drawdown does not make sense for the little profit it generates, which all results in this graph point to.</p>"},{"location":"trading/analysis/graphing/#parameter-importance","title":"Parameter Importance","text":""},{"location":"trading/analysis/graphing/#what-is-it_1","title":"What is it?","text":"<p>The parameter importance graph shows which parameters have the most weight in the deviation of results that the strategy generated.</p>"},{"location":"trading/analysis/graphing/#what-should-you-be-looking-for_1","title":"What should you be looking for?","text":"<p>You should be looking at the size of the bar for each parameter tested &amp; parameterized. A larger bar indicates that a parameter has more weight on the results, while a smaller bar (or no bar) indicates that it has little or no importance on the generated results.</p> <p>This helps you determine what parameters to tune. You should be focused on tuning parameters with more weight than ones with less of an impact.</p>"},{"location":"trading/analysis/graphing/#things-to-watch-out-for_1","title":"Things to watch out for:","text":"<ul> <li>Not testing enough parameters. If you do not test a variety of a parameter, it will inherently report back as low importance, which could be misleading. Keep note of this.</li> <li>Reliance solely on this metric for tuning - even if this graph says that one or two parameters dominate, you should not completely ignore other parameters as they may have importance unrelated to how the variance is calculated. It should influence your tuning, not fully dictate it.</li> </ul>"},{"location":"trading/analysis/graphing/#example_1","title":"Example:","text":"<p>As you can see, for the example bollinger band mean reversion strategy, the period dominates. This is a clear indicator as you tune the strategy parameters that the period value will have the most weight, while the allocation and stdev parameters may have less significance.</p> <p>This graph is relatively easy to interpret; it does not prove strategy profitability, but it helps you tune your strategies no matter the results from any other graphs.</p>"},{"location":"trading/analysis/graphing/#parallel-parameter-combinations","title":"Parallel Parameter Combinations","text":""},{"location":"trading/analysis/graphing/#what-is-it_2","title":"What is it?","text":"<p>Each line is one experiment (backtest); axes are the swept parameters plus the target metric. Lines are colored by the target metric (supplied in the constructor). This graph tells you if a parameter has an optimal zone, or if the majority of the results are possibly noise / overfitting.</p>"},{"location":"trading/analysis/graphing/#what-should-you-be-looking-for_2","title":"What should you be looking for?","text":"<p>You want to be looking for narrow, green bands (where bands are compositions of multiple lines). Narrow, green bands indicate consistent results across a type of parameter.</p> <p>On the other hand, narrow &amp; red/orange bands indicate that the strategy is consistently underperforming. This usually indicates that something in the strategy may have an edge, but it isn't being properly taken advantage of.</p> <p>Worst of all, scattered lines means that there is likely no real consistent edge at all, and that most of your results were random with little or no pattern involved.</p>"},{"location":"trading/analysis/graphing/#things-to-watch-out-for_2","title":"Things to watch out for:","text":"<ul> <li>Not all green lines are good. Even if a strategy produces a decent amount of green results, if not banded together, other market circumstances other than your actual strategy are likely influencing the results.</li> <li>Make sure to check the actual observed ranking metric (defaults to calmar ratio) to see if the metric is actually high. For example, green lines mean nothing if the maximum recorded calmar ratio is 0.2 - the strategy is not yielding good results, even though the lines are green &amp; clustered.</li> </ul>"},{"location":"trading/analysis/graphing/#example_2","title":"Example:","text":"<p>The range of calmar ratios are from -0.02 to ~0.2 - this in itself is a red flag that the strategy is not yielding good results.</p> <p>Nonetheless, looking at the leftmost part of the graph, we see that from the period to the stdev parameters, the lines are concentrated in red &amp; orange colors. Now look at the rightmost part of the graph - here, we see (after all parameters are compounded), that there are genuine narrow bands. However, the bands are red / orange, only equating to calmar ratios of -0.02 to 0.06, which is very little. </p> <p>The scattered green results are noise. Be careful when analyzing your graph and be sure not to consider them - they typically have no edge and are simply overfitted parameters for these exact results. If you were to use a different dataset, it's very likely that they would not yield good results as they are for this one.</p> <p>We can conclude once again that this is unprofitable, although now we can conclude that it is also consistent. There may be an edge, but we are not capitalizing on it with our raw bollinger band mean reversion strategy.</p>"},{"location":"trading/analysis/graphing/#pairwise-heatmap-grid","title":"Pairwise Heatmap Grid","text":""},{"location":"trading/analysis/graphing/#what-is-it_3","title":"What is it?","text":"<p>The pairwise heatmap grid shows one heatmap per third parameter (allows for up to three parameters to graph). Each shape in the heatmap grid shows the designated metric (defaults to calmar ratio) as a heated color for a parameter space. The heatmap helps determine if a result is overfitting or has a genuine edge.</p>"},{"location":"trading/analysis/graphing/#what-should-you-be-looking-for_3","title":"What should you be looking for?","text":"<p>A genuine edge will show a color gradient fading away from a cooler color. For example, if one parameter space was green, the next may be light green or yellow, and the next one after that may be orange or red. </p> <p>Overfitting (not an edge) will be extremely obvious - a cooler result surrounded by hotter colors directly adjacent to it.</p>"},{"location":"trading/analysis/graphing/#things-to-watch-out-for_3","title":"Things to watch out for:","text":"<ul> <li>Outliers that hinder your results. The color scale makes the coolest colored result the highest ranking result for that metric. This means that if an outlier yields unrealistic results (calmar ratio of 10.0, for example), the rest of your results will be hotter colors. The best way to combat this is to filter your results programatically to prevent outliers.</li> </ul>"},{"location":"trading/analysis/graphing/#example_3","title":"Example","text":"<p>Look at the leftmost heatmap with allocation as 0.25. At the bottom left of the heatmap, there is a dark green area. There seems to be a slight color gradient, but it isn't completely obvious at a glance.</p> <p>One nuance - the green sector is in the bottom left of the heatmap, which corresponds to a period of 5. This is relatively low, which means that the strategy was trading extremely frequently. This could indicate that the strategy yields more profits (and less risk) by trading more frequently, but it would need more testing with different datasets to confirm this.</p> <p>Now look at the rightmost heatmap with allocation as 1.0. At the top of the graph, there are two dark green sections surrounded by red. This is likely overfitting, and these sections should not be considered.</p> <p>We can now conclude that a bollinger band mean reversion strategy may have a genuine edge worth testing by altering the logic of the strategy.</p>"},{"location":"trading/analysis/graphing/#metric-distribution","title":"Metric Distribution","text":""},{"location":"trading/analysis/graphing/#what-is-it_4","title":"What is it?","text":"<p>The metric distribution shows the variance between the results for a certain parameter. Tight boxes indicate little variance (high consistency), while wide boxes indicate much variance (low consistency). A higher median for the box &amp; whisker plots means that the average of the backtests performed well; a higher upper fence or max does not necessarily mean that those results are consistent.</p>"},{"location":"trading/analysis/graphing/#what-should-you-be-looking-for_4","title":"What should you be looking for?","text":"<p>You want to look for tight boxes that with medians that yield high returns. Wide boxes indicate inconsistencies and likely randomness, while tighter boxes indicate a possible edge &amp; consistency.</p>"},{"location":"trading/analysis/graphing/#things-to-watch-out-for_4","title":"Things to watch out for:","text":"<ul> <li>Results that never actually traded. If parameters for a backtest never made a trade for a parameter, they may hinder your min &amp; lower fences, although the median should not be significantly affected (unless if the majority of your results never traded).</li> </ul>"},{"location":"trading/analysis/graphing/#example_4","title":"Example","text":"<p>Look at the leftmost box with the period parameter as 5. It is significantly wider than all of the other boxes. If you recall from earlier, we said that a period of 5 yielded the highest results of all the parameters, so it may have an edge - this directly refutes that hypothesis. Notice how a few backtests had massive returns, while others had little or no returns, artificially inflating the median. This is why it had a higher calmar ratio than the others.</p> <p>As for the other boxes, they are tight and narrow. This means that the backtests yielded similar results to each other, so the majority of the periods are consistent. However, notice how low the returns are. Although they are consistent, they are consistently yielding low profit.</p>"},{"location":"trading/analysis/graphing/#equity-curve-overlay","title":"Equity Curve Overlay","text":""},{"location":"trading/analysis/graphing/#what-is-it_5","title":"What is it?","text":"<p>The equity curve overlay shows the account value over time (USD) for each parameterized strategy. This helps you visualize how each strategy actually performed over time and if there are any unusual behaviors (such as massive dips or massive leaps) in the profits for the strategy. </p>"},{"location":"trading/analysis/graphing/#what-should-you-be-looking-for_5","title":"What should you be looking for?","text":"<p>In an ideal world, your profits would be steady &amp; linear. You want to look for some semblance of consistent profits throughout your time period, and no drastic changes based on different market conditions.</p>"},{"location":"trading/analysis/graphing/#things-to-watch-out-for_5","title":"Things to watch out for:","text":"<ul> <li>As usual, you want to look for &amp; ignore any outliers you found beforehand or any wildly high / low results that do not make sense.</li> </ul>"},{"location":"trading/analysis/graphing/#example_5","title":"Example","text":"<p>At the top of the equity curve graph, you see that some of the equity curves are more profitable than the others - significantly so. Looking to the right, we notice that they're part of our outlier group, period 5. These should be ignored.</p> <p>Lower in the graph, notice how all of the equity curves are bundled together, most making very little profit (~110% in 20 years). This aligns perfectly with our previous findings - the strategy is consistent, but unprofitable as-is.</p>"},{"location":"trading/analysis/graphing/#underwater-drawdown-plot","title":"Underwater Drawdown Plot","text":""},{"location":"trading/analysis/graphing/#what-is-it_6","title":"What is it?","text":"<p>The underwater drawdown plot tells us the maximum drawdown for the parameterized strategy. Massive maximum drawdowns suggest that the strategy does not handle losses well (or not at all), and is extremely risky because of it. A larger graph going downwards means a larger maximum drawdown.</p>"},{"location":"trading/analysis/graphing/#what-should-you-be-looking-for_6","title":"What should you be looking for?","text":"<p>You should be looking for small underwater graphs &amp; as always, consistency between different parameter combinations.</p> <p>Any large graphs going downwards indicate large risk that may or may not be worth it for your strategy. Generally speaking, you want to aim for strategies to be at around -10% drawdown or less (dependant completely on the allocation you are trading).</p>"},{"location":"trading/analysis/graphing/#things-to-watch-out-for_6","title":"Things to watch out for:","text":"<ul> <li>Outliers may artificially inflate the maximum drawdown from peak - as always, either filter them, ignore them, or hide them in the graph.</li> </ul>"},{"location":"trading/analysis/graphing/#example_6","title":"Example","text":"<p>From just a glance, you can tell that the drawdown is massive, sitting at -60% for the riskiest strategies. However, you must consider the outliers for this set - we determined that the outliers are mostly all of a period of five. Hide them from the graph by left clicking them on the right sidebar.</p> <p>Even after hiding them, the core losers stay the same (I didn't include a second picture because it visibly changed nearly nothing). Ignoring the biggest losers, we can see that the majority of parameters are relatively big losers anyway (~-30%). This is a red flag.</p> <p>What we can determine from this is that the low calmar &amp; sharpe ratios from before were at least partially from the massive drawdowns generated from the parameters. The amount of risk and the little profit generated (CAGR from the risk return overview) results in painfully low calmar ratios. </p>"},{"location":"trading/analysis/graphing/#trade-quality-scatter","title":"Trade quality scatter","text":""},{"location":"trading/analysis/graphing/#what-is-it_7","title":"What is it?","text":"<p>The trade quality scatter compares the win rate on the x-axis, profit factor on the y-axis, trade count by the point size, and a ranking metric (defaults to calmar ratio) as the color.</p>"},{"location":"trading/analysis/graphing/#what-should-you-be-looking-for_7","title":"What should you be looking for?","text":"<p>The ideal point would be big, yellow, and at the top right of the chart. This isn't very likely though - what you should look for is any decently sized point with a profit factor &amp; win rate that is profitable. Generally speaking, look for a profit factor of above ~1.5 and a win rate over 50%.</p>"},{"location":"trading/analysis/graphing/#things-to-watch-out-for_7","title":"Things to watch out for:","text":"<ul> <li>Outliers that may be caused by overfitting. If there are any wildly high / low results, this either indicates a low trade count (very small dot) or an overfitted parameter set - both should be ignored.</li> </ul>"},{"location":"trading/analysis/graphing/#example_7","title":"Example","text":"<p>Looking at the graph, we see that the maximum profit factor is 7.0. This is caused by parameters that resulted in a very low amount of trades. These are outliers. We should zoom in to the concentrated results to exclude the outliers.</p> <p></p> <p>You might notice a pattern of nearly identical results having slightly higher profit factors - this is because the allocation has no effect on the win rate, but affects the profit factor. This should be ignored as it is just noise. </p> <p>The takeway here is that the strategy has a high win rate (64%-76%) with a very low profit factor (most around 1.2-1.3). As discussed in earlier results, the calmar ratio is still low (maximum of 0.2) and sufficient trades are made for reliable results.</p>"},{"location":"trading/analysis/graphing/#conclusion","title":"Conclusion","text":"<p>We can conclude that this strategy is not sufficiently profitable as of now. The risk return overview revealed consistently low calmar ratios. The parallel parameter combination graph revealed consistency in parameters and a possible edge with bollinger bands, although not capitalized with this strategy. The heatmap did not provide any useful information this time, except bringing outliers to our attention. The metric distribution and equity curve both reinforced the same consistency we found earlier. The underwater drawdown plot highlighted a major reason for the low results - massive drawdowns upon the majority of our backtests. Finally, the trade quality scatter illustrated the low overall profit factor (but high win rate) for the strategy.</p>"},{"location":"trading/analysis/graphing/#what-next","title":"What next?","text":"<p>Given this information about the strategy, it must be altered and improved before actually being considered for the real market. We want to capitalize on the faults that we found here. A high drawdown can be reduced with stop losses; specifically, a mean reversion strategy like this would likely benefit from an ATR-based stoploss. Finally, a low profit factor can be improved by implementing another indicator (possibly an EMA or SMA trend factor, but this would have to be thoroughly tested). </p> <p>Anyway, I encourage you to experiment yourself! You should be familiar with a good workflow to identify &amp; interpret strategy results.</p>"},{"location":"trading/execution/backtester/","title":"Backtester","text":"<p>The backtester is a \"mode\" or way to iterate through a dataset using the engine. It allows for data to be processed without actual tools, brokers, or additional software, instead simulating all of it to model real life as accurately as possible.</p>"},{"location":"trading/execution/backtester/#backtester-config","title":"Backtester Config","text":"<p>A config is specialized for the backtester to determine simulated portfolio state, fill behavior, slippage modeling, commissions, etc. It must be passed to the strategy backtester upon conducting a backtest.</p> <p>See backtester config reference</p>"},{"location":"trading/execution/backtester/#strategy-backtester","title":"Strategy Backtester","text":"<p>The main backtester module. Via the static <code>.backtest(...)</code> method, a single backtest can be executed given a config, strategy, and OHLCV data. It will return execution data after iterating through the strategy &amp; OHLCV data.</p> <p>Nuances: The backtester does NOT support pyramiding (which means you must sell all of the shares you bought in a previous trade - no exceptions), nor multiple tickers, nor any short trades. This will obviously be changed very soon in the future, although the backtester is powerful nonetheless. Orders that violate these rules will be rejected by the backtester.</p> <p>See backtester reference</p>"},{"location":"trading/execution/engine/","title":"Engine","text":"<p>The engine holds the events, results, strategy base classes, and any other universal API / utility for a mode of trading. For example, one mode of trading showcased in this project is the backtester. The idea is that a strategy can be written through the engine, and any mode of trading can handle the same exact logic however it would like.</p>"},{"location":"trading/execution/engine/#events","title":"Events","text":"<p>Events are tuples of data that can be published and subscribed to by different modules &amp; methods through an event bus. </p> <p>For example, a <code>MarketDataEvent</code> can be published by one method, and all of its subscribers will receive the event sequentially by their explicit priorities. This allows for chaining of different event types without synchronously in a codebase, which significantly declutters the code and prioritizes readability.</p> <p>Some of the main events (but not necessarily limited to):</p> <ul> <li><code>MarketDataEvent</code> - Emitted for every OHLCV bar in a dataset (contains the close, open, high, low, volume, for a single symbol).</li> <li><code>OrderEvent</code> - A simple market order emitted after a user submits it through the order API. </li> <li><code>StoplossOrderEvent</code> - A stoploss order emitted after a user submits it through the order API.</li> <li><code>AcceptedFillEvent</code> - An accepted and filled order.</li> <li><code>RejectedFillEvent</code> - An order that was rejected and was not filled.</li> <li><code>PortfolioSnapshotEvent</code> - A snapshot of the portfolio for the current period in time.</li> </ul> <p>See event reference and event bus reference</p>"},{"location":"trading/execution/engine/#orders","title":"Orders","text":"<p>Orders are published by the order API component. The order API has a separate method for each event type that it can publish to the bus - for example, <code>OrderEvent</code> and <code>StoplossOrderEvent</code>. The order API is traditionally injected into a <code>Strategy</code> implementation by a separate component so that it can create orders and publish order events.</p> <p>See order API reference</p>"},{"location":"trading/execution/engine/#results","title":"Results","text":"<p>The engine inherently returns raw tuples of the events published for an execution. The results will always be in order by timestamp made or executed. Other modules must interpret these raw events; the execution engine does not hold this responsibility.</p> <p>See execution data reference</p>"},{"location":"trading/execution/engine/#strategy","title":"Strategy","text":"<p>A strategy is the base class that creates orders for the rest of the engine or mode to handle. Custom logic is made to determine when these orders should be made; the execution engine handles the data collection &amp; event logic. </p> <p>Variables such as <code>order_api</code> and <code>portfolio_snapshot</code> are injected and updated consistently to allow orders to be made and access the updated portfolio state respectively.</p> <p>See strategy reference</p>"},{"location":"trading/execution/engine/#see-more","title":"See More","text":"<p>Backtester</p>"},{"location":"trading/metrics/generation/","title":"Metric Generation","text":"<p>The metric system generates metrics to help with both graphing (visual analysis) &amp; strategy optimization. It contains the necessary information, derived from raw events, to determine profitability, risk, and factor out any results that indicate overfitting or unreliability.</p>"},{"location":"trading/metrics/generation/#metrics","title":"Metrics","text":"<p>A tuple containing the generated metrics for a strategy execution. It holds the strategy metrics for the following sub-categories:</p> <ul> <li><code>Return Metrics</code>: Metrics relating to returns (total return, monthy returns, equity curve, etc).</li> <li><code>Risk Metrics</code>: Metrics relating to reliability (annual return, sharpe ratio, monthly volatility, calmar ratio, etc).</li> <li><code>Drawdown Metrics</code>: Metrics relating to the worst possible outcomes of the strategy (max drawdown, average drawdown).</li> <li><code>Trade Metrics</code>: Metrics relating to the general profitability of the strategy (trade count, win rate, profit factor, expectancy, average loss, average win, average holding period, etc).</li> </ul>"},{"location":"trading/metrics/generation/#metric-definitions","title":"Metric Definitions","text":"<p>You may not be familiar with what these metric terms indicate or mean. Here are definitions and interpretations of all of them:</p>"},{"location":"trading/metrics/generation/#return-metrics","title":"Return Metrics","text":"<ul> <li><code>total return</code>: The percent of cash that your strategy generated over a time period. Generally speaking, a higher total return is better.</li> <li><code>monthly returns</code>: The percent of cash that your strategy generated for each individual month. Consistency across the months is ideal; inconsistencies may point to the strategy favoring specific timestamps rather than others.</li> <li><code>equity curve</code>: The cash in your portfolio for every bar timestamp. This shows you how your portfolio fended over time - if there are extreme dips or extreme increases in cash, that may indicate a few massive trades rather than consistent small ones being made.</li> </ul>"},{"location":"trading/metrics/generation/#risk-metrics","title":"Risk metrics","text":"<ul> <li><code>annual return</code>: The average percent of cash that your strategy generates per year. A higher annual return is better; this helps measure consistency across a time period if you see deviations elsewhere.</li> <li><code>sharpe ratio</code>: The sharpe ratio is the risk-adjusted return of a strategy - it shows how much excess return an investment generates per unit of total risk taken. Generally speaking, a sharpe ratio over 1.0 is considered good. Below 1.0 suggests that the investment may not justify the risk taken.</li> <li><code>monthly volatility</code>: Volatility measures how rapidly and unpredictably your returns move per month. A lower monthly volatility suggests consistency in results; a higher monthly volatility may indicate massive swings and returns rather than consistency.</li> <li><code>calmar ratio</code>: The risk-adjusted performance metric that measures an investments annualized return relative to its maximum drawdown. Similarly to the sharpe ratio, it shows if a strategies profits are worth it given it's maximum possible drawdown (loss). Generally speaking, a calmar ratio over 1.0 is considered good.</li> </ul>"},{"location":"trading/metrics/generation/#drawdown-metrics","title":"Drawdown Metrics","text":"<ul> <li><code>max drawdown</code>: The maximum drawdown is the maximum loss that the strategy made over a backtest. A lower max drawdown indicates a strategy that can handle unfavorable environments without losing hard.</li> <li><code>average drawdown</code>: The average of all the drawdowns for a strategy made over a backtest. A lower average drawdown indicates that a strategy consistently handles and reduces losses.</li> </ul>"},{"location":"trading/metrics/generation/#trade-metrics","title":"Trade Metrics","text":"<ul> <li><code>trade count</code>: The amount of trades made over a backtest. A lower trade count may point to the results being inaccurate because of a lack of tests to verify it over.</li> <li><code>win rate</code>: The percent chance that a strategy generates a win (over 0.0 profit). A higher win rate is generally better, but it does not always indicate a profitable strategy; you must look at the profit factor first.</li> <li><code>profit factor</code>: The profit made per trade compared to the gross loss. For example, a PnL of 1.63 would indicate that the strategy made $1.63 for every $1.0 loss. A PnL below 1.0 is unprofitable, 1.2-1.5 signifies a decent edge, and above that points to a very strong edge (assuming other metrics match).</li> <li><code>expectancy</code>: The expected profit per trade with the strategy. A higher expectancy is better as it indicates that a strategy yields consistent profits.</li> <li><code>average loss</code>: The average percent lost per loss. An average loss closer to 0.0 is better.</li> <li><code>average win</code>: The average percent won per win. A higher average win is better.</li> <li><code>average holding period</code>: The average amount of time that a strategy holds per trade. This is proportional to the trade count, and could be either a good or bad sign for your strategy (depending on what you expect your strategy to do).</li> </ul> <p>See metrics reference</p>"}]}