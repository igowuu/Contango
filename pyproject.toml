[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stocks"
version = "0.1.0"
description = "Stock analysis and optimization tools"
requires-python = ">=3.11"
dependencies = [
]

[tool.setuptools.packages.find]
where = ["."]

[tool.pytest.ini_options]
pythonpath = "src"
